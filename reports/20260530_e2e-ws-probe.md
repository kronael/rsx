# 20260530 — e2e WS-probe latency (baseline, gateway-bound)

**What:** end-to-end order round-trip over the live cluster (gateway→risk→ME→risk→gateway), driven by the FastAPI dashboard's aiohttp WS probe, closed-loop sequential. Single shard, 6-core box.

## Numbers
- **WS-probe e2e:** p50 **11,618 µs** · p99 51,860 µs · N=842 (closed-loop).
- **GW-only RTT** (Python aiohttp + WS + gateway reject, no ME): **143 µs** — i.e. the client/WS/gateway envelope is ~143 µs, NOT the 11.6 ms.
- **Single-order stage trace** (cumulative µs from `gateway_in`):
  `gateway_in 0 → risk_in 65 → me_in 493 → risk_out 556 → me_out 571 → gateway_cast_recv 4794 → gateway_out 4834`
- **`me_out → gateway_cast_recv` gap:** ~**0.8 ms p50 / ~10 ms p90** over 1934 probes.

## Conclusion
The ~11 ms is **not** Python and **not** risk/ME (those are sub-ms). It's the **gateway casting-recv poll loop starving on the shared monoio reactor** — the response sits in the kernel UDP socket buffer (and then the per-connection `outbound` VecDeque) waiting for reactor turns. `me_out → gateway_cast_recv` is essentially the whole e2e gap.

## Caveats
Closed-loop sequential (latency ≈ in-flight/throughput under saturation); Python client envelope ~143 µs; single shard; synthetic load. This is the **baseline for the gateway egress-tile-split** (decouple casting-recv to a pinned busy-spin tile → SPSC → WS writers; shard reactors).

Source: live cluster + `scripts/latency-publish.sh` + `log/*.log` stage samples.
