---
summary: |
  RSX perp exchange. Solo work. P0/P1/P2 correctness sweep.
  - CMP in-memory send_ring added to CmpSender for NAK retransmit without WAL
  - rsx-maker WS market maker implemented
  - P0: book overflow guard, snapshot bounds checks, WAL sync_all logging,
    flag bits in WAL writes, proxy crash fix, missing REST endpoints
  - P1: liquidation recovery trigger, margin overflow → i64::MIN, seq gap
    detection, maker reconnect backoff, gateway write error logging
  - P2: decode_record! macro (replaced 9 duplicate fns), unlink_order(),
    60s book eviction, CID validation aligned
  - 31 new E2E/integration tests: gateway session isolation, risk WAL crash
    recovery, liquidation cascade, insurance fund deficit
---

## 20:44

CMP NAK retransmit: added in-memory `send_ring` to `CmpSender` so
`handle_nak` can retransmit lost UDP packets without touching the WAL
(585fd84). Spec added: `specs/cmp-nak.md`. Rust maker `rsx-maker/src/main.rs`
expanded to full WS market maker (197 LOC). `market_maker.py` + `server.py`
wiring fixed. `useRestApi` `MetadataResponse` types corrected.

## 21:01

P0 correctness bugs (2a2b303):
- `emit()` now guards against `MAX_EVENTS` overflow with warning.
- Slab + user free-list bounds checked on snapshot load.
- `sync_all` failures logged before WAL rotate (were silent).
- `reduce_only`/`tif` flags populated from `OrderSlot` in WAL writes.
- `/v1/proxy` JSON parse wrapped in try/except (was crashing on 502).
- Added `/v1/account`, `/v1/positions`, `/v1/orders`, `/v1/fills` to
  playground server.

## 21:04

P1 silent failures (950493f):
- Liquidation recovery: `cancel_if_recovered()` now called when user returns
  above maintenance margin.
- `unrealized_pnl` overflow returns `i64::MIN` (not `MAX`) so user fails
  margin check correctly.
- Marketdata: seq gap detection with `warn!` log.
- Maker: exponential backoff reconnect.
- Gateway: WS write failures now logged.
- `server.py`: `latency_us` `None` guard; maker error eviction fixed to remove
  only the erroring cid, not both sides.

## 21:08

P2 simplification (c1156db): replaced 9 duplicate `decode_*_record` functions
with `decode_record!` macro. Extracted `unlink_order()` to eliminate duplicated
linked-list logic. CID validation aligned to 1–20 chars in parser and handler.
60s book eviction for symbols with no marketdata subscribers. `env_var()` helper
in dxs config. Synthetic candle seed varied by symbol+timeframe.

## 21:13

Added 31 missing E2E/integration tests (19736e7, +1077 LOC):
- Gateway: concurrent session isolation (third user sees nothing).
- Risk: 12 tests — position=sum-of-fills, WAL crash recovery, liquidation
  cascade, insurance fund deficit absorption.
- Liquidator: 18 tests — largest-position-first, partial liquidation, order
  rejection during liq, price-drop trigger, round escalation slippage schedule,
  ORDER_FAILED retry with higher slip.

## 21:21

Hotfix: `evict_stale_books` was calling nonexistent `subscriber_count` method;
replaced with `clients_for_symbol` (53ea1c7). Single-line fix caught by
compiler after the P2 refactor.
