---
summary: |
  RSX perp exchange. Solo work. 2026-W07 day 3. Light day.
  - Frontend spec audit: post_only, bitmask fix, TIF/RO/PO in WEBPROTO.
  - Deployment system: run.py dev runner, rsx-maker/rsx-sim stubs,
    DEPLOY.md rewrite, async process management, interactive REPL.
  - open: integration not yet validated end-to-end.
---

## 08:15

Frontend spec audit: WEBPROTO.md bitmask corrected, `post_only` added to
`RiskNewOrder`, trades feed and metadata frames spec'd; GATEWAY.md and
MARKETDATA.md updated (4dabe16, d114a1e).

## 19:11

Deployment system shipped: `run.py` (renamed to `./start` at 19:23) handles
build + async process management for all RSX binaries. `rsx-maker` and
`rsx-sim` stubs added to workspace (3f54d3f). DEPLOY.md rewritten 286→909
LOC. Short flags, `-c` wipes tmp, `--reset-db` recreates database (6f55221).
Interactive REPL added: `start/stop/kill/restart` individual workers at
runtime without stopping the whole system (8456f8b).
