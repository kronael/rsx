---
summary: |
  RSX perp exchange. Solo work. Wedge strategy active:
  rsx-dxs as open-source standalone (already domain-agnostic).
  Open items:
  - monoio-native UdpSocket on CmpReceiver (~50 LOC) — deferred
  - Aeron deep-dive + facts/aeron-vs-dxs.md to write
  - rsx-dxs naming decision (CMP/DXS vs new name)
  - rsx-dxs README polish + crates.io publish plan
  - re-run audit + browser pass once monoio port lands
  - generalize WAL archive discovery via list_wal_files_across(dirs)
  - sqlite manifest of WAL files (rebuildable from scan) — replaces O(files) listing
  - drop redundant `_last` from WAL filenames once manifest exists
---

## 09:09

`docs/benches.md` — full index of all 28 measurement
programs (26 Criterion bench files + 2 binaries), commit
0e0f6fa. Cross-references `.ship/18-COMPONENT-BENCHES/
LANDSCAPE.md` for numbers and `facts/syscall-latency.md`
for the syscall-level "why". Added 4-line docstrings to
10 previously-undocumented older bench files.

## 09:10

New convention shipped to global CLAUDE.md (and synced to
~/app/tools): **`facts/`** for researched findings (YAML
frontmatter with sources / date / status, checked in),
**`refs/`** for cloned external repos (gitignored, scratch).
Complements `.diary/` (chronological) and `.ship/` (in-flight
scratch).

## 09:10

First `facts/` entry: `facts/syscall-latency.md`. Researched
+ measured reference on x86_64 Linux syscall cost. Key
numbers: vDSO clock_gettime 13-24 ns; bare syscall with
PTI+Spectre 200-500 ns; sendto UDP loopback 144B 3.85us
(own); io_uring SQPOLL zero syscalls (kernel polls SQE
ring); DPDK 1-10us per packet. Sourced from 7 external
papers + my own dfe2ef4 measurement.

## 09:10

`/fin` SKILL improvement landed yesterday (e907af7-ish in
~/app/tools): stopping discipline + open-items pass +
cascade guard. `/ship` SKILL step 7 rewritten from "trim or
archive (don't delete)" to "close-out + prune" with
distillation routing table. Validated the new discipline by
pruning 5 shipped sprints + moving WEDGE.md → specs/2/50-wedge.md.

## 09:10

Important leftover from yesterday's send-path attribution
(dfe2ef4) is now in three places: the bench itself in
`rsx-dxs/benches/cmp_send_breakdown_bench.rs`, the
attribution table in LANDSCAPE.md, and the index entry in
`docs/benches.md`. Won't get lost.

## 09:10

MEMORY.md (user-claude) was updated yesterday with current
state. No stale entries today. The "open" items from
2026-05-23 carry forward (monoio-native UdpSocket refactor
remains deferred; other two resolved).

## 14:XX

WAL retention bumped 10min → 48h: rsx-matching + rsx-mark
call sites, CLAUDE.md, specs/2/10-dxs.md, specs/2/48-wal.md.
Commit 8468bad. Decision: CMP stays in rsx-dxs (NAK cold-tier
IS the WAL; splitting would create cross-crate coupling on
retransmit path). Next: Aeron study, rsx-dxs naming, prior
art survey (Solana Agave UDP, QUIC-vs-UDP tradeoffs).

## 17:00

Presentation-surface pass per kronael's forced rank.
Commits in order: cbd80a6, d90b9c1, d691bbc, ccc1ac3, 1e8f6a8.

- `ONEPAGER.md` — one-screen pitch leading with the wedge +
  three concentric bench numbers (340 ns / 9.58 µs / 1 128 µs)
  + open work; never existed before.
- `README.md` opening reframed: implementation pitch
  ("spec-first ... CMP/UDP ... fixed-point") → BLOG's
  two-artifact framing. "How fast" table now sits above the
  architectural rationale.
- `specs/2/index.md` — master index for the 47 phase-2 specs.
  Grouped by domain. Status legend. Linked from README.
- `rsx-dxs/README.md` polish: headline bench table at top
  (WAL append 31 ns, send body 3.87 µs, RTT 10.3 µs, fsync
  651 µs amortised). Install snippet + `cargo run --example`.
  Linked ARCHITECTURE.md + facts/syscall-latency.md.
  Cargo.toml: description, repository, license, keywords,
  categories, readme — all populated.
- `rsx-dxs` public API audit caught one real defect: the
  README's quick-start said `use rsx_dxs::{CmpSender,
  WalWriter};` but CmpSender / CmpReceiver / CmpConfig were
  not re-exported from lib.rs. Now they are. Added
  `examples/wal_replay.rs` — runs end-to-end, shows WAL =
  wire claim concretely. `cargo tree -p rsx-dxs --edges
  normal | grep rsx-` confirmed empty.

Playground demo validation delegated to the in-flight CEO
audit sub (round 2).

## 17:10

CTO + CEO audit round 2 launched in parallel
(`.ship/20-CTO-CEO-REVIEW-2/`). Stricter constraints than
round 1:
- CEO: must inject ≥3 faults, run a real demo flow, time
  every endpoint, ≥5 NEW findings (not in F1-F28), numeric
  0-100 grade vs round 1's "NO greenlight".
- CTO: pure source/bench reads, verify 5 specific claims by
  running benches or grep, propose 3 attack scenarios + trace
  through code, codex second-opinion on borderline calls,
  numeric 0-100 SLA-bet grade.

Both running in background; results will land in
`.ship/20-CTO-CEO-REVIEW-2/{CEO,CTO}-REPORT.md`.

## 11:27

rsx-dxs src minimization pass: -211 LOC (2,993 → 2,782, -7%),
behavior unchanged, all 96 dxs tests + downstream consumers
green. Five commits: 6cd69cb drop dead `flush_stalled`;
aee7b6e inline `handle_client_plain/tls` wrappers;
67bf4a7 collapse 5 framing sites into `frame_and_send`;
11a88e6 extract TLS into `src/tls.rs`; 1f803fb merge
`run`/`run_once` into one `FnMut(R)->bool` path (drops
`Pin<Box<dyn Future>>` workaround as bonus).

## 11:27

Design discussion surfaced two real follow-ups on WAL
discovery (now in summary). User pushed back on archive
hot/cold special-casing: discovery should be generic
across N dirs (`list_wal_files_across`), then reader is
one sorted Vec. Also flagged: directory listing is
O(files) on every reader open — sqlite manifest replaces
it. Manifest decouples filenames from protocol so
`<id>_<first>_<last>.wal` can become whatever.

## 12:36

Protocol comparison sprint shipped: 4 parallel worktree
subs (Aeron, KCP+Quinn, TCP+UDP+LBM, Chronicle Queue).
All merged. Oracle caught real bugs in 4 of 5 benches
(TCP black_box-swallowed failures; KCP needed bootstrap
update(); Quinn accept_bi race; Aeron Drop-order
backwards) + 6 factual errors in Chronicle doc, incl.
one in my own prompt (Chronicle OSS is multi-writer,
not single-writer). WAL discovery item 2 (sqlite
manifest) declined by sub with honest cost-benefit:
fs::read_dir is already fast enough for a control-plane
op, and authoritative manifest opens fsync ordering bugs
that touch durability invariants. Item 1 shipped, item 3
deferred. Niche-protocol survey launched (4h budget,
9 search categories — PGM/NORM/SRT, all QUIC stacks,
trading protocols, kernel-bypass, multicast pub-sub,
per-language niche, academic, CN/JP/RU ecosystems).

## 12:40

`.ship/21-AUDIT2-FIXES/` complete. 12 of 12 actionable
findings from the CTO + CEO round-2 audits shipped:

  R-N3 lint               6e4c6e2  (already)
  R-N6 gw advance_seq     d5162c1
  R-N5 jti rollback       bbf0912
  R-N2 risk stall         8707611
  R-N1 wal replay         ba11365 + 90f0e61
  R-N4 ts_ns fallback     d63c224
  R-N7 OrderFailed wal    09a395b
  CEO-1 /trade schema     6356158
  CEO-2 /x/order 503      aac9e18
  CEO-3 /x/* 404          267e5eb
  CEO-4 /verify count     f8df05d
  CEO-5 /faults restart   707ec08

Deferred (explicit, in PLAN.md): R-N8 (48h retention disk),
R-N9 (heap-per-frame perf), R-N10 (bench-reference re-seal —
process not code). All three are operational/CI items, not
correctness regressions.

State at HEAD `143b55c`:
  cargo test --workspace --lib --tests → 891 pass / 0 fail
  make lint → exit 0
  CHANGELOG.md not yet updated (deferred to next /ship cycle
  along with the audit-round-3 re-run).

Each correctness fix has a regression test (Rust side) or a
behaviour test (playground side) so the audit re-run is
about MEASURING delta, not re-discovering.
