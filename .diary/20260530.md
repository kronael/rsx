---
summary: |
  RSX perp exchange. Solo work. Demo + measured benchmarks.
  Open items:
  - Oracle FIXED: codex default model was retired gpt-5.4 → set gpt-5.5 in
    ~/.codex/config.toml; oracle skill now model-agnostic + has the gotcha note.
  - e2e/GW latency reprobe [resolved 2026-05-30: real-gateway WS+REST bench
    landed — WS 11.5ms p50, REST 131µs, client floor 15µs;
    reports/20260530_gateway-ws-rest-latency.md].
  - CPU + false-sharing setup CONCENTRATED into rsx-types::cpu (setup_hot_thread)
    + rsx-types::cache (Padded<T>); all 5 binaries wired. Hot-path AUDIT DONE:
    SPSC false-sharing already handled by rtrb, happy paths zero-alloc; only
    finding was LATENCY-TRACE-ALWAYS-ON.
  - Cast-processor HFT pass DONE: all cast-recv loops → zero-copy try_recv_with
    (kills per-msg Vec alloc); latency trace now a compile-time latency_sample!
    macro (feature latency-trace, default off) — NOT a runtime flag (founder).
  - BUG IOC-NOT-HONORED (MED): empty-book IOC rests; matching core is correct,
    bug is in tif propagation GW→risk→ME. logged, not fixed.
  - 4-crate oracle bug-hunt DONE: 19 triaged findings in bugs.md (verified/
    by-design/needs-verify). 3 oracle refinement rounds IN PROGRESS.
  - Risk return path ME→GW-direct: spec'd (28-risk.md partial), NOT impl.
  - Minor cleanup: OrderResponse::Accepted pushed to response ring but
    drained-and-ignored (only Rejected acted on) — push only Rejected.
  - Legacy-PG orphan-freeze rows: one-time recovery reconcile (minor, open).
  - GATEWAY LATENCY = casting-recv poll-loop starvation: response waits
    ~0.8ms p50 / ~10ms p90 in the GW UDP socket buf for a reactor turn (NOT
    Python — GW-only RTT is 143µs). Fix: tile-split casting-recv to a pinned
    busy-spin thread → SPSC to WS writers. Biggest e2e win.
  - BUG: ANSI escapes in log files (tracing with_ansi not off for files) —
    fix with_ansi(false) across rsx binaries. Not fixed.
  - TASK 24 [resolved: cross-file risk fixes redone in main tree (404ad91);
    all 17 worktrees confirmed integrated — 14 ancestors, 3 byte-identical].
  - 4 bug-fix subs merged (290 green); cast-docs + doc-recon merged.
  - Core layout: 0=OS+mark+recorder, 1=gw, 2=risk, 3=ME, 4=md, 5=spare
---

## 03:20

Oracle resurrected. Root cause: codex's default model in ~/.codex/config.toml
was gpt-5.4 (retired for this ChatGPT account → every call 400'd "not
supported", login status still OK). Fixed default → gpt-5.5; codex exec works
with no -m. Updated oracle SKILL.md: added "Model availability — login status
is NOT enough" section (round-trip probe, not login check; bump config default;
don't pin -m). Kept skill model-agnostic per founder.

## 03:25

Strengthened orphan_freeze_test.rs after manual review found test 1 (FM6)
CIRCULAR — it hand-fed recovery an empty frozen_orders map then asserted
empty (would pass even if the pre-send write-behind bug returned). Now derives
the recovery snapshot from the shard's actual persist stream. FM11 cancel/fail
got a control (accepted-replay alone freezes 10_050) so the ==0 isn't vacuous.
8 pass. Then reran via oracle (gpt-5.5): validated the fix, flagged tests
1/2/6 assert >0 not exact 10_050 — exact-amount strengthening still TODO.

## 03:40

Hot-path audit (e2e order push, post ring-removal). Verified BOTH return
routes consistent: route 1 (ME outcome: fill/done/cancelled/failed/inserted/
bbo → me_receiver → forward_to_gw, settling margin) and route 2 (risk
pre-trade reject → response ring → forward_to_gw). Both converge on ONE
contiguous gw_sender seq (forward_to_gw stamps next_seq; verified all 7
forwarded records have seq:u64 first → overwrite safe). Mutually exclusive
per order (reject XOR accept→lifecycle) = exactly-one completion. Fills-first
preserved (me drained before gw). Finding: Accepted response is dead weight
(pushed, drained, ignored) — push only Rejected.

## 03:45

Cluster booted for latency reprobe. start-all returned 400 (batch precondition)
but start-proc per process works — started all 6 individually (gw-0/risk-0/
me-pengu/marketdata/mark/recorder all running). Sonnet sub driving the F1
latency probe + extracting per-stage GW timings.

## 04:00

4 worktree bug-hunt subs (margin/position/liquidation/funding+insurance)
found + fixed REAL bugs, merged onto HEAD (290 green): margin upnl sum was
non-saturating → overflow could wrap+inflate equity past pre-trade/liq
(HIGH); calculate_fee overflow flipped a rebate into a charge; liquidation
backoff defeated when first order at now_ns==0 → order storm (HIGH); funding
per-user floor breaks zero-sum #9 (HIGH/P0). position math clean (900k fuzz).

## 04:05

cross-file fixes (funding settle wiring, liq fallback_mark_prices, halt-on-
failed, slip-guard) done in worktree 91f685d BUT it branched from a stale
base → conflicts; RECONCILE PENDING (task 24). Note: f2ed3be's settle_symbol
parks FULL residual = wrong for one-sided shards (a shard owns a user subset,
local Σnet_qty≠0); it's currently DEAD CODE (not wired), so no live
regression. cross-file sub's intra-shard-residual version is the correct one.

## 04:10

cast-docs refine (oracle via gpt-5.5, benchmark-forward, factual) +
hot-path doc-reconcile merged (b1b2905). Oracle resurrected: codex default
was retired gpt-5.4 → set gpt-5.5 in ~/.codex/config.toml; skill stays
model-agnostic. Worktree subs branch from a STALE base → cherry-picks
conflict; took doc files via `git checkout <commit> -- <files>`.

## 04:40

GATEWAY LATENCY ROOT CAUSE (corrected my earlier wrong "it's Python" claim
— GW-only RTT is 143µs, so the ~11ms is INTERNAL). Single-order stage trace:
me_out at 571µs but gateway_cmp_recv at 4794µs — a ~4.2ms hole where the
response sits in the gateway UDP socket buffer waiting for its casting-recv
poll loop to get a reactor turn. me_out→gw_recv gap = ~0.8ms p50 / ~10ms p90
= THE whole latency. Risk/ME are sub-ms. Fix: tile-split casting-recv to a
dedicated busy-spin thread (off the shared monoio reactor) → SPSC to WS
writers. Logged.

## 04:42

BUG found: tracing writes ANSI color escapes into LOG FILES (not a TTY) —
latency stage lines wrapped in \x1b[..m, un-greppable. Fix: with_ansi(false)
(or TTY-detect) in tracing init across rsx binaries. Not yet fixed.

## 05:30

Disk hit 99% (2.7G free) → ENOSPC risk. User approved cache cleanup: rm'd
18G of worktree target/ build caches + removed merged/dead agent worktrees
(kept #24's a740f52d) + cargo registry cache. Now 21G free. Some worktrees
stale-locked (dead agent pids) — left.

## 05:45

loop-arch bench (rsx-risk/benches/loop_arch_bench.rs) built + run + oracle-
critiqued + REFINED. Oracle (gpt-5.5) found the conclusion-inverter: tile/
batched got a free uncounted helper core. Refined to EQUAL core budget K
(tile = (K-1)reactors+1helper, service_us over full K) + open-loop mode +
honest echo-side syscall labeling + batched partial-flush (1.2k→60-74k req/s).
FAIR RESULT (closed-loop): SYSCALL-BOUND (2.00 sys/req, calc ~22ns); tile
does NOT win — it's worst once charged; tokio strongest; batching cuts to
~0.25 sys/req but needs in-flight depth. Committed. Open-loop latency run
in flight (the real "is the tile lower-latency than tokio at sustainable
load" test). User (correctly) flagged closed-loop "tokio faster" as an
artifact (Little's law: latency≈in-flight/throughput under saturation).

## 05:50

Per-model subagent thinking: NOT a config key (per claude-code-guide/docs).
Already achieved via global effortLevel=xhigh + subagent inheritance +
per-model auto-clamp: Opus subs→xhigh, Sonnet→high (xhigh unsupported→falls
back), Haiku→high. No change needed.

## QUEUE (open)

- Open-loop bench latency table → report (in flight).
- Risk benches: e2e throughput capacity + flood/load-growth — deliver via
  sub, spec'd well, ORACLE-CRITIQUE-BEFORE-SHIP (user instruction). Launch
  after open-loop run frees cores (one-sub-per-run pipeline).
- TASK #24: cross-file risk fixes (funding settle wiring + liq
  fallback_mark_prices + halt + slip-guard) — careful reconcile from stale-
  base worktree 91f685d onto HEAD. Financial correctness. Highest stakes.
- ANSI-in-logs fix (with_ansi(false)).
- Gateway egress tile-split (specced, not impl).

## 09:30

#24 cross-file fixes DONE (404ad91, verified 299 tests): funding zero-sum
WIRED into shard.maybe_settle_funding with the correct intra-shard-residual
settle_symbol (old one force-zeroed one-sided shards); liquidation reads
fallback_mark_prices (not inert when mark silent); halt-on-failed/resume-on-
fill; max_slip_bps config guard. NOTE: worktree isolation kept branching
from a STALE base (run_once/269) — redid in MAIN tree where the real HEAD
(tick/290) lives. Lesson: in this session, run subs in the MAIN tree, not
worktrees (worktree base != detached-HEAD commits).

## 09:45

Risk capacity+flood benches done (oracle-gated): engine ceiling ~10M ops/s
(accept depth=0), falls with frozen-order depth; knee at ~8M offered;
persist backpressure STALLS, never drops. Committed.

## 10:00

reports/ convention added to CLAUDE.md: reports/YYYYMMDD_tag.md, in git, the
durable latency/throughput record over time. Populated 4: e2e-ws-probe
(11.6ms gateway-poll-bound), loop-arch-bench (syscall-bound, tile loses,
batching is the lever), risk-capacity-flood, component-benches (ME ~210ns,
casting RTT 7.6us, deep-book match ~52ns flat to 10M).

## 10:05

Delivered the full gateway STRUCTURE analysis (one monoio reactor; 3 task
types; GatewayState Rc<RefCell>; ingress recv→parse→validate→cast-send;
egress recv-poll→route→conn.outbound VecDeque→conn-task write = TWO reactor
hops) + the reactor CODEWISE explanation (io_uring SQE/CQE + cooperative
task queue; completion→wake delay scales with reactor load; busy-spin tile
services its socket immediately). Real-gateway WS/REST warmed-latency bench
sub running (100k single-stream + 100 users×1k + REST). User (rightly) tired
of sub-churn + the closed-loop "tokio faster" artifact — reframed honestly.

## 11:40

HFT hands-on docs: rsx-gateway/notes/io-model.md (epoll readiness → you
read+copy, syscall/op; io_uring completion → kernel does read+copy, batched
CQE, SQPOLL→0 submit syscalls; zero-copy progression registered-bufs →
provided-rings → SEND_ZC → AF_XDP/DPDK). Reframed "no priority" → reactor is
saturation/capacity-bound (single core, cooperative, no preempt) per founder.

## 12:30

Concentrated the CPU setup (was inline core_affinity in 5 binaries) into
rsx-types::cpu::setup_hot_thread = pin + warm_stack + mlockall + isolation
warn. Stack warming: mlockall(MCL_CURRENT) only pins ALREADY-mapped pages, so
warm the lazily-grown stack (write 1 byte/page, 256KiB, inline(never)+
black_box) BEFORE mlockall. notes/hot-path.md gets the full page-fault
discipline (major/minor, write-to-prefault, allocator retain, THP-off, arena
bump-alloc, stack warm).

## 12:50

Concentrated false-sharing primitive: rsx-types::cache::Padded<T>
(repr(align(128)) + Deref) + LINE=64/PAD=128. 128 because Intel adjacent-line
prefetcher pulls pairs (= crossbeam CachePadded). notes/hot-path.md gains
"how to FIND false sharing": perf c2c HITM report, perf stat xsnp_hitm,
pahole static layout, the within-64B heuristic.

## 13:30

Real-gateway WS+REST warmed-latency bench landed (rsx-gateway/benches/
ws_order_latency.rs, hand-rolled blocking WS client, no runtime). WS single
11.5ms p50 / parallel 13.5ms p50 / REST 131µs / client floor 15µs — confirms
gateway monoio-reactor egress starvation (NOT transport). Baseline for the
egress-tile-split. reports/20260530_gateway-ws-rest-latency.md. Found
IOC-NOT-HONORED (MED): empty-book IOC rests instead of cancelling (logged).

## 13:45

Wired all 5 binaries to rsx_types::cpu::setup_hot_thread; dropped core_affinity
dep from 4 crates (dev-dep in rsx-risk, benches use it). Workspace bins build,
risk benches compile, lib tests green. CPU + cache concentration DONE.
Still open: hot-path AUDIT (apply Padded to shard counters/flags, verify no
hot-path malloc/log/syscall, SPSC batch/prefetch); 3 oracle refinement rounds;
4-crate bug-hunt — none started.

## 14:30

Oracle 4-crate bug-hunt (codex gpt-5.5, one bg pass per crate): 19 triaged
findings in bugs.md, each spot-verified + confidence-tagged. Real ones:
ME-NEXT-SEQ-REGRESSION (snapshot+0-replay leaves next_seq=1 → seq reuse),
BOOK-BBO-COMPRESSED-INDEX (compression is sawtooth, not price-monotonic →
best_bid/ask wrong across zones; verified by hand), BOOK-SCAN-NEXT-BID
from<2 off-by-one, several gateway WS/routing bugs, RISK-NO-PRICE-QTY-GUARD
(no price>0/qty>0 check before margin; validate_order unused — needs verify).
IOC-NOT-HONORED refined: matching core is correct, bug is in tif propagation.

## 14:35

Hot-path audit of cast processors: SPSC false-sharing already handled (rtrb
cache_padded), risk+ME order happy-paths zero-alloc/no-syscall. One finding:
LATENCY-TRACE-ALWAYS-ON (logged).

## 15:30

Worktrees confirmed ALL integrated: 14/17 are ancestors of HEAD, the other 3
([risk] cross-file fixes ×2, [fix-RN4/RN7]) are byte-identical to HEAD (redone
in main per the stale-base lesson). ~18 dirs under .claude/worktrees/ are dead
scratch, safe to prune.

## 16:00

Cast-processor HFT pass (planned + 4 opus subs, disjoint crates). (1) All
cast-recv loops migrated try_recv() → zero-copy try_recv_with(|hdr,payload|) —
kills a per-message Vec heap alloc on risk/ME/marketdata/gateway. rsx-cast
FROZEN, fix in callers (SEQ-1 pattern). (2) Latency gate: first did a runtime
AtomicBool (set_enabled/enabled) — founder rejected ("not runtime flags, use
macros"). Reworked to a compile-time latency_sample! macro (feature
latency-trace, default off = zero code, args not even evaluated); emit computes
the µs delta inline, drainer formats off-path. Dropped the move-delta-to-drainer
micro-opt (founder: clock is a few ns, beyond concern). Builds trace off+on,
lib tests green. Deferred: gateway Arc<str> broadcast (marginal on non-pinned
egress reactor; tile-split is the real fix).
