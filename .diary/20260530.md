---
summary: |
  RSX perp exchange. Solo work. Demo + measured benchmarks.
  Open items:
  - Oracle FIXED: codex default model was retired gpt-5.4 → set gpt-5.5 in
    ~/.codex/config.toml; oracle skill now model-agnostic + has the gotcha note.
  - e2e/GW latency reprobe IN PROGRESS (cluster up; Sonnet sub measuring) —
    refreshing the stale 11.6ms WS-probe + per-stage internal timings.
  - Risk return path ME→GW-direct: spec'd (28-risk.md partial), NOT impl.
  - Minor cleanup: OrderResponse::Accepted pushed to response ring but
    drained-and-ignored (only Rejected acted on) — push only Rejected.
  - Legacy-PG orphan-freeze rows: one-time recovery reconcile (minor, open).
  - Core layout: 0=OS+mark+recorder, 1=gw, 2=risk, 3=ME, 4=md, 5=spare
---

## 03:20

Oracle resurrected. Root cause: codex's default model in ~/.codex/config.toml
was gpt-5.4 (retired for this ChatGPT account → every call 400'd "not
supported", login status still OK). Fixed default → gpt-5.5; codex exec works
with no -m. Updated oracle SKILL.md: added "Model availability — login status
is NOT enough" section (round-trip probe, not login check; bump config default;
don't pin -m). Kept skill model-agnostic per founder.

## 03:25

Strengthened orphan_freeze_test.rs after manual review found test 1 (FM6)
CIRCULAR — it hand-fed recovery an empty frozen_orders map then asserted
empty (would pass even if the pre-send write-behind bug returned). Now derives
the recovery snapshot from the shard's actual persist stream. FM11 cancel/fail
got a control (accepted-replay alone freezes 10_050) so the ==0 isn't vacuous.
8 pass. Then reran via oracle (gpt-5.5): validated the fix, flagged tests
1/2/6 assert >0 not exact 10_050 — exact-amount strengthening still TODO.

## 03:40

Hot-path audit (e2e order push, post ring-removal). Verified BOTH return
routes consistent: route 1 (ME outcome: fill/done/cancelled/failed/inserted/
bbo → me_receiver → forward_to_gw, settling margin) and route 2 (risk
pre-trade reject → response ring → forward_to_gw). Both converge on ONE
contiguous gw_sender seq (forward_to_gw stamps next_seq; verified all 7
forwarded records have seq:u64 first → overwrite safe). Mutually exclusive
per order (reject XOR accept→lifecycle) = exactly-one completion. Fills-first
preserved (me drained before gw). Finding: Accepted response is dead weight
(pushed, drained, ignored) — push only Rejected.

## 03:45

Cluster booted for latency reprobe. start-all returned 400 (batch precondition)
but start-proc per process works — started all 6 individually (gw-0/risk-0/
me-pengu/marketdata/mark/recorder all running). Sonnet sub driving the F1
latency probe + extracting per-stage GW timings.
