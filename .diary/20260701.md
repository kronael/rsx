---
summary: |
  RSX perp exchange. Solo. Session opened as a status check ("where are
  we / is cast published / better than competition / e2e working") then
  turned into a 4-item "do ALL" pass.
  Status answered: v0.6.0 tagged (not pushed), 883 lib tests. Cast NOT
  published (forbidden externally); it IS better than competition on its
  workload (loopback 128B: ~9.3us RTT, at raw-UDP floor; beats TCP/QUIC/
  KCP/Aeron-networked; only loses to shmem IPC). E2E confirmed working by
  prior CTO browser-verify (order→fill→WAL, 86/86 API).
  Done + committed:
  - [book] BBO/cross detection fixed across compression zones (raw price,
    not sawtooth tick index) + scan_next off-by-one. 18 book_test green.
  - [cast] compare docs reconciled to measured numbers; removed stale
    StatusMessage/flow-control claims, 48h→4h retention, CRC32→CRC32C.
  Verified already-done (no new work needed):
  - FM6/FM11/FM12/FM13a/FM14 — orphan_freeze_test.rs (8 tests) landed
    05-29/30 already covers the COVERAGE P0 plan. Only residual is the
    legacy-PG orphan reconcile (boot-path, founder-review-gated).
  Deferred (with reason):
  - rsx-risk #42 (8-item live-review refactor) — 1527-line hot-path
    lifecycle/financial code; cross-crate reach (state_idx in rsx-health).
    Founder-supervised chunked pass, not autonomous. NOT started.
  Open bugs logged: BENCH-KCP-FLUSH-NEEDUPDATE (compare_all panics on KCP
    warmup, needs update() before first flush; bench-only, LOW).
---

## 08:03 2026-07-01 — status check → "do ALL" pass

User opened asking where the project stood + three cast questions (is it
published, better than competition, working e2e). Answered from diary +
compare/README + prior CTO verify. Then: "do ALL" four candidate chunks.

Ran two independent subagents in parallel (book bug, cast-docs polish) in
the MAIN tree; took the rsx-risk work myself.

## book — BBO across compression zones (b805aef)

best_bid/ask + scan_next + post_only cross + FOK liquidity compared the
SAWTOOTH compressed tick index as a price proxy → wrong BBO/crossing with
resting orders in >1 zone (bid side + cross detection; ask half was
already index-monotonic). Now track best_bid_px/best_ask_px by raw price;
scan_next does a bounded single pass picking best-by-price, which also
kills the scan_next_bid `from < 2` off-by-one (tick 0 dropped from BBO on
best-bid cancel). 18 book_test cases (3 fail-before/pass-after), whole
crate green. bugs.md: both FIXED.

## cast — compare docs reconcile (88ab81c)

Re-verified cast RTT (9.29us p50) + raw-UDP (9.91us) benches this session.
Fixed stale figures across compare/: WAL retention 48h→4h, removed
StatusMessage/receiver-window flow-control claims (dropped in 87b223e;
heartbeat-only now), CRC32→CRC32C, raw-UDP ~2us→~9.9us, payload 64B→128B,
WalHeader layout (seq lives in payload). README leads with the one-workload
caveat; every speed row carries a bench citation + measured-when column.
Confirmed the competitor benches (MoldUDP64/Aeron/SoupBinTCP/compare_all)
are REAL clean-room impls wired as `[[bench]]`, not doc-only; only
Chronicle (Java) + LBM (commercial) are doc-only, correctly labeled.

## risk FM tests — already done (verified, no new code)

orphan_freeze_test.rs already has 8 passing tests covering the COVERAGE
P0 plan: FM6 (orphan not durable + confirmed durable), FM11 (replay
rebuild + release-on-cancelled + on-failed), FM12/FM13a (dup order_id
freezes once, dup fill seq positions once), FM14 (double-replay identical).
Landed 05-29/30 after the 05-29 audit. Nothing to add. Residual: legacy-PG
orphan reconcile (drop a PG freeze with no WAL OrderAccepted at boot) —
real but boot-path, founder-review-gated; logged, not slipped in.

## risk #42 — DONE (founder said "do ALL" then /fin → authorized)

Initially deferred (below), but the founder's "do ALL" + /fin authorized it.
Executed via one opus agent in the MAIN tree (single writer, no worktree),
one commit per item, full suite after each. 9 [risk] commits 1b234f1..b79bb1d:
- collapse 1-variant MainTransition → Result<()> (1b234f1)
- load_state → set_state rename (645088d)
- saturation max as iterator fold (751e542)
- typed DaemonState #[repr(u8)] enum + set_state; raw state_idx field kept
  public so gateway/ME/marketdata/mark/recorder still compile (001de07)
- named spawned thread bodies persist/lease (43a68cc)
- centralized RuntimeConfig::from_env, PathBuf/SocketAddr, fail-fast;
  defaults byte-identical to the old scattered env::var sites (585d342)
- db::connect module (7f87921)
- extract lease machinery → lease.rs (5848f04)
- extract failover lifecycle (handle_replay/forward_to_gw/run_warm_catchup/
  drain_mark_warm) → failover.rs (b79bb1d). main.rs 1424→1081 lines.
Plus 7807709: fixed a carried-over manual_range_contains clippy lint in the
extracted failover.rs (make lint is -D warnings).
VERIFIED MYSELF (not the agent's word): git log, main.rs line count, new
modules exist, rsx-cast diff empty; re-ran full suite = rsx-risk 288/0/35 +
rsx-health 5/0 (identical to baseline); cargo build --workspace clean; and
spot-checked the config commit — removed originals confirm DATABASE_URL /
RSX_ME_REPLICATION_ADDR were already .expect()-required and every addr
default (9105/9106/9101/9102/28301, ./tmp/wal) is byte-identical. Pure
behavior-preserving refactor. NOTE: the agent's report claimed a "prior
session" did items 2-8 — false; all 9 commits are its own, dated today on
top of the KCP fix. It lost track of its own earlier commits. Result is
correct regardless.
Residual FM6 legacy-orphan reconcile still open (boot-path, founder-gated).

## direction — GUI discontinued, TUI is the trade surface (64b9990)

Founder: trade surface = TUI not GUI, in the best tool available. Picked
**ratatui** (best-in-class Rust TUI; reuses rsx-types/rsx-messages wire
types, connects via gateway WS webproto 49 — no second-language client).
Marked rsx-webui DISCONTINUED (README banner, frozen bugfix-only). Playground
keeps a lighter read-only web GUI (component pages/cast demo/logs/viz).
TODO.md: direction block + rsx-tui backlog item.

## tui — rsx-tui scaffold (ratatui) shipped

New crate `rsx-tui` in the workspace (ratatui 0.29, re-exports crossterm —
one UI dep). Full trade layout: orderbook ladder with depth bars (asks red /
bids green / spread), order entry (b/s side toggle, mock price/qty/tif),
positions table (upnl colored), trade tape, status bar (DEMO badge), help
row. crossterm event loop, q/Esc quit. MOCK data — live gateway WS wiring
(webproto 49, reusing rsx-types/rsx-messages) is the next step; it needs a
running gateway + auth, so it's a dedicated follow-up, not this closeout.
`cargo check -p rsx-tui` + clippy clean. `cargo run -p rsx-tui` shows the
shell.

## /fin open-items closeout

All 4 session tasks completed (book, cast, FM-verify, #42). Remaining items —
all Deferred-with-reason or Blocked-on-user, none Active:
- rsx-tui live WS client — deferred (needs running gateway + auth; scaffold is
  the committed increment).
- Mark dev CEX feed (so Mark is e2e-demonstrable) — backlog.
- BENCH-QUINN-ACCEPT-BI — logged (QUIC stream-lifecycle, separate surgery).
- legacy-PG orphan-freeze reconcile — founder-review-gated (boot path).
- walkthrough-embed variant — BLOCKED on founder go-ahead.
- GitHub OAuth creds — BLOCKED on founder (real CLIENT_ID/SECRET).

## component status table (delivered to founder)

All 7 processes have playground component pages (blurb+docs+logs); only
Matching has an interactive viz (book). NO guided walkthroughs shipped (the
embed-walkthrough variant is blocked on founder go-ahead). E2e demonstrable:
GW→Risk→ME→Risk→GW order→fill→WAL (CTO-verified); the one gap is Mark (no CEX
feed in dev → index fallback only). Publication: nothing external (policy);
rsx-cast writeup is publish-ready (founder decision).

## (initial deferral — superseded above)

1527-line main.rs, hot-path lifecycle + financial recovery, explicitly the
founder's live-review backlog. Even the "simple" state_idx→enum item is
cross-crate (rsx-health LoadGauges, all 6 daemons publish it). Initially
deferred as too big to blast autonomously — then "do ALL" + /fin overrode it.
