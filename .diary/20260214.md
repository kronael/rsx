---
summary: |
  RSX perp exchange. Solo work. 2026-W07 day 6.
  - HTTP header RFC 7230 violation found: Rust format string
    indentation added leading spaces → Python WS rejected handshake.
  - websockets dep swapped for aiohttp per python wisdom.
  - 48-bug fix batch (Rust + Python): overflow, XSS, WAL durability,
    JWT aud/iss, circuit breaker HalfOpen, FOK rollback, auth bypass.
  - 23-bug fix batch: FOK rollback (correctness), auth bypass,
    i64 overflow, OOM guards.
  - Docs root cleaned up; old session files archived.
---

## 10:58

Discovery: Rust WS handshake had leading spaces in HTTP headers — format
string continuation indentation applied literally to the header value,
violating RFC 7230. Python `websockets` library rejected the response.
Fixed (2366a1d). Gateway runtime migration TODO added to TODO.md.

## 11:36

`websockets` dep replaced with `aiohttp` for all WS clients in playground
(175037). Removes `sys.path` hacks, `pyjwt` added for stress client JWT
auth. Async test markers cleaned up.

## 13:16

48-bug batch (622d6bb): Rust — circuit breaker `HalfOpen` state missing,
rate limiter double-count, JWT `aud`/`iss` validation, mark price overflow,
book snapshot bounds, event buffer guard, WAL `sync`+rotation, CMP logging,
tip persistence fsync, risk overflow/div-by-zero, seq gap detection, CLI
record size limit. Python — XSS `html.escape` in 5 render functions,
`JSONResponse` type, `order_type` field, env-based gateway URL, scenario
state ordering, audit logging, idempotency keys.

## 13:28

23-bug batch (2b8c2ef): FOK rollback correctness (book left modified without
rollback on failure), auth bypass edge case, UB in unsafe block, `i64`
overflow in notional calc, XSS in two more render paths, OOM guard on
large order batch.

## 13:48

Root cleaned up: docs reorganized, old session files archived (881cc44).
