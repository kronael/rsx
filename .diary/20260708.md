
---
summary: |
  RSX docs polish and terminal transport cleanup.
  - Concept docs now follow the why-layer contract.
  - Slab cap error fixed: 65 536 is MAX_EVENTS, not order capacity.
  - Open: tui in-flight files and blog/30 remain unrelated and uncommitted.
  - Open: follow-up tui QUIC fixes from codex critique.
  - Open: publish roadmap after tui fixes.
---

## 08:19 2026-07-08 — book/matching presentable polish (cont.)

Continuing the founder-directed doc polish from yesterday. Small copy fixes to
the rsx-book README (unit style: "60 nanoseconds" → "60 ns"). Book correctness
fix + recentering wiring landed and verified yesterday (see 20260707.md); open
threads carried over: 3 plausible audit items, the recenter-latency follow-up,
and the skill sync to ~/app/tools/skills.


## 09:21 2026-07-08 — README/docs presentable marathon + tui QUIC + two analyses

Long founder-directed session. Highlights:

**Docs presentable pass (many small commits).** Positioning: it's a
**derivatives** exchange, not "perpetuals" — swept the tagline across ~18
docs, leaving product-specific perp mentions (funding, orderbook examples,
v1 scope) and specs/1 history. README: instruments list (perps supported,
options/SFDX pending); components reordered libs-first (book, cast) then
processes with a distilled "problem it solves" each; "How it scales" (renamed
from "Architecture" to avoid the doc clash) now explains vshards + what-scales
-as-what before the diagram; "How fast" reorganized top-down (whole-system →
inter-component → internals, deep tables deferred to crate READMEs); cut
honesty-theater and fluff (pinning/cranelift/userspace-UDP notes tightened);
removed an overstated io_uring "Next step" section. Blog marked AI-generated
draft (the intended educational core, unedited). PROGRESS rewritten terse
(same facts, dropped the % framing + meta prose).

**rsx-tui → protobuf-over-QUIC only** (opus sub): removed the WebSocket
transport (ws.rs, tungstenite/jsonwebtoken deps), QUIC sole transport,
hand-derived prost wire (no .proto). Verified green (29 tests). Then **codex
critique** (founder asked) found real gaps I verified: the order wire dropped
the **symbol** field; **spec 54 reconciliation was incomplete** (still said
WS+JWT/wss — the sub over-claimed "reconciled it"); silent enum coercion to
Buy/Gtc; asymmetric MAX_FRAME; no in-band auth (JWT dropped). Now being fixed
in a follow-up opus sub (symbol, QUIC auth first-frame, enum→error, symmetric
frame guard, latency correlation-id, golden test, full spec-54 reconcile).

**Two analyses (read-only, get-back-to-me):**
- gateway↔marketdata symmetry (opus): two forks of one monoio shape; filed
  alignment findings to BUGS.md (MD-EGRESS-STALL / GW-OUTBOUND-UNBOUNDED are
  mirror halves; WS-fork-drift; env-doc drift).
- monoio-vs-tiles (fable): **keep tokio off-path** (replication disk-bound, PG
  drain-bound, mark tolerant); **don't tile gateway/marketdata.** Killer
  finding: the ms-scale WS latency is not the reactor — monoio 0.2.4's timer
  wheel is ms-granular and rounds up, so the gateway's "500µs" egress poll
  fires at ~1–1.5 ms; marketdata has no egress wake at all. Fix = notify-based
  egress wake (0 extra cores). A full tile split saves ~5–15µs p50 for 4 cores
  the box lacks; loop-arch-bench already measured busy-spin tiles worst
  (syscall-bound; batching, not pinning, drives throughput). To file:
  MD-EGRESS-NO-WAKE + the monoio-timer root cause.

**Lesson (the hard way):** my rapid doc commits raced the tui sub's shared git
index — `git add X && git commit` swept the sub's staged deletions into a docs
commit (77e2961). Nothing broke (all wanted changes) but the history is
mislabeled and unfixable without amend/rebase (forbidden). Rule: **never
`git commit` while a code sub commits on the same tree**; if unavoidable, use
an explicit-pathspec commit (`git commit <file>`, only-mode) so it can't sweep
the index. Holding BUGS.md (fable + codex findings) + the publish roadmap
(book → matching → gateway+marketdata → tui → hosting/install-run → blog)
until the tui-fix sub finishes.

## 10:27 2026-07-08

Committed 902c31c: concept docs fixed slab capacity, expanded CompressionMap,
moved network-edge I/O, split SPSC rings from tiles. Left tui and blog/30 dirty.

## 11:xx 2026-07-08 — /fin close-out

Drove the accumulated queue to done. Landed this pass (all committed):

- **Concept docs** (codex #1/#2, verified): slab-cap error fixed (65,536 is
  MAX_EVENTS, not the order slab — configurable to millions, demand-paged);
  CompressionMap zone-bisection expanded; network-edge-io split out (io_uring
  ladder, SQPOLL, hand-roll ceiling, tile↔io_uring bridge); spsc-rings split
  from tiles; tiles doc got tile-YES/NO/sleeper examples, tokio-thread naming,
  core-5-is-spare-margin, matching-as-one-tile-thread. Added a
  `docs/concepts/CLAUDE.md` house-style guide. **Renumbered all concept docs
  01–13** by index reading order; every cross-link verified resolving.
- **Keyboard concept** reframed: the whole keyboard is the input space; 9
  bound today; full binding is deliberate future design.
- **`DEMO.md` → `demo.md`** (uppercase only at repo root) + refs.
- **Publish roadmap** in README §Roadmap (book→matching→gw+md→tui→hosting→blog).
- **Userspace-UDP / io_uring** recorded in README §What's-not-done + BUGS
  (CAST-SOCKET-COUPLING-BLOCKS-IOURING): fix = two additive cast APIs
  (expose `Framed` bytes; parse-received-bytes) so the caller owns the socket
  — a sanctioned frozen-cast extension, founder sign-off. On the roadmap.
- **blog/23-latency**: gateway is a monoio reactor, not a tile (fixed framing).
- **rsx-tui**: WS residue gone (quic:// fixture); **palette module** committed;
  **default dials `rsx.krons.cx`** (real CA roots, no cert file), `RSX_GW_LOCAL=1`
  → local debug gateway, `mock` → offline demo. Build/test/lint green.
- Merged the egress-wake worktree (notify wake, TCP_NODELAY) back to main;
  MD-EGRESS-STALL marked FIXED in BUGS.

**Deferred to other sessions (per founder):**
- **SQPOLL gated on core config** — documented (roadmap + 05-network-edge-io),
  but a kernel-fragile, dep-adding, later-scaling change; needs io-uring dep +
  IORING_SETUP_SQPOLL fallback (CAP_SYS_NICE) — do as a focused pass.
- **Skill sync** — `rs`/`speed-demo`/`cto-eval`/`ceo-eval` edits still need
  copying to `~/app/tools/skills/` (cross-repo, LOCAL.md path is also stale —
  it points at the dead `claude-template/global`, not `~/app/tools/skills`).
- **Blog reformulation** — the blog is a catalog with stale numbers, a triple
  90-bugs redundancy, and no narrative spine; needs the human editorial pass
  it's banner-marked for. blog/30 kept as a draft.

Tree clean. Task list #1–#11 all resolved (SQPOLL scoped/deferred).

## 09:40 2026-07-08 — trade-terminal spec + palette unification

New spec `specs/2/55-terminal.md`: perps screen mockup + keybindings +
confirm-preview, new-trader ("13yo") requirements scorecard (liq/side/uPnL+
ROE%/margin/confirm/leverage/mark-vs-last, each present/partial/missing with
data-source note), multi-market mockups (account, options, sfdx basis-function
`f=Σαᵢφᵢ` from krons, lending), data-source honesty table. Committed f816949.

Ayam Cemani palette: new `rsx-tui/src/palette.rs` (ratatui RGB mirroring the
dashboard Tailwind retune), swapped ~25 `Color::` sites in render.rs. Green,
clippy clean, lib tests pass. Committed 8bee250.

Coordination: another agent concurrently refactored the same crate (WS→QUIC per
spec 54); the two-agents-one-crate window briefly reverted my uncommitted palette
edits (backed up to scratchpad, re-landed path-scoped). Transport/live-book stays
that agent's lane.

## 11:45 2026-07-08 — concepts: merge, humanize, 13yo, CTO review (done)

Closed out the `docs/concepts/` set per the user pipeline "merge → humanize →
13yo review → cto review → done."

**Merge + renumber (ee78ab6, 92b71ee).** 13 pages → 11. Merged
`wal-is-wire-is-stream` + `reliable-udp` → **01-casting**; `tui-quic-protobuf` +
`keyboard-driven-terminal` → **10-trading-terminal**. Renumbered the middle
(03-tiles→02 … 13-glossary→11) via git mv + a sed remap of bare prefixed-names
across concepts/blog/README/specs. The sed had one casualty: it doubled a number
in `blog/17-asymmetric-durability` → `17-07-…` (3 sites); caught + fixed (92b71ee).
Lesson: a bare-name remap can match inside another doc's `NN-name`; scope the
pattern or diff the blog/README refs, not just concept→concept links.

**Humanize (dc08fde) + 13yo (31105d3).** Docs were already terse/house-style, so
light: dropped the Prometheus-as-example line (RSX rejects Prometheus), `append`
→ `append_framed`, stale merged-page link names, "the key insight is" signposting;
expanded the undefined `CFS` acronym and glossed `p50/p99` on first use (kept out
of the glossary — it's RSX-specific-terms-only).

**CTO review (codex, 195k tokens).** The valuable pass. 12 findings; I verified
each against source before acting (WISDOM: codex is advisory). **9 real, fixed**
(1731f56, aeb3081, 22045ba): fills are forwarded *before* fsync → 0–10 ms loss,
not "fsync before forwarding"; dedup key is `(user_id, order_id)`, cid is zeroed
in `RECORD_ORDER_ACCEPTED`; order path is ME→Risk→GW (ME→GW-direct + async settle
is spec-only, `28-risk` §, not impl); slab level storage ~40 MB (32 B PriceLevel ×
617k × 2 arrays), not 15 MB; live recenter is eager `recenter_now`, not the lazy
frontier walk; risk routing is vshard/shardmap; risk rings all stall on full (no
BBO/mark drop-newest path — that claim contradicted the page's own thesis anyway);
SQPOLL/multishot/GSO marked as the specced ladder (56), not shipped; WAL retransmit
horizon 4 h not 10 min. **1 discarded** (#7: codex miscounted risk threads — only
`std::thread::spawn` is the persist worker; lease renewal runs on the tile thread,
so it IS 2 threads). **2 logged not fixed** (BUGS 72e7cf2): README 7.5/16.9 µs vs
benches.md 9.58 µs vs cast-RTT 8.802 µs need one reconciled dated number; spec
`21-orderbook` still says 24 B PriceLevel. The concept mirrors README per house
rule, so those fixes belong in the authoritative docs, not the concept in isolation.

Tree clean. Concepts done. `rsx-tui/src/main.rs` had a stray rustfmt of my earlier
`b7fe167` — committed path-scoped (fb734dd), never swept into a docs commit.

**Open (other sessions):** skill sync to `~/app/tools/skills/`; SQPOLL impl (spec
56); blog reformulation; the two BUGS doc-number reconciliations above.

## 12:50 2026-07-08


## 13:55 2026-07-08


## 14:10 2026-07-08 — ME correctness + failover spec (parallel-session thread)

Worked the "get ME working well" track while another session ran benches/
marketdata/config on the same crate (rsx-matching) — real two-sessions
contention, managed by path-scoped commits + a stash-verify-commit-pop dance.

Landed + verified (mine):
- `4a0df9d` **dedup double-execute fix** (ME-SNAPSHOT-NO-INDEX-DEDUP-REBUILD):
  recovery now rebuilds the dedup window from the WAL (`DedupTracker::seed` +
  `wal::rebuild_dedup_window`); integration test proves a pre-snapshot resend
  is deduped. Was a real correctness gap (5-min window vs 10s snapshot →
  post-crash resend could double-fill), known in code TODOs but unfiled.
- `0f9f871` `REASON_DUPLICATE` → rsx-book `FAIL_DUPLICATE` (one FAIL_* namespace).
- cto-eval review → `.ship/41-MATCHING-RELEASE/CTO-REPORT.md`; findings filed
  in BUGS. `process_cancel` reuse left filed (LOW, not a clean swap).

Collision resolved benignly: other session widened dedup window 5min→1hr +
FxHashMap→FxHashSet; my rebuild reads DEDUP_WINDOW so it adapts, no regression.

Failover: found operator-managed failover + WAL recovery + playground
recovery/fault tests already exist; the ONE real gap is the advisory-lock
single-writer fence — RECOVERY-RUNBOOK assumes "1 lock per shard" but ME
takes no lock. Design → `.ship/41/DESIGN-me-failover.md`. Founder chose to
make it a **cast** feature → phase-3 spec `specs/3/1-cast-failover-transport.md`
(new specs/3 dir): runtime-free failover coordinator (Fence trait injected,
no PG/tokio in cast) + io_uring transport decoupling (the two additive byte
APIs) + "one coordinator over std-UDP or io_uring". Founder-signed frozen-cast
extension; implementation phased + later.

Also (earlier today): TUI Ayam Cemani palette `8bee250`, terminal spec
`specs/2/55-terminal.md`, matching bench-report quarantine cleanup.
