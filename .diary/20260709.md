
## 02:07 2026-07-09

— terminal → "superior" goal + worktree consolidation

Consolidated the three git worktrees: removed the two already-landed ones
(Go TUI copied to main; spec-59 cherry-picked as c33cde0), and merged the
`.matching-book-refine` work into main — code `b2d3c52` (update_order_index
3→1, generic get_env) cherry-picked cleanly, and 3 correctness findings
re-filed in BUGS (`029779a`): ME-FILL-DONE-STALE-SLOT-TIF (deferred — risk
reads FillRecord.reduce_only for position accounting, semantics need care),
MD-SHADOW-MIXED-SLOT-BBO-L2, BOOK-USER-RECLAIM-U16-CAP.

New goal: make rsx-term a *superior* trading terminal. Researched pro DOMs
(TT MD Trader, Bookmap, Sierra, NinjaTrader, Quantower), Bloomberg
conventions, HFT latency dashboards, fat-finger case law (Citi) + a
pro-trader visual critique. Verdict: well-built demo, not yet pro. Plan in
`.ship/44-TERM-SUPERIOR/PLAN.md`. Highest-impact sequence: static price
ladder → orders/position/last-trade drawn in it → right-aligned columns →
responsive reflow → visual latency hop-bar.

Wave 1 shipped (`45a0b6c`): fixed-width aligned numeric columns, responsive
reflow (ladder depth off WindowSizeMsg), spread-row emphasis. Waves queued:
2 (working-orders panel + cancel-by-selection + own-order/last-trade
markers), 3 (static price ladder — the defining change, opus), 4 (decimal/
tick formatting via the webproto Metadata frame), 5 (latency stacked hop-bar
+ p99.9 + SLA colouring), 6 (action hotkeys + ladder cursor + fat-finger
hard-guard + ? help + confirm-off ARMED mode). Account session limits started
hitting subs (~02:00 UTC) — continuing waves in the main session.

## 02:30 2026-07-09 — matching release: ran the two runtime checks (founder freed the box)

Founder said to kill the cluster and actually run the two box-blocked checks.
Did. Both surfaced real findings (recorded in `.ship/41-MATCHING-RELEASE/
HANDOFF.md`, `reports/20260709_matching-benches.md`, and BUGS
`MATCHING-BENCH-SHARED-HOST-VARIANCE` / `DEMO-TRADE-SH-STALE`):

- **Bench: the published numbers don't reproduce.** Full-workspace `bench-gate`
  is nightly-scale (timed out mid-rsx-book). Ran the matching benches directly on
  the idle box: `match_by_depth` ~48 ns (published 30), `me_accept_path/full`
  ~295 ns (published 266), throughput ~3.46M/s (published 3.6M — stable). Match
  stays depth-independent. NOT a regression — the WAL-dedup re-bench (before this
  session's refactors) already saw ~51 ns. Shared-4-core-docker-host variance
  (~1.6× on tight per-op benches; the Jul-03 report itself says "indicative on a
  shared host"). The 30/266 headline needs a **dedicated box** to be a
  trustworthy floor; throughput + depth-independence are robust.
- **Demo: could not drive a verified fill — env/harness, not code.**
  `demo-trade.sh` is stale after the playground rework: POSTs the removed
  `/api/submit-order` (404) + wrong WAL path (`tmp/wal/10` vs actual
  `tmp/wal/pengu/10`). Current submit is `/api/orders/test` (form, human units:
  price 0.05 qty 10). A maker rested (real ME round-trip) but takers time out
  with the ME WAL empty — live-cluster round-trip not completing, though all
  procs run + rmem tuned. Concurrent session's in-flux playground lane, not the
  engine. **ME logic is verified by the passing unit + integration suites.**

Cleaned up (stop-all + server down — exercised the graceful shutdown I wired).

Lesson: "verify on a quiet box" wasn't enough — the shared host is consistently
~1.6× slower than the published match/accept numbers. Bench honesty (the
founder's next-phase probe axis) has teeth: publish a floor only from a dedicated
box. And a runtime demo assertion rots fast when its harness API is reworked out
from under it — demo-trade.sh needs to track the playground.

## 04:30 2026-07-09 — terminal → superior: static ladder + pro features shipped

Drove the "superior trading terminal" goal to a strong state, all solo (subs
rate-limited ~19h). 11 verified commits, full build/vet/test/-race green:
- Wave 1: fixed-width aligned columns, adaptive resize (off WindowSizeMsg),
  spread emphasis.
- Wave 2: own-order ▸ / last-trade ‹ markers in the ladder, working-orders
  panel, cancel-by-selection (↑↓ cursor) + X cancel-all (killed blind
  cancel-newest).
- Wave 3: **static price ladder** — fixed price axis, bid-qty-left/ask-qty-
  right, recenter-on-drift (not per-tick reshuffle), gaps show liquidity
  shape; own/last marked in it; + a top-of-book imbalance bar. THE defining
  pro-DOM change (research: TT/Sierra pattern). Rendered-probe verified.
- Wave 5: latency stacked hop-bar (net│internal│engine proportional) +
  SLA-breach RTT colour — the differentiator made visual.
- Wave 6: m market (IOC far touch), R reverse, fat-finger hard-guard (all
  order paths route through arm() — hard-block, the Citi lesson), ? help
  overlay. Docs (SCREENS/VISUALS) synced.

Honestly-remaining (queued, not solo-doable well): wave 4 decimal/tick
formatting needs the webproto Metadata frame + real per-symbol tick (opus +
live cluster — hardcoding risks a wrong display; mock falls back to raw);
server-gated scaffolds (liq/margin/ROE%/funding + live internal/engine legs
= spec 59 inc 3) stay honestly dashed. Plan+status: .ship/44-TERM-SUPERIOR/.

## 06:20 2026-07-09 — terminal: wave 4 + depth bars + risk row, solo

Continued the superior-terminal goal solo. 8 commits, build/vet/test/-race green:
- **Wave 4** `03f521a` — decimal/tick formatting: raw i64 → human decimals at
  the render boundary (`fmtDec`/`strWidth`), PENGU 6/4 via env. Ladder/tape/
  orders/positions/confirm/status all read `0.010001`, not `10001`. Wire stays
  raw. The last "amateur tell" closed.
- **Wave 6 tail** `961c59f` — `+/-` improve-tick (seeds from mid, floors at one
  tick), `j/k` join best bid/ask, `F2` ARMED confirm-off (single-enter fire,
  loud red banner; fat-finger guard still holds).
- Position panel `b21ee10` — quote-precision uPnL (was 0.0500050000 → 0.050005),
  wrap-safe stacked layout, honestly-dashed liq/ROE/margin-health risk row.
- **Depth bars** `3b9b7dd` — restored the spec's `▊` per-level histogram
  (dead-const divergence from the static-ladder rewrite); bid grows left, ask
  right, converging on the spread. Made `ladderRow` a Model method.
- Responsive `59ebf2d` — wired the dead `narrow()`: stack panels vertically
  below the 3-col width instead of clipping the right column.
- Reconciled spec 55 mockup + SCREENS/VISUALS to the actual bid-left/ask-right
  depth ladder.

Found+fixed 2 real defects en route: 10-decimal uPnL, and the panel wrapping the
narrow column. Dead code removed: `signed()`, `maxBarLen`. Plan: .ship/44-TERM-SUPERIOR/.

## 08:10 2026-07-09 — terminal: decimal input, mouse, colorblind — plan complete

Second batch, all solo, build/vet/test/-race green (66 ui tests):
- **Decimal input** `370b8d8` — the coherent half of wave 4. Visual-critique
  gallery caught the read-decimal/type-raw mismatch: ladder shows `0.010001`
  but you had to type raw `10001`, and qty "5" previewed as `0.0005`. Added
  `parseRaw` (fmtDec's inverse) + `.` key → you type the price you read.
- **Confirm-box fix** `23be93e` — same gallery caught the RingPanel confirm
  overflowing orderWidth and shattering its border; rendered inline now.
- **Depth bars** `3b9b7dd` — restored the spec's `▊` per-level histogram (was
  a dead-const divergence since the static-ladder rewrite); bid-left/ask-right
  converge on the spread.
- **Positions** `b21ee10` — 10-decimal uPnL bug → quote precision; wrap-safe
  stacked layout + dashed liq/ROE/margin risk row.
- **Mouse click-to-price** `69e77db` — `priceAtY` maps screen row→price (shared
  `resolvedCenter`, verified at every boundary); left-click sets price, never
  submits. Disabled in narrow/stacked.
- **Responsive** `59ebf2d` — wired the dead `narrow()`: stack vertically below
  the 3-col width.
- **Colorblind** `c864d97` — `RSX_TUI_THEME=colorblind` → blue/orange (bid/ask
  red-green is the deuteranopia case).

The `.ship/44-TERM-SUPERIOR` plan is fully landed terminal-side. Backend-gated
(liq/margin/ROE%/funding + live internal/engine latency legs, spec 59 inc 3)
stay honestly dashed with a fixed home. Lesson: an interactive-render gallery
(dump every state's stripped ANSI) is the TTY equivalent of a screenshot diff —
it surfaced two real defects (input mismatch, border shatter) nothing else did.
