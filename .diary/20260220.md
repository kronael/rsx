---
summary: |
  RSX perp exchange. Solo work. Major cleanup + CMP seq continuity fix.
  - 134 stale files deleted: docs/specs/v1/, todos/, archive/, duplicate playground docs
  - discovered: ME emits BBO on every book change, consuming seq slots; risk was
    swallowing BBO without forwarding, breaking GW expected_seq and stalling fills
  - fix: risk now forwards BBO raw bytes to GW to keep seq stream gapless
  - blog/04 and blog/06 enriched; playground readme + pages improved
---

## 06:49

Mass cleanup (94bf4dab): deleted 134 files / 39 342 lines — obsolete
`docs/specs/v1/`, `todos/`, `archive/sessions/`, duplicate playground docs
(DOD, FIXES-APPLIED, PROGRESS, TEST-PLAN, COVERAGE_MATRIX, etc.), and the
old `docs/blog/` tree superseded by the current `blog/` dir.

## 06:49

Critical correctness fix (be69ac32): ME emits BBO records on every book
change, consuming CMP seq slots. Risk was consuming BBO without forwarding
to GW, causing GW's `expected_seq` to drift. FILL and ORDER_DONE deliveries
stalled. Fixed: risk forwards BBO raw bytes to GW (ignores content); seq
stream stays gapless. Required changes across `rsx-dxs`, `rsx-gateway`,
`rsx-matching`, `rsx-risk`.

## 06:49

Playground `market_maker.py` expanded (+69 LOC). Blog posts `04-wal-and-recovery`
and `06-test-suite-archaeology` enriched. `blog/README.md` written. Playwright
`play_book.spec.ts` + `play_orders.spec.ts` updated (ea6e51d).
