---
summary: |
  RSX perp exchange. Solo work. 2026-W07 day 2. Heaviest
  commit day of the week: ~35 commits. Full live path wired.
  - CMP preamble standardized; CmpRecord trait.
  - ME→Risk→Gateway event forwarding complete (Phase 3).
  - Liquidation engine: check + generate + wired into risk.
  - Insurance fund, tick/lot validation, backpressure.
  - ME order dedup (5min pruning, OrderAcceptedRecord).
  - DXS TLS for WAL replication; WAL archive fallback.
  - Per-order frozen margin release fixed (fill-side bug).
  - open: mark→risk CMP feed routing bug (fixed same day).
---

## 11:50

CMP payload preamble standardized across all record types; `CmpRecord` trait
extracted so encode/decode is one impl per type (730c54). Header simplification
+ Phase 1 complete (23d81f5). UB in decode fixed, `as_bytes` consolidated,
CRC validation wired (7e5427c).

## 13:49

ME→Risk→Gateway event forwarding (Phase 3) complete: fills flow
GW→ME→Risk→GW. Gateway connection handler impl, cancel handler, heartbeat,
per-IP rate limit, circuit breaker, JWT auth all wired (87e23b7, b22baf2,
a9ab828). Marketdata receives ME fanout via CMP; shadow book decoder landed
(d7dbee7, 9cee8bd).

## 14:23

Time helpers consolidated into rsx_types::time — per-crate `now_ns()` wrappers
removed (6af178b). PROGRESS.md: phases 1-3 complete, 494 tests, 19.5k LOC
(269f7db).

## 15:30

Liquidation engine shipped: `check_liquidation`, `generate_liquidation_order`
with `LiquidationConfig` stub in risk config (1f1876, 7bd9893, e0b217).
Wired into risk shard: check after each fill, send liquidation orders to ME
(e0b217). Server heartbeats (5s/10s), idle timeout, stale-connection reaping,
`ORDER_FAILED` routing (b184652).

## 15:53

Mark receives prices from Mark process via CMP for liquidation checks
(d07ca4). BBO emission from ME after best bid/ask changes (e91ece6).
`ConfigApplied` handling added to risk/matching/gateway (64399ae).

## 17:11

Edge-case spec docs batch: liquidation round edge cases, WAL replay edge
cases, order validation edge cases, 60+ position tracking edge cases (3457fca,
e8ab6f1, 074e212, 1c57cb7). TLS support for WAL replication (d658573).
Per-IP rate-limit E2E tests (e1763396).

## 20:23

Insurance fund: `InsuranceFund` struct, socialized loss processing, cold-start
load from Postgres. Tick/lot validation at gateway before order routing.
Persist ring backpressure in risk shard main loop (5e9ace2). DXS TLS/archive,
JWT, marketdata replay/heartbeat, matching config from prior session squashed
into checkpoint 55430361.

## 20:52

CMP env-var config, seq gap detection, symbol halt, archive fallback fix
(851db0). Gateway status codes corrected per WEBPROTO spec (5613c8). ME order
dedup with 5min pruning and `OrderAcceptedRecord` in WAL (b5f48e7). Orderbook
snapshot save/load with binary serialization (52541c6). WAL dump debug binary
`rsx-wal-dump` (5ccf2d5). SAFETY comments on all remaining `unwrap` calls
(7ad1048, e62ef6f).

## 21:20

Discovery: `Mark→Risk CMP feed` routing was broken — wrong CMP socket addr
(f416d66). `OrderDone` status mapping wrong in gateway convert.rs — fixed.
Per-order frozen margin release fix: `fill_side` was setting `frozen_amount=0`
instead of the actual frozen amount (2169a94). `wal_dump` moved to rsx-cli
crate; replica round-trip tests added.

## 22:13

Per-crate ARCHITECTURE.md docs + comprehensive README rewrite (a16642,
6a75941). Makefile targets fixed; CMP/TLS/JWT/config test coverage improved
(b8af9fe). Crate READMEs, spec TOCs, blog post stubs (548172). `rsxcli` crate
added with `wal-dump` subcommand and stub matching bench (dbcd331).
