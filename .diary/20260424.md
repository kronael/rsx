---
summary: |
  RSX perp exchange. Solo work. Today: OAuth wiring + 06-PUBLISH sprint (7/10 done).
  - frozen_margin leak root-caused and fixed (RSX_RISK_RESET_FROZEN_ON_START)
  - order-flow end-to-end verified working post-fix (WAL 500KB+, mid_override confirmed)
  Open items:
  - frozen-margin release audit (fills/cancels must release) — 06-PUBLISH task 4
  - 3 deferred 06-PUBLISH tasks (task 5 + task 6 + task 10)
  - rsx-auth GitHub OAuth app secrets still needed from user
---

## 08:46

OAuth UI wiring: `AuthButton.tsx` + `auth.ts` (93 LOC) added to rsx-webui. "Sign in
with GitHub" button in TopBar calls `/auth/proxy` on playground, receives RSX JWT.
Commit e62edb5.

## 08:48

Playground manages `rsx-auth` as a subprocess: `POST /api/auth/start` /
`POST /api/auth/stop`. Guard `_auth_configured()` blocks start if
`RSX_GW_JWT_SECRET` or GitHub client id/secret are missing. +139 LOC to server.py.
Commit 5f87d6c.

## 08:53

06-PUBLISH housekeeping: added `.env.example`, fixed stale test counts in scripts,
updated canonical Playwright count. Also fixed `.gitignore` to exclude `.env`.
Commit 741b0d7.

## 09:02

Discovery: mid_override was a red herring. maker IS reading it correctly. Real bug:
orders never reach ME — WAL is 0 bytes. ME logs show "cancel: order not found"
for inserts that never landed. Book endpoint was showing `_maker_book` fallback
(maker-status.json), not real ME state. Investigation plan recorded. Commit c0d1adf.

## 09:03

Replaced vacuous `test.skip()` in `play_latency.spec.ts` with explicit `status==200`
assertions. Old code was silently passing with no checks. Commit 8260686.

## 09:05

Decision: liquidator spec was wrong. Code processes positions before orders (correct:
positions must be current before liquidation eligibility check). Updated
`specs/2/13-liquidator.md` to match code. Commit 04b0b8c.

## 09:06

Updated canonical Playwright count to 421 in `gen-release-truth.py`. Commit 04d43b6.

## 15:16

Root cause found for WAL=0 / orders-not-reaching-ME: `frozen_margin` accumulated in
DB across runs, reaching 1e17 on user 99. Every pre-trade margin check rejected
`InsufficientMargin`. After `UPDATE accounts SET frozen_margin=0` + cold restart: WAL
grows to 500KB+ in seconds, mid_override confirmed live (50000→49900/50100,
51000→50898/51102). Commit a8cca5c.

## 15:21

Fix: `RSX_RISK_RESET_FROZEN_ON_START` env var. When set, cold start zeroes
`acct.frozen_margin` before WAL replay; replay then re-freezes still-pending orders
via existing path. Default false (production-safe). `start` script sets =1 for dev.
Tested: inject frozen=9.9e15 → restart → frozen=93B (real pending only). Commit eaea43e.

## 15:24

Wired liquidation env vars: `RSX_LIQUIDATION_{BASE_DELAY_NS,BASE_SLIP_BPS,MAX_ROUNDS,
MAX_SLIP_BPS}`. Previously hardcoded. Tests updated. Commit 73e7131.

## 15:27

06-PUBLISH status: 7/10 tasks done. System verified end-to-end. Remaining deferred:
task 4 frozen-margin release audit (fills/cancels), task 5, task 6. `trade-ui` tests
90/93 pass (3 failures assume empty state, not functional bugs). Commit da2b8fb.
