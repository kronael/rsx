---
summary: |
  RSX perp exchange. Solo work. Full arc: F13-F28 audit fixes, CTO+CEO
  dual-lens review, refine-2 sprint, FillRecord wire change, 6-stage
  latency measurement, rsx-log crate, bench harness suite.
  - GW→ME→GW Rust p50 = 1 128 µs (not 11.8ms; Python WS overhead = 10.7ms)
  - Dominant cost: monoio sleep(100µs) in CMP poll loops (~655µs)
  - 100µs sleep fix attempted (da21d07) → reverted 9bbb8f6 (starves accept)
  - rsx-maker dead code removed (292 LOC)
  - open: monoio-native UdpSocket on CmpReceiver (~50 LOC); LANDSCAPE
    harness fixes pending
---

## 00:08

play_safety bf5b165: poll for all-stopped instead of fixed delay — avoids
flaky timing in stop/start sequences.

## 08:17

playwright regression specs eb30730: F3-F11 covered with dedicated tests.

## 08:21

Clippy 1.93 errors fixed 533ccf9: blocked gate-1/gate-2; now clean.

## 08:37

F20 fixed 58b567e: start_all binary-kill scoped to build path, SIGTERM-first.
Previously killed all processes named the binary, not just the managed ones.

## 08:52

Oracle pass 2 a320ef9: **8 more lies** (F21-F28) — perf/telemetry dashboard
fiction. Core affinity always "0", latency probe cid mismatch, bench labels
overstating throughput. Filed with line citations.

## 09:17

F13-F19 fixed 47b6fce: dashboard stops lying about process state and pricing.
rsx-book tracing warn swap bbb0f9f: 2 eprintln! → tracing::warn.
rsx-dxs 8MB UDP recv buffer 2fe18e8: survives bursts without drop.
TODO.md rewrite 596d24a: shipped/open ledger, stale F1-F19 detail dropped.
F21-F28 fixed 596d24a: real affinity, cid-matched probe, honest labels.

## 11:02

JtiTracker wired through ws_handshake 72bd481: was shipped but dormant.
Replay defence active: second handshake with same token → 401 "jti replay".
Test: test_ws_handshake_rejects_jti_replay. Count: 883 / 0 / 46 (+1).

## 11:19

BLOG.md reframed 82f096d: wedge framing (rsx-dxs as open-source orthogonal
lib, exchange as load-bearing demo). First reliable e2e baseline post-F22:
p50 = 11 778 µs, p99 = 347 755 µs. Old probe had cid mismatch — returned
on first F frame of unrelated fill, masking real tail.

TODO.md 047bdf2: all v0.2.0 carry-overs closed; ready for v0.3 cut.

## 11:40

Baseline refined 21792d5: N=2000 run. p50 = 11 780 µs, p99 = 233 ms
(was 348 ms at N=200 — more samples smooth tail outliers).

## 12:21

Publishing policy 0d9d265: CLAUDE.md + BLOG.md + TODO.md. No external
publishing (crates.io, npm, PyPI, blog, twitter) without sign-off.
GitHub is ok when explicitly asked.

## 12:26

CTO+CEO dual-lens review methodology 1811ace: meta/cto-ceo-review.md
codifies the repeatable pattern — CEO=browser-only, CTO=code-only,
adversarial stance, anti-patterns, shared report skeleton.

## 12:41

CTO due-diligence 53b7be4: code/spec review. CEO due-diligence a3d9ec0:
live playground run. Synthesis 1453a8f: 6 actionable fixes for v0.3.

## 13:55

Refine-2 plan 3858e44: 4 buckets synthesized from CTO+CEO findings.
Bucket 1: i64 formatter, jti emit+reject. Bucket 2: bench probes. Bucket
3: MAX_EVENTS + fill stall. Bucket 4: let _ = sweep.

## 14:04

F3.1 605b317: i64 formatter for risk/topology/maker (was truncating to
string "0" for large values).
F2.1/F2.2/F2.3 f4ff065: emit jti in responses; reject tokens missing jti.

## 14:07

F4.1 59965c5: native Rust E2E latency probe (bench-probe binary).
F4.2 f93ade1: gateway-only RTT probe (/api/latency-probe-gw).

## 14:18

F3.2/F3.3 4aa554e: TTL cache for topology + verify-fail → red health pill.
F3.4 aec96ec: per-pipe CMP counters from per-process WAL.
F4.3 daccaba: per-stage latency tracing on GW→ME→GW path (6 stages).

## 14:22

MAX_EVENTS raised 65 536 5032085 + 9159639; assert on overflow. Enforces
spec invariant "ME never drops events".

## 14:24

Fill ring stall-loop cbfdb8d: `let _ = fill_prod.push(...)` was silently
dropping fills on full SPSC ring — breaking position = sum(fills) invariant.
Now spin-stalls calling shard.run_once. BBO/mark drops counted, logged.
let _ = sweep ee30c37: **19 silent-drop sites** fixed workspace-wide. CMP
sender.send Ok(false) annotated SAFETY (receiver recovers via NAK/replay).

## 14:37

Refine-2 closeout 5bb3f35: all 9 items shipped. 885/0/46 tests.
Deployment gap noted — post-fix latency capture blocked by shared cluster.

## 15:12

rsx-maker removed f7fce24: 292 LOC dead code. Playground uses
market_maker.py; the Rust binary was never on the live path.
Workspace member dropped 3b803ee; Cargo.lock refreshed.

## 15:13

jti added to every JWT mint site 2e97b4c: market_maker.py, server.py
probes, stress_client, bench-probe, tests/conftest. Gateway's new jti
enforcement was 401-ing the maker → killed via preflight circuit breaker.

## 15:21

Per-stage latency first measurement 2a237cd: STAGE-LATENCIES.md. First
time the individual Rust legs were documented.

## 17:48

Discovery: probe U+F race fixed 82e9966. F (fill) can arrive before U
(ack) via different SPSC paths. Old code: early F classified as unrelated,
probe timed out after 2s → broken-pipe symptom. Fix: buffer F by taker_oid
until U arrives, retro-match. First correct probe captures.

## 17:49

FillRecord wire change 2fc3bac: added taker_ts_ns: u64 at offset 88
(fits in 32B implicit padding, sizeof stays 128). Unified 6-stage tracing:
risk_out + gateway_out anchor on taker_ts_ns, composable with gateway_in.
26 test/bench literals updated. 885 / 0 / 46 tests.

e2e baseline post-fix 0355f2a: p50 = 11 876 µs, p99 = 100 107 µs.
N=551. First end-to-end p50/p99 that is actually correct.

## 17:52

Confirmed: GW→ME→GW Rust p50 = **1 128 µs** a1e8358. The 11.8ms probe
number decomposes as ~1.1ms exchange (Rust) + ~10.7ms Python aiohttp overhead.
risk_out→gateway_out = 666µs = 60% of Rust budget (CMP poll sleep dominant).

## 19:56

REFINE-2 final report a2e975: full arc — review, sprint, maker/probe
regressions, wire change, 553 coherent 6-stage measurements.

## 19:59

Speed baseline 0d0097a: snapshot for future regression. Per-leg p50:
gw→risk 60, risk→ME 205, ME match 158, ME→risk 39, risk→gw→WS 666, total
1128µs. Six hypotheses listed for granular telemetry pass.

## 20:03

Granular sub-stage tracing 8c6d3ba: 222 LOC added across gateway, risk,
matching mains. Captures sub-stage timestamps inside ME and gateway/risk legs.

## 20:10

SPEED-GRANULAR.md a514244: hypothesis verdicts + targeted optimisation list.
Mining scripts mine_deltas.py + mine_substages.py for log extraction.

## 20:33

Off-hot-path latency drain 08cb179: hot path was paying ~58µs per
tracing::info! in debug builds. New design: thread-local rtrb ring
(8192 samples), drain thread wakes every 100ms. Push = wait-free SPSC
~20-30ns. 887/0/46 tests (+2 latency module unit tests).

## 20:38

Measured: off-hot-path tracing saves ~20µs net bdc2e1a. Real bottleneck
confirmed: CMP poll loop's 100µs sleep, NOT tracing emission.

## 20:50

rsx-log crate extracted 5ad7d91: off-hot-path logging into its own crate.
rsx-types is the foundation; pulling rtrb+tracing into it forced deps onto
rsx-dxs which is supposed to be domain-agnostic.

## 20:56

100µs sleep killed da21d07: replaced with rsx_types::yield_now() — returns
Pending once, wakes by ref, resumes on next scheduler tick (single-digit µs,
no timer). Expected: ~500µs off Rust GW→ME→GW p50.

## 20:58

Gateway bench suite 401ce0c: ws_parse, ws_encode, jwt_validate, jti_tracker
criterion benches.

## 21:01

Component bench suite daafdfb: ME accept path, n-level sweep, WAL replay,
md shadow_book_apply, mark aggregator — 757 LOC across 8 files.

## 21:09

Reverted yield_now 9bbb8f6: monoio single-threaded reactor — yield_now
starves the accept task. The fix needs monoio-native UdpSocket on
CmpReceiver to expose `readable().await`. ~50 LOC refactor; deferred.

## 21:10

Meta-review cf17a43: .ship/18-META-REVIEW/PROGRESS-REVIEW.md (758 LOC)
+ course-correct proposal. Captures where the project stands across all
sprints.

## 21:14

DXS bench suite committed c2fd3bd: udp_rtt, cmp_one_way, cmp_rtt,
wal_fsync, wal_random_read, risk validate — sub agent produced files but
timed out before commit; recovered manually.

## 21:32

LANDSCAPE.md + harness fixes 60c0b56: component-isolation results.
Key findings: book_match_n_levels n=1 = 149µs (Criterion's 54ns only
measured the inner fn); WAL append+fsync = 651µs (never measured before).
e2e pipeline main loop reworked a0b7973: 75% timeout issue patched.

## 21:40

LANDSCAPE final 1aedaea: **9µs in-process p50 vs 1 128µs cross-process**.
118× overhead is all in CMP plumbing + tokio scheduling + 100µs sleeps.
The ME algorithm itself is 320ns p50.
