---
summary: |
  RSX perp exchange. Solo. PM session: OAuth fix + playground UX overhaul.
  Open items:
  - PLAYGROUND UX SPRINT (.ship/29): [resolved 2026-05-31: all shipped — verify
    <details> rows 781ca04, affordances+stop-fix e504d32, embed-walkthrough opus
    eval 7d35585 (verdict: do a variant — owner decides on impl).]
  - #12 dashboard stability: [resolved 2026-05-31: code part (raw-PID stop now
    marks intentional → watcher stops reviving, e504d32) done; rest is the
    known harness limit + systemd = ops decision, not a code defect.]
  - GitHub OAuth: nginx 404 fixed; BLOCKED ON USER — needs real GITHUB_CLIENT_ID/
    SECRET in .env + a GitHub OAuth App to complete a real login.
  - EMBED-WALKTHROUGH variant: eval done; implementing it is BLOCKED ON USER
    go-ahead (new task) — see .ship/29/WALKTHROUGH-EVAL.md.
  - PLAYWRIGHT NOT YET GREEN: pre-fix 180/17/262; 4 fixes landed; full re-run pending.
  - ORDER PATH 500: [resolved 2026-05-31: send_order_to_gateway returns 3-tuple on
    all paths; KeyError(1)→500 gone. Deeper "orders don't reach ME" layer untouched.]
  - risk HEALTH SERVER not up: RSX_RISK_HEALTH_ADDR unset by `start`. Still pending.
  - rsx-risk 16-item refactor (task #42) still pending (founder live-review backlog).
---

## 07:10 — /fin: playwright/playground green

Gates 1-3 (FastAPI server tests): GREEN after fixing CMP→cast sweep fallout —
`/x/cmp-flows`→`/x/cast-flows` in 2 gate tests (test_htmx_partials,
test_no_absolute_links) the sweep missed (114132d). gate-3 563/564 (1 = a
cluster-dependent stress test mis-sitting in cluster-less gate-3).

## 07:20 — full Playwright run: 17 fail, cascade

play-full: 180 pass / 17 fail / 262 did-not-run (run ABORTED). Failures =
safety+latency+htmx (fills/wal-after-order, maker offline, no bids, gateway
restart recovery). Root: gw-0 PANICKED on restart —
`failed to bind cast receiver: AddrInUse` at main.rs:246 (.expect on a fast
restart racing the old socket release). Fix: retry bind on AddrInUse (b46888d).
This is the founder's item-10 (bind .expect→retry, not panic).

## 07:35 — deeper root cause: external PG

Even with gw fixed, orders 504'd. risk-0 CRASH-LOOPING "error connecting to
server" — cluster pointed at `postgres://rsx:folium@10.0.2.1:5432/rsx_dev`
(sandbox host gateway, unreachable). Source: server.py:64 hardcoded DEFAULT
`DATABASE_URL` = that dev-machine value (server reads DATABASE_URL, but .env
sets PG_URL — mismatch, so .env never applied). Fixed default → local
rsx:rsx@127.0.0.1/rsx (13eaac0), matching .env/start DEFAULT_PG_URL/CLAUDE.md.
Started a local `rsx-postgres` docker container (rsx/rsx/rsx:5432).

## 07:42 — boot-hang on fresh cluster (FIXED) → risk Live

risk then stuck in WARM CATCHUP: "replay not available from seq=1" — fresh ME
(empty WAL) can't serve a replay, risk retried forever, never Live. The earlier
boot-hang fix only covered tip>0; fresh cluster has tip==0. Fix: treat NotFound
at tip==0 as caught-up (empty ME → nothing to apply → Live) (3c4efba). risk now
reaches `state=Live` cleanly on a fresh boot.

## 07:50 — order-500 fixed; E2E re-run BLOCKED on harness

Order-500 root cause: server.py `send_order_to_gateway` returned a DICT on
missing JWT secret while every other path returns a 3-tuple → caller's
`result[1]` = KeyError(1) → 500 "detail:1". Fixed → 3-tuple (graceful error).
Also: the playground server lacked RSX_GW_JWT_SECRET (gateway has
`rsx-dev-secret-not-for-prod-padpad`); the launcher injects it via setdefault.
BLOCKER: cannot keep a detached playground server alive across Bash tool calls
in this harness session (runs fine foreground; first launch survived 20min but
not reproducible after the PG-fix restarts). Full Playwright re-run needs the
server up ~15min → blocked HERE, not a code defect. In a normal env (persistent
`playground start`), the cluster boots risk→Live with all 6 fixes and the 17
cascade failures should clear.

## 07:44 — cascade guard: stop + hand off

Order still 500s ("detail:1") and ME WAL stays 0 — orders don't reach ME (5th
layer). Per /fin cascade guard (PG→crash→boot-hang→order-500, each fix reveals
the next), STOPPED to report rather than spiral. 4 real fixes committed; cluster
now boots with risk Live; order-fwd path + risk health-addr wiring are the next
layers. Local PG container must stay up for any cluster run here.

## 08:00 — PM session: OAuth 404 + playground UX overhaul (.ship/29)

OAuth login was nginx 404: /etc/nginx/sites-enabled/rsx had no /auth or /oauth
location. Added both → :49171 (playground proxies to rsx-auth). 404 gone (now
graceful 502 when auth down). Still needs real GitHub creds in .env to complete.

## 08:10 — opus sub as oracle (codex dead: usage limit)

codex hit account usage limit → used an opus sub for plan sign-off instead.
Returned REVISE (applied): no CPU as health (busy-spin=100%=green by design);
show status+RSS+logs+viz; docs open new-tab (not in-layout); per-component fan-out
must be serialized (one writer on pages.py/server.py). Plan in .ship/29/PLAN.md.

## 08:20 — 4 UX bugs fixed (c38b0d1)

From a 4-agent 13yo browser audit (.ship/29/AUDIT.md): topology /x/topology/summary
was shadowed by the /{component} catch-all (moved below) → "unknown component"
gone; risk Deposit/Freeze hardcoded user 1 → riskAction() templates from #risk-uid;
latency "rsx-dxs"→"rsx-cast" + dropped leaked .ship path; orders 504 silent-fail →
response-error handler. "walkthrough auto-redirect" was a PHANTOM (no such code).

## 08:30 — shipped: docs split, cast page, overview, component pages, logs

docs GUIDE vs DOCS split via /docs/<root>/<file> allowlist (guide/concepts/
platform/spec), traversal-guarded (8920db2). Cast demo page /cast + overview
live-latency & load-test cards (3f61a66). Per-component pages /component/{key} +
Components tab, health=status+RSS+logs+viz (5031794). Logs view: killed the
full-screen modal → inline-accordion table + pill filters + JS tail (1a16411).

## 08:35 — confirmations from prior entry

The uncommitted rsx-risk/main.rs working change = this morning's tip==0 fix
(3c4efba) — founder's, left untouched. Dashboard "deaths" this session are the
SAME known harness limit logged at 07:50 (can't persist across Bash calls),
plus a couple self-inflicted (pkill -f server.py matched my own shell). Not a
new code defect. PG showed down (10.0.2.1:5432) — local rsx-postgres container
likely stopped since the morning.
