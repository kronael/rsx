---
summary: |
  RSX perp exchange. Solo work. Naming unification day.
  - DB tables, Rust structs, PersistEvent variants, and REST endpoints aligned
  - liquidation_events→liquidations, funding_payments→funding (migration 002)
  - /x/live-fills→/x/fills; PersistFill→FillRecord etc.
  - NAMING.md written: canonical name table for all concepts
  - playground: auto-seed accounts + auto-start maker wired into start_all()
---

## 21:39

Naming was inconsistent across DB, endpoints, and Rust types. Unified in one
pass (072089417): DB migration `002_rename_tables.sql` renames
`liquidation_events→liquidations`, `funding_payments→funding`. Rust:
`PersistFill→FillRecord`, `FundingPaymentRecord→FundingRecord`,
`LiquidationEventRecord→LiquidationRecord`. Endpoint `/x/live-fills→/x/fills`.
Fixed SQL missing-space bug in risk-user position queries (×2). `NAMING.md`
written as canonical reference (52 LOC). `.ship/tasks.json` + PROGRESS.md
regenerated.

## 21:51

Playground `start_all()`: added `seed_accounts()` — upserts users 1–5 + 99
with 1T collateral into Postgres before risk engine starts (idempotent,
`ON CONFLICT DO NOTHING`). Auto-starts maker 3s after processes spawn.
Extracted `do_maker_start()` from `api_maker_start()` handler (26b134c).
