---
summary: |
  RSX perp exchange. Solo work. CI pipeline hardened; trade UI polished.
  - risk crash-loop fixed: migrations now run on startup, DATABASE_URL required
  - pg_locks.objid is oid (i32), not i64 — lease renew type mismatch fixed
  - 4-gate hard-ordered CI: gate-1-startup→gate-2-partials→gate-3-api→gate-4-playwright
  - E2E matrix 6/6 green, 806 tests by end of day
  - .ship/tasks.json + PLAN.md + PROJECT.md initialised for this sprint
---

## 08:45

Risk process crashed in a loop on fresh start: migrations were not run and
DATABASE_URL was not required. Fixed both; also corrected trade UI proxy paths
in `vite.config.ts` and WS hook base URLs (608c828).

## 08:55

Discovered `pg_locks.objid` is type `oid` (maps to i32), not i64. Lease renew
was casting wrong — fixed in `rsx-risk/src/lease.rs` (bb1abf3). One-line fix,
silent bug.

## 10:31

Added `SHIP-TRADE-UI.md` (282 LOC) — ship spec for the trade UI phase. Fixed
duplicate docs link + broken architecture links in `rsx-playground/docs/README.md`
(99817e0).

## 10:40

Orderbook: both/bids/asks view toggle, 1x–50x tick grouping (display-layer
bucketing), toggleable count column. Extracted `useOrderbook`/`useBbo`/
`useTrades`/`useSymbolMeta` Zustand selectors with shallow equality to prevent
unnecessary re-renders (90a74bf, +1017 LOC, -455 LOC).

## 10:42

TP/SL placeholder inputs added to OrderEntry — side-by-side, state tracked
locally, ready to wire when protocol supports it (ba26f9c). Settings store with
`localStorage` persistence: `soundEnabled`, `confirmMarketOrder`,
`confirmCancelAll` (2a1b340).

## 10:44

`useKeyboard` hook: B/S keys focus price input and set side; Up/Down nudge
price by one tick when focused; Enter submits from price or qty input (40a329a).

## 10:45

Chart: EMA9/EMA21/BB/RSI indicators + H-Line/Trend drawing tools. Depth chart
component added (426 LOC). `useSoundAlerts` hook. Fixed pre-existing TS errors
across Orderbook, DepthChart, useKeyboard (a427c01, +1356 LOC).

## 10:48

TopBar: funding rate countdown (live 8h timer), mark price, index price.
`useFundingData` selector with `nextFundingTs` field (805d20d).

## 14:15

Hard-ordered release gates in Makefile: each gate depends on prior passing.
`conftest.py` autouse fixture patches `httpx.Client` to fail fast on 5xx with
endpoint-class triage (32a160d, +174 LOC).

## 14:25

Playwright sharding: 4 domain projects with failure-signature hashing via
`play-shard.sh`. Added rendered-HTML regression for root-absolute href/src.
`COVERAGE_MATRIX.md` mapping all 223 tests to page/endpoint/user-flow with
P0–P3 blast-radius labels (c51ddf3, +879 LOC).

## 14:33

`scripts/check-progress.py`: validates PROGRESS.md header/table/denominator
consistency; fails CI when denominator ≠ 223. `gate-3` now writes
`gate-3-report.json` and diffs via `report_diff.py`. `conftest.py` JSON report
accumulator. `play-shard.sh` JUnit+JSON per shard (e609403, +1230 LOC).

## 14:37

Release gate: `gate4_status()` reads `play-artifacts/<shard>/report.json`;
`canonical_ok` blocks `all_green` unless exactly 223 tests pass (cb856e2).
No-retry-storm policy: cap retries at `MAX_RETRIES=3` per failure signature;
contradiction linter rejects snapshots where any test key appears in both DONE
and FAIL (a2fa943, 954e70f).

## 15:02

E2E matrix run: 6/6 steps green, 806 total tests. Added `allow_5xx` marker to
conftest; `e2e-matrix.sh` (219 LOC). PROGRESS.md rebuilt (682 lines, +861 LOC
total) (27f63c1).

## 15:05

Supersession logic in `acceptance-bundle.py`: auto-removes `.sig`/`.count` when
a shard passes; records `superseded[]` in bundle snapshot (59d1ea5).

## 18:13

Three parallel commits landed together:
- Playground: `market_maker.py` (268 LOC), server + pages improvements,
  stress client, `DOD.md` + `PROGRESS.md` for playground (1f0dc08).
- WebUI: positions panel rewrite, Assets component, `ring.ts` + `sounds.ts`
  libs, Playwright config, 3 new spec files (order-entry, orderbook,
  orderbook-bench) (6e32919, +1469 LOC).
- `.ship/tasks.json` (4123 LOC), PLAN.md, PROJECT.md, SHIP-PLAN.md,
  `scripts/publish-progress.py` (375 LOC) (2e0f845).
