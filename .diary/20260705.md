# 2026-07-05

## 00:04 — playground to green: env fix + restyle + 12-failure fix

Continuation of the 2026-07-04 push. Goal: playground up, demos deployed,
all tests green, deploy; then recorder/marketdata/gateway to cast quality.

**Environment was the real blocker (not code).**
- OOM on a 7.8 GB box: two concurrent Playwright suites + full cluster +
  agents → oom-kill; marketdata/maker died → ~100+ tests cascaded.
- marketdata 48 GB-alloc crash-loop: replaying accumulated WAL sized the
  shadow book's level array to ~2 B `PriceLevel`s (poisoned/extreme replay
  price from prior crashes). Logged MARKETDATA-SHADOW-BOOK-UNBOUNDED-LEVEL-
  ALLOC. Cleared `tmp/wal/*` + tips → **6/6 green, stable**.

**Test cascade root cause:** global-setup only *polled* the maker, never
started it; the minimal scenario has no maker → empty book → every book/
activity test failed. Fixed global-setup to start it → dropped to **12
genuine failures** (190/386 in the play-full subset).

**Restyle (user):** every left-accent callout bar → surrounding "ribbon"
border (krons.fiu.wtf palette, `border`/`border-2` + `rounded-[3px]`,
colours unchanged); rsx-book WHY.md (humanized design rationale) now rendered
on the crate page; mobile-responsive docs (`min-w-0` flex fix + table
overflow); design-system doc flipped LEFT-BARS → SURROUNDING ACCENTS.

**12 failures fixed** (see `.ship/38-PLAYGROUND-GREEN/FAILURES.md`): code —
risk-action buttons `onclick` → declarative `hx-post`; `api_orders_test`
503 → 200 graceful degradation; latency probe always emits `skipped_fills`
(F22); market_maker backs off instead of exiting after gateway restart.
tests — aligned stale assertions (WAL "has data", card renamed to "WAL stream
lag", dev "no pinning", raw `->`, "hung order", pulse timeout); global-setup
heartbeat renews the *active* session id (orch-smoke swaps it).

Also: honest compare/ docs for recorder/marketdata/gateway; memory rule —
docs describe what-is-now, never history/fluff. Verification play-full run in
progress.

## 03:55 — disk unblock + maker root cause found

Session hit total disk exhaustion (ENOSPC on the harness tmpfs AND root fs)
from ~6 play-full runs' artifacts + subagent transcripts — bash stopped
executing entirely (couldn't create command-output files), Write failed too.
Freed externally → resumed.

**Maker root cause: `localhost` → IPv6 `::1`, gateway binds IPv4 only.** The
maker's `ws://localhost:8080` preflight resolved to `::1` (Network
unreachable), aborting the maker after 8 tries. Fix: `127.0.0.1` for
gateway/marketdata WS in market_maker.py + server.py URL defaults. With the
fix loaded on a FRESH dashboard, the maker now runs `errors:[]` clean and
stays up (90s+ stable, was dying pre-#107). Prior "still failing" runs reused
STALE dashboards (old localhost code) via `reuseExistingServer:true` — the
process-management churn, not the fix. Definitive play-full run in progress to
confirm the last 3 (#107/#184/#197) clear → green.

