---
summary: |
  RSX perp exchange. Solo work. Blog-as-GTM: open experiment,
  demonstrate exchange internals, benchmark transport, publish openly.
  Open items:
  - Reliability bench harness (netns + netem) — still pending
  - End-to-end latency harness (measured GW→ME→GW cross-process)
  - bench checklist in specs/2/52-blog.md before publishing
  - FAULTED replay wiring for risk + marketdata + gateway (highest-ROI next-sprint item; without it the auto-maker overruns rmem and risk panics)
  - WAL fsync (651 µs single / 24 µs batch) + random-read (23.5 ms @ 10K) numbers in rsx-cast README need host-pinned re-measure before any external publication — see .ship/27/RESULTS.md
  - `make tune-host` to bump rmem_max so maker auto-start can come back
  - F-N4-F-N16 playground honesty cluster (synthetic book ladder, IOC-as-resting, false-PASS stress, gateway max-conn cap, etc.) — deferred from .ship/27 fix sub
  - notes/ diagrams trimmed too aggressively — pseudocode + tables to restore
---

## 00:30

.ship/27-REFINE-AUDIT closed at 50a5593. 48 commits since 4329f32.
RESULTS.md: inner-loop perf claims hold within noise (31 ns WAL
append, 340 ns match, casting RTT in line). WAL fsync numbers
+ 23.5 ms random-read flagged for host-pinned re-measure before
publish. SUMMARY.md gives 3 bullets for next-session pickup.
879/0 tests. Demo trades end-to-end with `start-all minimal` +
manual maker start (auto-maker still rmem-blocked).

## 01:30

Skills shipped: cto-eval (adversarial code+bench audit) and
ceo-eval (adversarial demo audit) codify the .ship/27 patterns.
create-eval updated to scaffold `service-eval/` (not generic
`eval/`) — disambiguates from the new adversarial skills.
Synced to ~/app/tools at dea1683.

## 01:45

.ship/28-REFINE-AUDIT-2 launched. Plan at f0c63c7. Carry-over
focus: R1 FAULTED replay wiring (highest-ROI, closes the loop
that today blocks auto-maker); R2 playground honesty cluster
(F-N4-F-N16 from .ship/27/CEO-REPORT); R3 CTO carry-over
(consumer heap, BBO Vec, BBO double-CRC); R4 bench infra
(`make tune-host`, `make demo-trade`, host-pinned re-measure).
