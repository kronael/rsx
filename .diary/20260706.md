
# 2026-07-06

## 07:46 — playground audit → 24 dashboard fixes + test-class extension; disk emergency

Continuation of the 2026-07-05 playground overhaul. User asked for "precautions
so I don't find any more issues" → ran an **exhaustive adversarial browser
walkthrough** (opus, read-only): 32 findings (`.ship/40-PLAYGROUND-AUDIT/
FINDINGS.md`) — 10 BROKEN, 17 CONFUSING, 5 POLISH. Split DASH (dashboard) vs SYS
(deep system).

**24 DASH fixed** (opus, `cb9fbc4→b6d020e`): recurring root cause = endpoints
returning JSON / `hx-swap=none` where a button expects rendered HTML, and two
sources of truth disagreeing (WAL fills vs persisted PG positions; spawn-plan vs
scanned procs; fresh vs cached cpu). Risk lookup respects user_id, order overflow
guarded, WAL dump/verify render, stress produces reports, ONE process-count
everywhere, reject codes → words, human units, docs re-themed + nav, raw JSON
formatted.

**Test-class extension** (opus, `04eaaf0`): the suite passed while all 24 were
broken because it asserts *rendering*, never *interaction*. Added 4 specs (36
passing): **play_links** (crawl every link on 38 pages, 200-or-405),
**play_render** (no escaped-markup/raw-JSON/unknown leaks, base-href per depth),
**play_controls** (click controls, assert effect), **play_flows** (order fills +
trace reaches done, reject as words). Each maps to a finding, regression-catching.

**8 SYS bugs recorded** (`bugs.md`, Phase-2 — NOT papered over): recorder
dead-but-"healthy" (durability broken + silent — the worst), marketdata drops →
shadow-book vs WAL divergence, mark produces no index, phantom risk positions,
archive WAL balloon.

**Disk emergency:** the archive tier has NO retention (hot tier retains 4h;
archive keeps every daily segment). Maker quote-churn grew it to **115 G** (71.7 G
yesterday + 46 G today) → 22 G free, about to wedge the box. Stopped the maker,
deleted yesterday's segment → 93 G free. Real fix (archive retention) is next.



## 08:50 — Phase-2 SYS bugs fixed, ALL caller-side (rsx-cast never touched)

The audit's 8 SYS bugs were the real Phase-2 (recorder/marketdata/gateway → cast
quality). Fixed across two subs; the standout: **none needed an rsx-cast change**
— every fix was in the caller, vindicating "try caller-side first".

- **Recorder** (`607d10c`,`80ee6fe`): archive had NO retention → maker churn grew
  it to 115 G, nearly wedged the box → `prune_archive` + `RSX_RECORDER_RETAIN_DAYS`.
  Health was a startup constant → watchdog faults `/health` when replication
  stalls (no more "healthy while dead"). Catch-up investigated: cold-WAL tier
  already works in rsx-cast; the recorder only fell behind because ME prunes its
  WAL at 4h and we'd kept it stopped — operational, not a rsx-cast bug.
- **start-all idempotency** (`f1f2d11`): duplicate PIDs came from the 2s watcher
  respawning daemons during the `cargo build` window + an incomplete port-clear
  list (missed health/replication ports) + racy sleeps. Fix: derive ports from
  the spawn plan, detach daemons from the watcher before pkill, poll for
  port-free (not sleep), reap orphans. Verified one PID per binary, 6/6, mark
  binds 9830 clean.
- **Mark no-index** (`01c95db`): Binance **combined** stream wraps trades as
  `{stream,data:{s,p}}` but `handle_binance_msg` read `s`/`p` at top level → early
  return → 0-byte WAL. Unwrap `data`. Verified live: mark WAL 0→growing, Verify
  "mark has data" PASS, index {10: 6644}.
- **Marketdata "drops"** (`1c5aee0`): NOT kernel drops (470/500 delta=1 = false
  detector). ME fans out ALL record types on one monotonic seq, but MD tracked
  seq PER-SYMBOL and only advanced on 3 types → every ignored record read as a
  gap → needless resend. Fix: track seq per-STREAM. Gap-warn 11.3/s → 0/s, drops
  0. The 8 MB rcvbuf in frozen rsx-cast was a red herring.

Secondary (noted, not changed): 4-core box assigns CORE_MD=4 (out of range) →
marketdata unpinned; the "shadow vs WAL BBO" Verify row is a dashboard display
artifact (actual books match, drops 0). Also documented latency's ONE home
(benches + gate, not playtests) in CLAUDE.md + bench-gate.sh.


## 12:40 — post-/fin triage: what's left, what actually needs doing

Session resumed on "is there a need and how to continue with these?" after the
autonomous /fin declared the board green. Assessment, not new code:

- **Nothing blocks the demo/deploy.** System runs e2e, demos work, deploy
  artifacts exist. "Need" is a function of the bar, not a hard gap.
- **Two MED items worth closing before a *real* prod deploy** (both caller-side,
  no rsx-cast): STARTUP-ORDERING-FRAGILITY (supervised bring-up — the one that
  bites an operator) and HEALTH-PORT-BAND-OVERLAP-AT-SID-10 (latent; deploy spec
  dodged it via BTC/ETH/SOL = sid 1/2/3).
- **Hygiene:** RISK-ENV-VARS-SET-NOT-READ, @long grep-invert scoping.
- **Founder-owned:** the rsx.krons.cx push (infra decisions).

Recommendation given: do STARTUP-ORDERING-FRAGILITY next if continuing. Also
flagged suspicious untracked root files (`HEAD`, `config`, `objects/`, `hooks/`,
`scratch_offsets.txt`) — look like git internals / scratch leaked into the tree.

## 14:02 — v0.6.1 release (caught a red integration build) + CAST go-to-market prep

Pivoted to shipping the raison d'être: the rsx-cast story out to the world.

**Release v0.6.1** (`2df2e4d`, tag `v0.6.1`): turnkey deploy + honest
dashboard + the marketdata/mark/recorder/gateway SYS fixes + the rsx-cast
`AsRawFd` accessor. Repo tag; crate versions track independently.

**Caught a regression while running the release verify.** `make test` was
RED, not green — `tee` had masked make's `Error 101`. The SYS #3 fix
(`1c5aee0`, per-symbol → per-stream seq) removed `MarketDataState::check_seq`
but left an entire dead test file (`seq_gap_test.rs`, 15 calls), a stale
assertion block in `multi_me_aggregation_test.rs`, and an outdated
ARCHITECTURE section — all invisible to `--lib`-local runs, so master
carried a red `tests/` build. Fixed caller-side (`123b555`,`ecdc7ab`):
dropped the obsolete file + block, rewrote the doc for per-stream. **Process
note:** integration tests in `tests/` don't compile under the per-commit
`--lib` gate → a whole class of breakage can land unseen.

**CAST go-to-market:**
- Drafted X thread (6 tweets) + LinkedIn post for the rsx-cast demo —
  `scratchpad/cast-social-drafts.md`. Honest framing: same-bytes design +
  ~10µs in-process, NOT sub-10µs cross-net (repo says 1.1ms; copy says so).
  Drafts only — founder publishes (no-external-publishing rule).
- Bench-table caption fix (`0b28e7c`): dropped "(same host, same pinning)"
  for a terse note on harness design (one Criterion harness, fixed 128 B
  payload, pinned core → rows compare directly).
- **CEO-style GitHub cold-visitor critique** (opus + agent-browser, live
  public repo): 10-sec verdict = "AI-vibe-coded toy" because of three
  things — the description literally says "Vibe", a Shakespearean sonnet is
  the first rendered content, and rsx-cast (the crown jewel) is invisible in
  every metadata surface (0 topics, no website link, no social preview).
  Substance underneath is strong and trusted-on-inspection; the packaging
  throws it away in 10s. P0 list (all 5-min settings/README edits): rewrite
  description (kill "Vibe", name rsx-cast), add topics, demote the sonnet
  below a plain-English hook, surface rsx-cast above the fold, add a social
  preview. Screenshots in scratchpad.


## 15:17 — rsx-cast unfrozen for a session: interactive finishing pass

Founder lifted FROZEN on rsx-cast **for this session** ("finish it and perfect
it") and ran a fast, deep, interactive review — a stream of "why is this like
that / fix it / do it like that". Kept the tree green + committed between each.

**Docs/API polish** (commits `06e81fd`→`9f51f78`→`a9375d0`→`e6444b2`):
- Install pins a release **tag** (`tag = "v0.6.1"`) not a placeholder rev.
- Dropped the invented "replication Phase 2" name; reworked the lifecycle
  ASCII diagram (aligned, no Phase labels, clear fault→re-drain loop).
- `process(rec)` not `process(rec.header, rec.payload)` (don't split a record);
  commented `tip_file`/`None(TLS)` params + the `run_once` bool.
- Fixed stale "silently drops" (reorder overrun now → sticky `Reconnect`).
- **Removed crate-local `specs/`** (dup of parent specs); repointed all refs
  (README/ARCHITECTURE/compare + 6 module-header code comments) to parent
  `specs/2/` or ARCHITECTURE. Bench caption → harness design, not host/pinning.
- Moved `nak_fallback_latency_test` src→`tests/`, dropped the `<5ms` latency
  assert (benches own latency); it was an integration test misfiled as `--lib`.

**Behaviour changes** (cast was unfrozen):
- `run` **unified on a bool callback** (`984dd42`): was `FnMut(Record)` wrapped
  to always-true so the user's callback couldn't stop it. Now `FnMut(Record) ->
  bool`, `false` stops. Threaded `io::Result<bool>` through
  connect_and_stream/handle_stream (Ok(true)=reconnectable EOF, Ok(false)=stop);
  `run_once` keeps its `()` facade. This also **resolved** the "run_once ==
  connect_and_stream" redundancy the founder flagged — they now differ by return.
- **Tip file → JSON number** (`88..`): `persist_tip` writes `tip.to_string()` (a
  bare decimal, valid JSON), `load_tip` parses; missing/unparseable → 0
  (idempotent). Human-readable, no serde_json dep. Tests hit the real fns.

**Design findings surfaced (Q&A, not all acted):**
- `reset_after_replay` **clears the whole reorder ring**, but its docstring says
  it only drops seqs `<= new_tip` — doc/code mismatch. The founder's "buffer UDP
  during TCP replay, then feed" idea = making the code match that intent
  (preserve+deliver seq>tip). Level-1 (preserve on reset) = cheap win; Level-2
  (listen concurrent with drain) = bigger. Not yet done.
- NAK handoff is receiver-driven; the TCP→UDP seam is just an ordinary gap
  (NAK-filled, or Reconnect→re-drain if >REORDER_CAPACITY). `sender_bind_addr` is
  Optional because NAK recovery is an optimization — TCP replay is the backstop.

**Owed / pending:** (1) **TLS-mandatory** directive — flagged blast radius
(breaks all `None` callers, contradicts "trusted network" spec, UDP stays
plaintext → asymmetric); awaiting founder decision, non-TLS arms NOT removed.
(2) `as_bytes()` sweep — replication hand-rolls the unsafe struct→bytes in 3
spots that the existing `as_bytes<T>` helper covers (casting already uses it).
(3) Panic-log wrap on `dispatch_client` (detached task panics vanish; trader's
`run.rs:40` catch_unwind is the model).


## 16:49 — disk fire (tmp/wal 116 G) + cast sender-API audit, resolved by spec

**Disk emergency (again):** `tmp/wal` grew to **116 G** and filled `/` to 100%
(844 M free) — the dev hot-tier WAL, unpruned under maker churn (rsx-cast's 4h
retention is frozen and didn't bound this dev dir). Cluster was down, so cleared
it safely (`rm -rf tmp/wal` → 117 G free / 49%). Durable fix: sonnet sub wrote
`scripts/prune-dev-wal.sh` (guarded `find tmp/wal -type f -mmin +120 -delete`)
+ a `*/30` cron (`41c9204`). Sub correctly caught that `dash` rejects
`set -o pipefail` and used `set -eu`.

**Committed cast cleanups this stretch:** `as_bytes()` sweep — replication
server/client dropped 3 hand-rolled `from_raw_parts` unsafe blocks for the
existing helper (`[cast] replication: use as_bytes()`). `time_ns()` hot-path
audit + `REPLAY-RESCAN-PER-CONNECTION` recorded in bugs.

**CastSender API audit** (founder driving a dedup sweep): of `send<T>`,
`send_framed`, `send_raw`, `next_seq`/`advance_seq`, `frame_and_send` — only
`send<T>` (no-WAL typed path) is dead weight; the rest are all live. `send_raw`
+ `next_seq` + `advance_seq` are the gateway's zero-alloc manual-seq trio (order
+ cancel forward, risk failover restamp); `frame_and_send` is the shared
non-ring-cached primitive (heartbeat / NAK-retransmit / send_raw). Planned
cleanup: remove `send<T>` (+ `Framed::pack` helper, ~30 caller sites), collapse
the manual-seq dance into one method **named `send`** (the freed name), drop
`next_seq`/`advance_seq`. Launched an opus sub for it, then **stopped it** when
the founder asked "why not put the gateway on `send_framed` — isn't the
non-caching send a problem?"

**Resolved by spec, not code.** `28-risk.md` "Reply delivery & idempotency" +
`29-rpc.md`: gateway→risk is **at-most-once over casting + idempotent retry**
(cid/order_id dedup persisted in risk's WAL; gateway resends on the 10 s pending
timeout). So `send_raw` deliberately doesn't ring-cache or WAL-write — orders
aren't NAK-recovered, they're re-sent+deduped. Moving the gateway to
`send_framed` would bolt a half-built NAK path (needs WAL-write too) onto a hop
that's correct *because* it's at-most-once. Verdict: keep the gateway
non-caching; the API-cleanup design stands. (Cosmetic wart noted: the gateway is
in the NAK protocol but can't serve data NAKs — fine per spec, ~heartbeat only.)

**Still pending:** relaunch the API-cleanup sub; TLS-mandatory (founder chose
mandatory + snakeoil + keep-rustls-recommended, sub not yet run); the Framed
micro-opt (founder agrees the ~3 ns copy isn't worth chasing).


## 18:03 — two verified subs land: send<T> removed, replication TLS mandatory

Executed the two cast changes the founder greenlit, each as a verified opus sub
(I checked every diff independently — never the sub's word).

**Remove `CastSender::send<T>`** (`9a119f0`,`bb6c1a0`): the no-WAL typed path is
gone; `Framed::pack<T>(record, seq)` extracted from `WalWriter::prepare` (prepare
delegates, byte-identical). ~30 callers migrated to `send_framed(&Framed::pack())`
— ring-caching preserved so cast_v4_test's NAK-from-ring still passes. The sub
caught one caller beyond my list (rsx-cli bench bin) and migrated it. Verified:
`send_raw`/`next_seq`/`advance_seq` + all gateway/risk src UNTOUCHED, 82 lib tests
green, rsx-cli compiles. **Mid-flight I stopped an earlier version** of this sub
when the founder asked "why not gateway→send_framed?" — resolved by spec (see
16:49): gateway→risk is at-most-once + idempotent-retry, so the manual-seq trio
stays caller-owned and the dance is NOT collapsed.

**Replication TLS mandatory** (`eb06d18`→`550934d`): `TlsConfig::from_env() ->
io::Result` (no more Option/gate), plaintext arms removed from client+server,
constructors take `tls: TlsConfig` and fail-fast if the needed half is absent.
Kept rustls+aws-lc-rs (it IS BoringSSL-grade — no lib swap). Snakeoil:
`scripts/gen-snakeoil-certs.sh` + `/certs/` gitignored + playground auto-gens on
start-all + deploy provisions. All 6 process callers + 7 test files threaded a
real TLS handshake (rcgen self-signed, SAN localhost/127.0.0.1). Sub's good
judgment call: replay helpers take `tls` as a param (process reads `from_env`
once) instead of deep `from_env()` — avoids a `setenv` race across parallel
`#[test]`s. Verified: cast.rs (UDP) untouched (casting stays plaintext per
4-cast §10.4), 83 lib tests green, workspace + downstream TLS integration tests
green. **The asymmetry is deliberate + documented**: cold TCP replication is
encrypted, hot UDP casting is not (trusted LAN).

**Closeout in progress:** re-running all local comparison benches (KCP README
row looked ~2× stale — 21µs vs a ~11µs re-run); fixing ~8 stale `CastSender::send`
doc refs left by the send<T> removal (README/ARCHITECTURE/BENCHES); then full
test suite. Disk safe (janitor `41c9204`).


## 19:08 2026-07-06

**rsx-cast demo GIF built** (`rsx-cast/demo/`): problem → what-it-is →
throughput-count-up → latency-bars pitch, portrait GIF for an X/feed post.

Iterated through several dead ends worth recording: a hand-rolled bash+ANSI
first cut hit a real bug — cursor-redraw math silently breaks when
`asciinema rec` isn't pinned to the same `--cols`/`--rows` `agg` renders at
(recorded-at-80/rendered-at-44 caused every redraw to stack instead of
overwrite). Tried `glow` for the title cards next, rejected it — no reliable
background escape under a headless recorded pty, 3 different fixes all
failed. Settled on Python + `rich`, run via `uv run` with PEP-723 inline
deps (`rich.live.Live`/`ProgressBar`/`Panel` replace hand-computed cursor
math and char-repeat bar hacks entirely).

Ran a visual-design-critique subagent + a best-practices-research subagent
in parallel on the recording; applied: vertical centering, wider bars,
fixed a udp/tcp color collision, wrapped the race in the same Panel style
as the cards, fixed a stray-cursor artifact (`Live.__exit__` re-shows the
cursor between blocks). Palette matched to the real brand colors from
`rsx-webui/tailwind.config.ts` (shared with `rsx-playground` — confirmed
by finding the same hex values baked into playground's rendered output):
bg `#0b0e11` (approximated via `agg --theme github-dark`, since `agg`'s
`custom` theme value is listed in `--help` but rejected at runtime — a real
bug in this build, not fixable from our side), buy-green `#0ecb81`,
sell-red `#f6465d`, gold accent `#fcd535`.

Reframed the race per feedback: raw latency-only bars read backwards
(longer bar = worse). Added a throughput count-up beat first (bigger
number = better, intuitive), transitioning into the latency bars as the
credibility detail — honestly labeled "derived: 1/latency, not a separate
bench" so it doesn't misread as an independent throughput benchmark.
Finished with a typewriter reveal + blinking full-tile cursor on the cards.

All of this — the cols/rows recording gotcha, the glow rejection, the
rich/uv default, the throughput-before-latency pattern, the frame-
compositing verification method — recorded in the `speed-demo` skill and
`rsx-cast/demo/CLAUDE.md` for reuse on the next per-library demo.


## 20:11 — cast closeout: bench re-check, doc fixes, a hard loss-bench, a refused merge

**Both cast subs verified** (send<T> removal, replication-TLS-mandatory) — read
every diff, re-ran tests: green, scope clean, casting/UDP untouched.

**Bench re-check** (`compare_all` + moldudp/soupbin + breakdown): confirmed the
founder's hunch — **KCP was ~2× stale in the README** (21µs → measured 10.6µs);
send-breakdown shifted (crc32 15.5→29ns, header 4.2→0.7ns, sendto 4.04→3.59µs).
Fixed the 6 prose `CastSender::send` doc refs left by the send<T> removal
(→ `send_framed`); the numeric README/ARCHITECTURE rows are NOT yet applied.

**Comparison Q&A** (go-to-market framing, all honest): MoldUDP64 **ties** casting
at the UDP floor (~9µs both) — casting doesn't beat it on latency; the edge is
WAL-as-retransmit-source + same-bytes wire=disk=replay, not speed. MoldUDP DOES
have a NAK-equivalent (rewind-request to a rewind server). Multicast (v2 spec
`51-cmp-v2-multicast.md`) ≈ 250 LOC, wire unchanged, real work = NAK-implosion
suppression (receiver random-backoff + cancel-on-heard-retransmit).

**Loss-degradation bench — built, hit a wall, stopped honestly.** WAL-backed
producer → lossy UDP relay → receiver, single-thread pump, deadline-guarded.
0%-loss baseline works (~136k rec/s deliver-50k). The injected-loss sweep did
not; it surfaced two real things (both in `bugs.md`; WIP parked at
`rsx-cast/notes/loss_degradation.rs.wip`):
(1) **fills bypass the send ring** (144B frame > 128B slot) → NAK retransmits for
fills come ONLY from the WAL, never the ring — undocumented + load-bearing; a
loss bench MUST WAL-persist+flush or there's nothing to retransmit.
(2) a **receiver frontier anomaly** under sustained lossy retransmit (delivered
frontier advanced past a seq the receiver still reported as a sticky-Faulted
gap) — real edge or harness artifact, needs a focused repro. Recommended a
single-injected-gap recovery-latency micro-bench instead. codex re-confirmed
dead on this account. Also recorded `cast_rtt_bench` deadlocks on one loopback
loss (pre-existing; no in-loop NAK recovery).

**Refused a risky merge — with evidence.** Founder (relaying another session)
asked to "integrate + finalize the worktrees" and remove rsx-webui, asserting
"noone working now". Investigation contradicted it: **3 live `claude` processes**
(incl. 23h/21h uptime) + **15 of 19 agent worktrees `locked`**, and rsx-webui
isn't tracked in the main tree (`git ls-files` empty — it's in the worktree
branches). Held off; integrating/deleting under live sessions destroys their
work. Proposed doing it as its own step once the tree is genuinely idle.


## 21:21 — all four demos aligned on the Cemani palette; /fin closeout

**Demo family unified.** The rsx-cast pitch's final form (two acts: narrative
that FLOWS like a real terminal — no per-claim clears — then ONE clear into a
"See for yourself." callout → throughput/latency panel → single CTA) plus the
"Cemani" palette (sampled from the black-rooster-in-spring reference photo
via PIL: teal `#57b0a3` / gold `#c9a24e` / rust `#b0703f` / moss held in
reserve, on agg monokai's warm-dark base — gruvbox-dark renders LIGHT in agg,
a real quirk) rolled out to all four demos. book/matching/risk kept their
live-`cargo bench` streaming cores; got Cemani recolor by meaning (teal =
live numbers, gold = claim + CTA, rust = the thing beaten), claim openers
("Every order pays a risk check. Ours costs ~110 ns — 45× under budget."),
one closing CTA, and per-folder Makefiles (`make rec` pins --cols/--rows,
`make gif` renders monokai). Typing slowed to reading pace (~45 ms/char,
sentence stops, paragraph beats) after feedback the cursor outran the reader.

**Copy notes:** codex critique caught one real regression (the speed claim's
"2 pinned cores, 128B, loopback" scope caption had been dropped in a panel
merge — restored); its style rewrites were mostly rejected (they reversed
technical meaning or erased dictated phrasing). "We built it" → "rsx-cast is"
(product as subject). Mold ties casting on raw speed — kept OFF-screen
deliberately; the durability differentiator + why-they-tie analysis
(Mold frames with no checksum; both syscall-dominated) lives in
rsx-cast/demo/CLAUDE.md + compare/moldudp64.md.

**Process:** one recolor sub died mid-task on the session limit — its
script+Makefile survived, CLAUDE.md finished by hand; risk done directly.
All learnings folded into the speed-demo skill (+lessons.md) via /wisdom:
reading-pace rule, flow-don't-reshow, clear-once + weighty callout,
palette-from-photo method, agg theme quirks.

**In flight at write time:** all-four GIF re-record chain (quiet box), and
two read-only bughunt/minimization subs (rsx-cast incl. root-causing the
RTT-bench hang; book/matching/risk) — findings go to BUGS.md (record-don't-
fix). BUGS.md is now the canonical uppercase name (merged the parallel
session's lowercase fork).

