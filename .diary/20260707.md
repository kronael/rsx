
## 06:41 2026-07-07

### Removed never-shipped/superseded specs

Dropped `specs/3/` (Phase 3 Future/archival: future, implementation,
orderbookv2 "not planned", archive draft — never shipped), plus
`specs/2/46-trade-ui.md` (self-marked SUPERSEDED → rsx-tui, the removed
React webui) and `specs/2/14-management-dashboard.md` (split into
dashboards 7/12/23/27). Fixed dangling refs in `specs/index.md`,
`specs/2/index.md`, `54-tui-access.md`. Kept `specs/1/` (shipped
ship-logs — historical, not superseded). Commits ee0048f, 2c3c614.

### Landed the two parked cast recovery benches

**Outage recovery** (`outage_recovery.rs`): big-gap recovery over the TCP
replication cold path. The parked WIP hung for two reasons, both harness
bugs: (1) the `run_once` callback never returned `false` on
`RECORD_CAUGHT_UP`, so it tailed the live replication feed forever (the
server tails live after catch-up by design); (2) the dark window was a
wall-clock firehose (~100k rec/s) → millions of records → the server's
per-connection WAL rescan never finished. Fix: stop on CAUGHT_UP + pace
the gap to `outage_secs × RATE`. Result: 7.5k-record gap ~260–790ms
(drifts up across cycles as the active WAL grows — the REPLAY-RESCAN cost,
now confirmed), 52k-record gap ~1s. Commit 25c9aa2.

**Loss tolerance** (`loss_degradation.rs`): how much sustained loss the
live NAK path survives. Key insight — the ceiling is set by the retry
COUNT (`max_nak_retries`), since a gap dies only after that many
*consecutive* retransmit losses (~loss^retries), so retransmits must be
allowed to drop too. Two traps found and fixed: (a) 144B fills bypass the
send ring → O(N) WAL-scan retransmits, so a short debounce out-runs its
own retransmit and faults spuriously → switched to 64B ring-served
records; (b) even then, debounce < retransmit-round-trip-under-jitter
caused non-monotonic spurious faults (1% worse than 5%) → bumped debounce
to 5ms (jitter-immune; debounce sets speed, not the ceiling). Result
(default 8-retry budget, N=10k): reliable through **30% loss** (5/5,
throughput degrading ~75×), collapse at 40%. Far beyond the "≤0.01%"
naive figure. Commit 50c78f9. Report:
`reports/20260707_cast-loss-recovery.md`.

### Simplified run_once & friends

Assessed: the 4-method layering (run=reconnect / run_once=single-shot /
connect_and_stream=endpoint-failover / handle_stream=wire) is justified —
both public entries are load-bearing (recorder uses `run`, everyone else
`run_once`), no dead code. The one real nit was the `Ok(true)/Ok(false)`
return contract (implicit flag). Replaced with a 2-variant
`StreamEnd { Eof, Stopped }` so `run`'s reconnect match self-documents.
Internal only; public signatures unchanged. Commit 126491c.

### Verification + a toolchain-drift finding

`make test` fully green (126 test groups, 0 failures). Docs refreshed
(README loss claim now cites the measured 30%; BENCHES.md lists the three
recovery benches). BUGS.md: the CAST-RECOVERY finding is resolved (hang
was harness, not cast); REPLAY-RESCAN confirmed by the outage drift.

**Watch-out:** the repo has NO rustfmt.toml and isn't `cargo fmt`-managed
— every crate reflows under default rustfmt (cast.rs ≤88 chars, rsx-risk
≤194). It's hand-formatted to CLAUDE.md's "80, max 120". Don't run
`cargo fmt` here; `rustfmt <one-file>` or match surrounding style by
hand. My error this session: ran `cargo fmt -p rsx-cast` to tidy a bench,
which churned 18 files; reverted.

**Open:** `make lint` (`clippy --workspace -D warnings`) is red workspace-
wide (rsx-cast 2, rsx-risk 8, rsx-recorder 1, …) from newer-clippy lint
drift (`manual_is_multiple_of`, `needless_borrow`, `int_plus_one`). Not
introduced this session; a separate workspace-wide clippy pass, and it'd
touch frozen hot-path code — left for a deliberate decision.

## 09:03 — cast demo final polish: founder copy passes + packet-count race

Overnight/morning stretch iterating the cast pitch under live founder
direction, each round recorded + composite-verified + committed.

**Copy (several founder rounds):** word-level meaning colors (speed teal /
brand-durability gold / cost rust — words, not sentences); "Multicast, not
pub/sub" (it isn't peer-to-peer); costs beat now "Total reliability costs
latency: a broker adds hops, and fsync on every byte costs milliseconds —
a hundred times the network hop. If your servers never all die at once,
don't pay for it." Two dishonesty traps caught: "a thousand times" was
wrong by 10× (1 ms / 9.3 µs ≈ 107×), and listing "hand-rolled UDP" as a
cost dissed the thing rsx-cast is — both guarded in demo/CLAUDE.md. Added
the deployment beat: "One library, one binary: the WAL and the replay
server can run in your process. Nothing to deploy."

**The race (replacing the /s count-up):** a climbing *rate* is fake — the
panel now counts packets moved in elapsed time (1/latency × elapsed,
linear), live "packets moved in N s" timer, 10 s race + 15 s hold, row
colors saturating DIM→hue as the gap opens (quantized ramp; GIF ~695 KB).
Layout halved to 12 rows at font-size 28 (775×510, hi-DPI glyphs). Panel
caption dropped — derivation/citation in demo/CLAUDE.md only.

**Docs:** durability model written down (rsx-cast README Guarantees +
GUARANTEES.md principle): replicas + continuously-spinning sync; loss
needs producer disk AND every replica dead inside one ≤10 ms flush window,
everything else replays to zero loss. "Nothing to deploy" README bullet
(ReplicationService is a library type). Demo CLAUDE.mds cut to what-is-now
(cast 167→94 lines; book's bug-triage history stripped).

**Audits:** rsx-cast read-only bughunt — no library bugs (send<T>→
send_framed verified byte-identical on the NAK ring); RTT-bench hang
root-caused as a bench-side unbounded spin-wait deadlock on one dropped
loopback packet — duplicate of this morning's parallel-session finding;
BUGS.md corrected (my send<T>-regression hypothesis was wrong).
book/matching/risk audit sub died twice on session limits — deferred.
"Is the QUIC 37 µs rigged?" — no: handshake excluded, persistent stream,
inside picoquic's published 20-400 µs loopback band; per-packet AEAD +
userspace protocol machinery is the gap (compare/quinn.md covers it).

**Skills:** speed-demo gained reading-pace typing (~45 ms/char),
claim-not-label openers + product-as-subject, flow-don't-reshow,
clear-once + weighty callout, palette-from-photo method, agg theme quirks
(gruvbox-dark renders LIGHT; --theme custom broken in 1.9.0). sonnet/opus
skill drift synced ~/.claude ← app/tools.

