---
summary: |
  RSX perp exchange. Solo work. Demo + measured benchmarks.
  Open items:
  - [resolved 2026-05-29: SEQ-1 fixed, FAULTs→0, e2e 551/500 ok 0 fail]
  - Roundtrip MEASURED: ME in-process floor ~210ns; system RT transport-bound
    (4 casting hops @ ~3.8µs ≈ 15µs internal; compute <2%). Live WS-probe
    p50=11.6ms is Python aiohttp+WS framing, not internal. Pure-path harness TODO.
  - Spec merge plan DEFERRED (oracle flaky: ChatGPT acct rejects all codex models)
  - Risk return path RESPEC'd → ME→GW-direct (28-risk.md partial); NOT impl yet
  - [resolved 2026-05-29: orphan-freeze fixed via confirm_freeze on ME OrderAccepted]
  - [resolved 2026-05-29: input SPSC rings dropped — clone-less hot path, fills-first preserved]
  - Deep-book bench done: match ~52ns flat at 100k/1m/10m resting
  - Risk return path RESPEC'd → ME→GW-direct (28-risk.md partial); NOT impl yet
  - Legacy-PG orphan-freeze rows: one-time reconcile on recovery (minor, open)
  - Oracle dead on this account (codex rejects all models); use manual review
  - Core layout: 0=OS+mark+recorder, 1=gw, 2=risk, 3=ME, 4=md, 5=spare
---

## 06:50

Systematic UDP-loss investigation (user: "udp has to work without packet
loss"). Proved with kernel counters: RcvbufErrors/InErrors/SndbufErrors +0,
OutDatagrams==InDatagrams under load. Loopback UDP is lossless — "packet
loss" was a misdiagnosis. Two distinct diseases below.

## 06:52

Disease 1 (FIXED): rsx-mark busy-spun 97% CPU but was unpinned. CFS floated
it onto gateway's pinned core 1 → gateway starved (0.4%) → couldn't drain UDP
socket → 16MB backlog → kernel RcvbufErrors (real drops). Fix: mark ergonomic
(sleep 250µs, 97%→4%), unpinned like other off-path svcs. Documented 6-core
layout in README + CLAUDE.md. Drops → 0. Also fixed marketdata FAULTED-panic
crash loop (RSX_MD_REPLAY_ADDR unset, stream_id=1 vs ME stream 10) and
WAL-replay retry on NOT_AVAILABLE (10ms flush window).

## 06:55

Disease 2 (DIAGNOSED, queued bugs.md SEQ-1): residual ~30 FAULT/min with zero
packet loss = casting seq-space collision. ME stamps BBO from CastSender's own
next_seq (send()) but WAL'd records from WAL seq (send_framed); BBO not WAL'd
→ counters desync → wire seq regresses → "sender reset detected"/FAULTED. Plus
filtered-fan-out holes: OrderDone cmp-only, OrderFailed neither, risk forwards
subset to gw, risk margin-reject sent with seq=0 (gateway drops seq==0). Fix
recommended: full fan-out on single WAL seq. Needs sign-off — touches ME/risk/
gateway/WAL/replay.

## 07:30

SEQ-1 FIXED + verified. Three-part fix: (1) ME publish_events fans OrderDone/
OrderFailed/BBO to both streams on the WAL seq (BBO was on CastSender's own
counter — the desync root cause). (2) ME main loop fans OrderAccepted (was
WAL-only) + duplicate-reject (was cmp-only) to both — OrderAccepted per resting
order was the ~2/sec residual. (3) risk forward_to_gw renumbers the gateway
stream with gw_sender's own contiguous seq (fixes seq=0 margin-reject + ME-seq
holes; gateway never replays from risk so safe). Result: FAULTs 0/15s on all
streams; e2e probe 5→551 samples, 0 fail. Exchange works end-to-end.

## 15:40

rsx-cast marked FROZEN (rsx-cast/CLAUDE.md): bugfixes only, anything else
needs founder sign-off. Rationale: it's the extractable stable artifact; churn
ripples into every process. SEQ-1 fix correctly lived in callers (matching/
risk), not in cast — that's the pattern.

## 15:55

Spec consistency pass (post-SEQ-1). Reconciled 6 specs: 6-consistency (rewrote
Event-routing table — transport now fans EVERY record to risk+md on one WAL
seq; split transport vs logical-consumption; fixed drain_events pseudocode),
17-matching (BBO no longer "not WAL-framed"), 28-risk (mark IS wired),
15-mark/39-testing-mark/25-process (mark ergonomic, not busy-spin). Dropped
dangling 50-wedge.md index entry (file deleted 2026-05-24).

## 16:05

Measured matching + risk roundtrip. ME in-process floor ~210ns p50 (dedup+
match+WAL-append, no fsync). Risk: pretrade 110ns, apply_fill 3.5ns, BBO→index
5ns, validate+forward 114ns. Casting loopback RTT (A→B→A) 7.6µs. System RT is
transport-bound: 4 casting hops @ ~3.8µs ≈ 15µs internal floor; compute <2%.
Matching is NOT the bottleneck — UDP hops are (DPDK/AF_XDP is the lever).

## 16:08

Bug found+fixed: me_process_order_full_path bench panicked "wal buffer full,
backpressure" in criterion warmup — appended to real WAL every iter, never
drained, produced no perf number. Fix: reset_write_buf() (no fsync) every 1024
iters; fsync is batched off-path, must not be in the timed loop.

## 16:30

Expanded ~/.claude no_blocking_terminal.py hook (terminal-control): now denies
poll loops (while/until+sleep), watch, tail -f, long foreground sleeps (≥3s);
always allows run_in_background + short seq sleeps. 12-case self-test passes,
synced to tools template. Oracle flaky on this account (ChatGPT acct rejects
all codex models intermittently) → spec merge plan deferred.

## 17:30

CORRECTION: the terminal hook was the WRONG fix — user wanted Claude Code's
TUI to stop grabbing PgUp/PgDn (native scrollback). Rolled back the hook +
template sync entirely. Real fix: CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1 in
~/.claude/settings.json env (classic renderer, no key/mouse capture). Also in
template settings. Restart to apply.

## 18:00

Added deep-book bench (rsx-book/benches/deep_book_bench.rs): seeds 100k/1m/10m
resting orders, measures insert + match at depth. Deterministic xorshift64*
PRNG (no rand dep). Prices Student-t(3) (heavy tails, clamped ±45%), sizes
scale with distance from mid, takers ~exp. Net-neutral ops keep depth ~N.
RESULT: match ~52ns FLAT at 100k/1m/10m (depth-independent, O(consumed) not
O(resting)); insert ~190-215ns flat. OrderSlot=128B, slab u32 → RAM-bound
(10m≈1.3GB). Proves matching scales to 10M resting.

## 18:30

Respec'd risk return path → ME→GW-direct (28-risk.md, status shipped→partial).
Input leg (reserve, GW→Risk→ME) stays sync; confirmation ME→GW direct (gw a
direct ME consumer, 4 hops→3, ~7.6µs saved); settle ME→Risk async. Safe:
worst-case freeze + fills authoritative + WAL-replay recovery. Cost: margin
eventually-consistent (no read-your-writes); solvency never at risk. Hybrid
escape hatch documented. Caller-level only — rsx-cast stays frozen.

## 18:45

Failure-mode analysis of ME→GW-direct. Key: pre-trade REJECT originates at
risk (ME never sees it) → needs a residual thin Risk→GW path; can't delete
Risk→GW entirely. Freeze rebuilt from ME WAL OrderAccepted on replay
(replay.rs:326) → most crashes self-heal. OPEN BUG: orphan freeze — risk
freezes pre-send + write-behinds to PG, crashes before ME accepts → PG
snapshot keeps a phantom freeze the WAL never confirms/releases. Fix: WAL
OrderAccepted = sole freeze authority; reconcile PG against it on recovery.
Not yet logged in bugs.md or spec'd.

## 19:30

ORPHAN-FREEZE FIXED (sub, verified). Option B: process_order keeps the
in-memory freeze (pre-trade gate) but no longer write-behinds to PG
pre-send; new shard::confirm_freeze writes the durable FrozenInsert only
on ME's RECORD_ORDER_ACCEPTED. PG can't hold a freeze the WAL never
confirmed. Verified: checked diff, confirmed ME emits OrderAccepted for
every order (main.rs:628), re-ran tests = 268 pass. 8 new tests
orphan_freeze_test.rs (FM6/11/13a/14). bugs.md marked fixed; legacy-PG
one-time reconcile noted as minor follow-up.

## 20:30

CLONE-LESS HOT PATH done (sub A died on process exit, finished by me + sub
C, verified). Removed the intra-tile input SPSC rings (order/fill/bbo/mark)
— recv loop and shard are one tile, so they were a redundant per-msg copy.
Recv handlers now call shard.process_order/process_fill/stash_bbo/
update_mark directly: hot path = decode_payload (1 copy off net buf) →
process. run_once→tick (liquidation/tip/stashed-bbo/backpressure). Kept
response/accepted output rings + persist (cross-thread). Reordered loop so
me_receiver (fills) drains BEFORE gw_receiver (orders) — preserves
fills-before-orders capital efficiency. This closes the "drop SPSC rings"
open item. Commit 3702b94 (also carried the P1 tests — Sub B had pre-staged
them; one commit not two, all verified 268/85/60 pass).

## 20:35

P1 failure-mode regression tests added (sub B, verified). seq_gap_replay
(SEQ-1 guard: gap→FAULTED→TCP replay→reset→resume), replication_federation
(NOT_AVAILABLE endpoint fallthrough, was 0 coverage), replay_fifo (intra-
level time priority across replay — sorted book_state() couldn't catch it).
New rsx-cast/tests/ dir (vs public API; frozen src untouched).

## 20:40

Oracle UNUSABLE all session (codex rejects every model on this ChatGPT
account: gpt-5.4/5.3/5.1/5.1-codex/5 all "not supported"). Test-usefulness
review of orphan_freeze_test.rs deferred to manual.
