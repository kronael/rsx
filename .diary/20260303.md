---
summary: |
  RSX perp exchange. Solo work. Publishing prep + playground walkthrough.
  - BLOG.md written; crate-level doc comments added to all 8 lib crates
  - test counts were wrong across all docs — corrected to 785 Rust / 1034 Python / 398 Playwright
  - npm/npx replaced with bun/bunx across all configs, Makefile, docs
  - Playwright trade tests had stale selectors + WS-dependent paths — fixed
---

## 18:37

Added `[profile.bench-fast]` without LTO to `Cargo.toml` (374d350). Bench iteration cycle was slow; non-LTO profile cuts compile time without affecting correctness measurements.

## 18:44

Publishing prep pass: crate-level doc comments added to all 8 `lib.rs` files. `FEATURES.md`, `PROGRESS.md`, `README.md`, `TODO.md` updated for external readability. Added `rsx-maker/README.md` (+55 LOC). Specs still reference `specs/v1/` — noted (0a16926).

## 18:45

`BLOG.md` written from scratch (+198 LOC, a29410c). Development narrative with bench numbers — intended as publishable content, not internal docs.

## 18:49

Added interactive walkthrough as playground landing page (`rsx-playground/pages.py` +530 LOC, 6a371af). Routed from `server.py`. Gives reviewers a guided entry point without needing to know the system layout.

## 18:54

Discovery: test counts were inconsistent across `BLOG.md`, `PROGRESS.md`, `README.md`, `TESTING.md`, `FEATURES.md`. Corrected to 785 Rust / 1034 Python / 398 Playwright everywhere (927ddfb). Added `rsx-playground/docs/tabs.md`.

## 18:56

Fixed `ARCHITECTURE.md` crate count; added `rsx-webui/README.md` (+53 LOC, 4830167).

## 19:30

Replaced all `npm`/`npx` references with `bun`/`bunx` across Makefile, test scripts, playwright configs, package.json files, and 4 spec docs (d2476e6). 16 files, no logic changes — toolchain alignment with existing `bun.lock`.

## 19:57

Fixed Playwright trade tests (`deb1bce`): stale selectors updated, BBO fallback added, WS-dependent assertions migrated to REST equivalents. `bun.lock` added under `rsx-playground/tests/`. Tests were failing due to selector churn from earlier UI changes.
