---
summary: |
  RSX perp exchange. Solo work. JWT auth hardening + frozen_margin correctness fix.
  - frozen_margin leak on restart: per-order PG table replaces aggregate field
  - gateway requires RSX_GW_JWT_SECRET, adds user_id claim, aud/iss checks
  - x-user-id path gated behind RSX_GW_ALLOW_INSECURE_USER_ID=1
  - playground subprocess lifecycle hardened, admin token added
  - CLAUDE.md updated: subprojects, playground entrypoint, release-gate workflow
---

## 07:06

api_risk_test hardened to accept live-engine responses (40e1550). pages.py relative URL fix for gw-mode-badge htmx (687e469).

## 07:48

play_latency: socket hang-ups tolerated in recent-orders-capped test (f5953e0). api_proxy_test: upstream-up guards added for two skip conditions (bf97645, f56a44d).

## 17:02

Discovery + fix: frozen_margin leak on restart (9ca6f10). Account::frozen_margin was an aggregate persisted alongside in-memory frozen_orders; WAL replay on restart re-applied ACCEPTED records doubling the reservation. Collapsed to one representation: new frozen_orders PG table (migration 004), aggregate derived on demand. 21 files changed.

## 19:39

JWT hardening batch (0d87e7a): gateway requires RSX_GW_JWT_SECRET, adds user_id claim, aud="rsx-gateway", iss="rsx-auth". x-user-id header path gated behind insecure flag. conftest + test clients updated to use Authorization headers.

Playground subprocess lifecycle hardened, admin token added (ee98c18). 517→+429 LOC in server.py; gate-1/2/3 green.

scripts/regen-progress delegates to publish-progress (530a09e). CLAUDE.md updated with subprojects, playground entrypoint, release-gate workflow (53721ae).

## 20:00

start script sets RSX_GW_ALLOW_INSECURE_USER_ID=1 for dev gateway (7e444dc). Caught during clean-boot attempt post-0d87e7a.

## 20:03

Dead code sweep: -220 LOC across types/risk/dxs/marketdata (b16e086). rsx-risk::replica, rsx-types macros removed.

## 20:25

Gateway x-user-id path deleted (83e06f5); dev tools now mint real JWTs. -149 LOC net across gateway, playground, start script.
