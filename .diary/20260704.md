
## 06:21 2026-07-04


## 07:23 2026-07-04

### Session: IOC + ME-FAULTED fixed, sonnet bug-hunt, disk crisis

**Context:** Founder redirect — dispatch IOC + ME-FAULTED fixes to a background
sub while resolving gateway latency on the front. The dispatch went sideways
(disk), so both fixes ended up done inline, correctly.

**Disk crisis (recurred):** the recorder ARCHIVE (RECORDER-ARCHIVE-UNBOUNDED)
refilled the disk and ENOSPC'd the Bash-capture tmpfs mid-session — it had
already killed the dispatched sub (which cleared the 59 GB archive itself before
dying). Stopped the recorder, cleared the archive, kept it down. This bug bit
twice; it needs a real retention cap.

**IOC-NOT-HONORED — fixed (7a8ccbd), live-verified.** The killed sub concluded
"tif propagation lost, doesn't reproduce" — WRONG on both counts. Reproduced it
live, then ran a runtime trace (temporary DBGTIF/DBGIOC logs, since reverted):
`tif=1` reaches `risk_in` AND `me_in` intact and the engine's IOC residual
branch fires (correct cancel). Real cause: a code-space collision — the ME wrote
`OrderDoneRecord.final_status = REASON_CANCELLED(1)`, but per 49-webproto.md
`final_status` is a webproto U-frame status where 1=RESTING, 2=CANCELLED. So a
cancelled IOC was reported to the client as "resting". `REASON_FILLED==0==FILLED`
masked it for fills. Fix: translate reason→webproto status in
`wal_integration.rs` (`done_final_status`); gateway mapping was already
spec-correct. Regression test + live check (IOC→cancelled, fills/resting
unchanged). Lesson: subagent success reports are not evidence — the empirical
repro flipped the whole diagnosis.

**ME-FAULTED — resolved (22aa9c8).** Implemented the founder fault model: the
risk→ME order stream is drop-safe (client re-sends dropped pre-ack orders, WAL
dedup = exactly-once; ME re-sequences on output so an inbound gap is never an
output gap). On FAULTED the ME now skips the gap (counts `gauges.drops`, WARNs
the range, `reset_after_replay(gap_end)`) instead of panic-on-unset-
`RSX_ME_REPLICATION_ADDR`. Removed the dead consumer-replay path (`replay.rs`,
−657 lines) + its test. Kept the ME WAL server (risk-side fill recovery) and
cold-start local-WAL replay.

**Sonnet bug-hunt (01473e4):** 4 read-only hunters over the tree, all 8 findings
re-verified genuine (zero false positives), logged to bugs.md — NOT fixed (fixes
being handled in other sessions). Highlights: COMPRESSION-ZONE-TICK-UNIT (zones
mis-sized for tick_size≠1, latent since demo=PENGU tick=1), WAL-ROTATE-PREWRITE-
MISLABEL (boundary records unreachable for NAK/replay), CLI-PTR-READ-UNALIGNED-UB.

**Deferred:** gateway-latency clean stage-trace — box under load ~4-5 from
concurrent sessions; the GATEWAY-LATENCY bug already requires a QUIET box.
Contaminated `bench-baseline.json` reverted (didn't seal a bad number).

**Final state:** cluster 5/6 (recorder intentionally down); ME+risk on the
committed binaries; IOC/resting/fill lifecycle all verified live.

## 09:13 2026-07-04

### Session 2: complexity audit, fin-hook bug, gateway-latency focus

**Complexity audit (`FINDINGS.md`/`VERDICTS.md`, committed).** 4 read-only sonnet
subs pinpointed 36 complexity smells across all crates; 1 opus sub adjudicated
each: 25 CONFIRM+SIMPLIFY, 3 KEEP (load-bearing — A7 loop-termination, B4 the
per-user secondary index is the O(all)→O(per-user) perf fix so centralize don't
remove, B6 gateway replay is documented accepted-loss), 8 rsx-cast record-only
(frozen), 0 rejected. Pure wins queued: delete dead `LoadGauges::Default` (D3),
dead gateway `ws_read_frame` (B8), `update_order_index_local` copy (A3); plus
correctness D1 (unnecessary `unsafe` in marketdata replay — `run_once` has no
'static bound) and D2 (cli `ptr::read` UB → safe `decode_payload`, closes the
logged CLI-PTR-READ-UNALIGNED-UB). Founder deferred applying — recorded only.

**rsx-cast freeze codified (`3ec96c2`).** Root project CLAUDE.md now states
rsx-cast is finalized — bugfixes only, no edits without explicit sign-off.

**`/fin` hook bug fixed (in `~/app/tools` + `~/.claude`).** The stop hook fired
every turn regardless of completion: `fin_recent()` substring-matched the last
60 transcript lines for the prose "finish mode" — but the hook's OWN nag text
contains that phrase, so each firing re-seeded the next. Self-perpetuating,
stateless (no stuck flag). Fix: match only the structural invocation marker
(the skill JSON shape / command-name tag), never free prose. Verified silent
once the marker scrolls out of the window.

**Gateway-latency focus — measurement is broken, being fixed.** Confirmed the
POLL_ADD readiness fix landed in a PRIOR session (946b71d), not this one, and
it's still UNVERIFIED. Traced why: (a) per-user rate limiter (cap 10) rejects
rapid probes before `gateway_in` → sparse capture; (b) the return leg
(`risk_out`/`gateway_out`) emits ZERO samples because a taker's completion comes
back via `route_order_done` (status=filled), while `gateway_out` lives only in
`route_fill`. So the `me_out→gateway_out` egress delta — the leg POLL_ADD
targeted — is unmeasurable. Dispatched an opus sub to trace+minimally fix the
instrumentation (emit egress samples on the path the fill actually takes) and
re-measure. Logged as LATENCY-TRACE-RETURN-PATH-UNOBSERVABLE (`964f54c`).

**In-flight at write time:** opus gateway-latency trace+fix; sonnet skill-nudge
softening in `~/app/tools`.

## 10:36 2026-07-04


## 12:32 2026-07-04

### Session 3: TUI speed-test sprint (T1-T3) + a real bug caught

Founder asked for full-stack, speed-measuring usability tests for `rsx-tui`
proving it supersedes the browser. Opus planned it (`.ship/33-TUI-SPEED-TESTS/
PLAN.md` + `TASKS.md`): 6 file-disjoint tasks, harness = headless ratatui
`TestBackend` + real cluster over the gateway WS. Key discovery: the TUI speaks
QUIC but the gateway is WebSocket-only, so T1 had to build the transport first.

Sonnet shippers, sequential, each verified by me before the next:
- **T1 (`28da0a7`)** — `WsConn`: webproto `{N:[...]}` over tokio-tungstenite,
  HS256 JWT, U/F/E fold. I added heartbeat echo (sub had left `H` ignored →
  would drop idle e2e connections after 10s).
- **T2 (`1f23b48`)** — `TuiHarness` (feed_key/tick/wait_for/screen) + cluster
  (skip-if-down) + timing (pctile/Timer). MockConn smoke green.
- **T3 (`350dd08` + `899414c`)** — orders + book e2e over WsConn. This is where
  the harness paid off: it CAUGHT a real bug.

**RETURN-PATH-INTERMITTENT-DROP** (logged, `899414c`): a taker's fill returns
reliably over a *transient* per-order WS (playground path 12/12, user 3 fills
fine) but is LOST over a *persistent* `WsConn` — the exact model the TUI uses.
Verified real (persists on clean non-trace binaries, fresh restart), NOT
user-specific, NOT margin/lot/rate-limit/trace. The two fill-based e2e tests are
`#[ignore]`d on it. Opus dispatched to root-cause+fix (blocks T4/T5 which need
fills); it was interrupted cross-session with no code landed, now resumed.

Also this session: gateway-latency measurement fixed (`8b33bbc`/`f6201d4`) — my
earlier "return leg never emits" diagnosis was WRONG; the real bug was
`/api/latency-stages` subtracting medians across a mixed oid population. Real
egress `me_out→gateway_out` is bimodal (~35-45µs spinning / ms-scale parked).
