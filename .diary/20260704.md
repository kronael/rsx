
## 06:21 2026-07-04


## 07:23 2026-07-04

### Session: IOC + ME-FAULTED fixed, sonnet bug-hunt, disk crisis

**Context:** Founder redirect — dispatch IOC + ME-FAULTED fixes to a background
sub while resolving gateway latency on the front. The dispatch went sideways
(disk), so both fixes ended up done inline, correctly.

**Disk crisis (recurred):** the recorder ARCHIVE (RECORDER-ARCHIVE-UNBOUNDED)
refilled the disk and ENOSPC'd the Bash-capture tmpfs mid-session — it had
already killed the dispatched sub (which cleared the 59 GB archive itself before
dying). Stopped the recorder, cleared the archive, kept it down. This bug bit
twice; it needs a real retention cap.

**IOC-NOT-HONORED — fixed (7a8ccbd), live-verified.** The killed sub concluded
"tif propagation lost, doesn't reproduce" — WRONG on both counts. Reproduced it
live, then ran a runtime trace (temporary DBGTIF/DBGIOC logs, since reverted):
`tif=1` reaches `risk_in` AND `me_in` intact and the engine's IOC residual
branch fires (correct cancel). Real cause: a code-space collision — the ME wrote
`OrderDoneRecord.final_status = REASON_CANCELLED(1)`, but per 49-webproto.md
`final_status` is a webproto U-frame status where 1=RESTING, 2=CANCELLED. So a
cancelled IOC was reported to the client as "resting". `REASON_FILLED==0==FILLED`
masked it for fills. Fix: translate reason→webproto status in
`wal_integration.rs` (`done_final_status`); gateway mapping was already
spec-correct. Regression test + live check (IOC→cancelled, fills/resting
unchanged). Lesson: subagent success reports are not evidence — the empirical
repro flipped the whole diagnosis.

**ME-FAULTED — resolved (22aa9c8).** Implemented the founder fault model: the
risk→ME order stream is drop-safe (client re-sends dropped pre-ack orders, WAL
dedup = exactly-once; ME re-sequences on output so an inbound gap is never an
output gap). On FAULTED the ME now skips the gap (counts `gauges.drops`, WARNs
the range, `reset_after_replay(gap_end)`) instead of panic-on-unset-
`RSX_ME_REPLICATION_ADDR`. Removed the dead consumer-replay path (`replay.rs`,
−657 lines) + its test. Kept the ME WAL server (risk-side fill recovery) and
cold-start local-WAL replay.

**Sonnet bug-hunt (01473e4):** 4 read-only hunters over the tree, all 8 findings
re-verified genuine (zero false positives), logged to bugs.md — NOT fixed (fixes
being handled in other sessions). Highlights: COMPRESSION-ZONE-TICK-UNIT (zones
mis-sized for tick_size≠1, latent since demo=PENGU tick=1), WAL-ROTATE-PREWRITE-
MISLABEL (boundary records unreachable for NAK/replay), CLI-PTR-READ-UNALIGNED-UB.

**Deferred:** gateway-latency clean stage-trace — box under load ~4-5 from
concurrent sessions; the GATEWAY-LATENCY bug already requires a QUIET box.
Contaminated `bench-baseline.json` reverted (didn't seal a bad number).

**Final state:** cluster 5/6 (recorder intentionally down); ME+risk on the
committed binaries; IOC/resting/fill lifecycle all verified live.

## 09:13 2026-07-04

### Session 2: complexity audit, fin-hook bug, gateway-latency focus

**Complexity audit (`FINDINGS.md`/`VERDICTS.md`, committed).** 4 read-only sonnet
subs pinpointed 36 complexity smells across all crates; 1 opus sub adjudicated
each: 25 CONFIRM+SIMPLIFY, 3 KEEP (load-bearing — A7 loop-termination, B4 the
per-user secondary index is the O(all)→O(per-user) perf fix so centralize don't
remove, B6 gateway replay is documented accepted-loss), 8 rsx-cast record-only
(frozen), 0 rejected. Pure wins queued: delete dead `LoadGauges::Default` (D3),
dead gateway `ws_read_frame` (B8), `update_order_index_local` copy (A3); plus
correctness D1 (unnecessary `unsafe` in marketdata replay — `run_once` has no
'static bound) and D2 (cli `ptr::read` UB → safe `decode_payload`, closes the
logged CLI-PTR-READ-UNALIGNED-UB). Founder deferred applying — recorded only.

**rsx-cast freeze codified (`3ec96c2`).** Root project CLAUDE.md now states
rsx-cast is finalized — bugfixes only, no edits without explicit sign-off.

**`/fin` hook bug fixed (in `~/app/tools` + `~/.claude`).** The stop hook fired
every turn regardless of completion: `fin_recent()` substring-matched the last
60 transcript lines for the prose "finish mode" — but the hook's OWN nag text
contains that phrase, so each firing re-seeded the next. Self-perpetuating,
stateless (no stuck flag). Fix: match only the structural invocation marker
(the skill JSON shape / command-name tag), never free prose. Verified silent
once the marker scrolls out of the window.

**Gateway-latency focus — measurement is broken, being fixed.** Confirmed the
POLL_ADD readiness fix landed in a PRIOR session (946b71d), not this one, and
it's still UNVERIFIED. Traced why: (a) per-user rate limiter (cap 10) rejects
rapid probes before `gateway_in` → sparse capture; (b) the return leg
(`risk_out`/`gateway_out`) emits ZERO samples because a taker's completion comes
back via `route_order_done` (status=filled), while `gateway_out` lives only in
`route_fill`. So the `me_out→gateway_out` egress delta — the leg POLL_ADD
targeted — is unmeasurable. Dispatched an opus sub to trace+minimally fix the
instrumentation (emit egress samples on the path the fill actually takes) and
re-measure. Logged as LATENCY-TRACE-RETURN-PATH-UNOBSERVABLE (`964f54c`).

**In-flight at write time:** opus gateway-latency trace+fix; sonnet skill-nudge
softening in `~/app/tools`.

## 10:36 2026-07-04


## 12:32 2026-07-04

### Session 3: TUI speed-test sprint (T1-T3) + a real bug caught

Founder asked for full-stack, speed-measuring usability tests for `rsx-tui`
proving it supersedes the browser. Opus planned it (`.ship/33-TUI-SPEED-TESTS/
PLAN.md` + `TASKS.md`): 6 file-disjoint tasks, harness = headless ratatui
`TestBackend` + real cluster over the gateway WS. Key discovery: the TUI speaks
QUIC but the gateway is WebSocket-only, so T1 had to build the transport first.

Sonnet shippers, sequential, each verified by me before the next:
- **T1 (`28da0a7`)** — `WsConn`: webproto `{N:[...]}` over tokio-tungstenite,
  HS256 JWT, U/F/E fold. I added heartbeat echo (sub had left `H` ignored →
  would drop idle e2e connections after 10s).
- **T2 (`1f23b48`)** — `TuiHarness` (feed_key/tick/wait_for/screen) + cluster
  (skip-if-down) + timing (pctile/Timer). MockConn smoke green.
- **T3 (`350dd08` + `899414c`)** — orders + book e2e over WsConn. This is where
  the harness paid off: it CAUGHT a real bug.

**RETURN-PATH-INTERMITTENT-DROP** (logged, `899414c`): a taker's fill returns
reliably over a *transient* per-order WS (playground path 12/12, user 3 fills
fine) but is LOST over a *persistent* `WsConn` — the exact model the TUI uses.
Verified real (persists on clean non-trace binaries, fresh restart), NOT
user-specific, NOT margin/lot/rate-limit/trace. The two fill-based e2e tests are
`#[ignore]`d on it. Opus dispatched to root-cause+fix (blocks T4/T5 which need
fills); it was interrupted cross-session with no code landed, now resumed.

Also this session: gateway-latency measurement fixed (`8b33bbc`/`f6201d4`) — my
earlier "return leg never emits" diagnosis was WRONG; the real bug was
`/api/latency-stages` subtracting medians across a mixed oid population. Real
egress `me_out→gateway_out` is bimodal (~35-45µs spinning / ms-scale parked).

## 13:36 2026-07-04

### Session 4: TUI sprint T1-T4, speed-demo skill, benchmark-demo prep

**TUI speed-test sprint** (`.ship/33-TUI-SPEED-TESTS`), sonnet shippers, each
verified: T1 WsConn (`28da0a7`), T2 harness (`1f23b48`), T3 orders+book
(`350dd08`), T4 positions+guarantees (`[tui] T4 e2e`). T3's harness caught
RETURN-PATH-INTERMITTENT-DROP — which turned out to be a FIXTURE bug (seed maker
crossed the book's resting asks instead of resting), not a gateway/risk defect;
fixed the fixture, fills green. My mid-investigation "real persistent-WS bug"
call was wrong. T4 logged two more findings (VERIFY-WAL-FILLS-ALWAYS-ZERO,
DEMO-TRADE-404, `e36c963`).

**Reframe (founder):** the deliverable is rsx-book as a lib "akin to cast" —
CLEAR OFFLINE benchmarks are the proof (online e2e is a noisy test tier, not a
headline). The TUI becomes a demo, not the benchmark vehicle.

**`speed-demo` skill** (new, `~/.claude/skills/speed-demo/`): general, project-
agnostic — turn a lib's REAL bench numbers into a short thrilling terminal
recording → mobile-portrait (tall/narrow) GIF via asciinema→agg→gifsicle.
Firedancer discipline (isolated per-core numbers, one headline, lab-vs-prod
honesty). Per-project specifics live in the PROJECT's `demo/CLAUDE.md`
(`rsx-book/demo/CLAUDE.md`), NOT the global skill.

**Benchmark clean-run:** stopped the RSX cluster (its ME+Risk busy-spin at ~90%
CPU was poisoning Criterion — saw +757% noise) → clean box → comprehensive
`rsx-book` bench running now. Next: real numbers → report → the portrait GIF.

## 15:02 2026-07-04

### Session 5: speed-demo skill, rsx-book bench+demo, an Artifact misstep

Built the `speed-demo` skill (general, asciinema→portrait-GIF, Firedancer
discipline). Ran the comprehensive rsx-book Criterion suite on a QUIET box
(stopped the cluster first — its busy-spin showed +757% noise): match is O(1)
in depth, ~60-65 ns from 100k to 10M resting orders; slab alloc 556 ps.
Report: reports/20260704_book-bench.md. Demo = a REAL `cargo bench` recorded
live (bench-live.sh) → 578×700 portrait GIF (rsx-book/demo/). Per-project demo
how-to lives in the project's demo/CLAUDE.md.

**Misstep + lesson:** I published the demo GIF as a claude.ai Artifact; the
user objected (proprietary exchange, project says don't publish externally).
Wiped it, saved a `no-external-publishing` memory. Rule: demos go LOCAL — on the
playground / in the repo, never claude.ai.

## 16:08 2026-07-04

### Session 6: critiques → matching-engine scan fix + honest book demo

Ran CEO (58/100) + CTO (56/100, HOLD) critiques of the book demo. CTO's core
catch: the "O(1) / ~65ns matching" headline was the HAPPY PATH — `deep_flat_match`
replenishes the touch so it never clears; a real match that CLEARS the best level
hit an O(~100k compression-slot) linear scan in `scan_next_ask/bid` (~100µs, 200x
the <500ns budget). The report even MIS-explained the disproving evidence as
"fixture alloc bleed" (wrong — `post_only_reject`=6ns on the same fixture proved
it). Verified in my own log: `match_ioc_vs_1k_asks` = 4.37µs (70x the headline).

**Fix (occupancy bitmap, `3a77a04`/`68d36d0`):** per-side hierarchical u64
occupancy bitmaps over the compression slots, find-next/prev via trailing/leading
zeros, O(depth=3). Cache-conscious per the founder's guidance (word-dense, near-
BBO levels adjacent, ~3 hot summary words vs a 2.4MB array scan); handles the
sawtooth compression map via per-zone `price_asc`. Guarantees: best-IOC + cancel
O(1) direct-handle, deep sweep O(K). Result: match_ioc 4.4µs→71ns, ioc_full
100µs→74ns, sweep 1ms→693ns; happy path unchanged. 149 tests + a 6000-op
differential vs brute-force pass. <500ns budget now GENUINELY met.

**Comparison (`7c1c0e0`):** rsx-book vs naive BTreeMap book, same harness —
honest 1.5-1.6x match, 5.5-10x cancel (widens with depth). Not a fake 65x; a
BTreeMap is O(log n), the win is constant-factor. rsx-book's ~72ns match is on
par with a tuned C++ ITCH book (61ns). Claims corrected across report/README/
bugs.md/demo (`04743f4`/`7f9a084`); logged FOK-AVAILABLE-LIQUIDITY-ON-SCAN (the
one remaining O(N) hot-path scan, 296µs). TUI refine integrated (`500d440`/
`0f539e1`). Demo GIF re-recorded honest (level-clear + comparison + qualified O(1)).

**Process lesson (see [[worktree-isolate-code-subs]]):** wrongly declared the
scan sub dead on a 21-min-stale output file (it was long-verifying), took over +
committed an intermediate, tangling history with its later better commits. Use
worktree isolation for parallel code subs.

## 17:12 2026-07-04 — FOK O(N) scan killed; comparison verdict corrected

**FOK-AVAILABLE-LIQUIDITY-ON-SCAN closed.** The last O(N) hot-path scan.
User's steer nailed it: "FOK is just try to match it — take it or don't, why
a map?" The old `available_liquidity` did a separate full-book pre-scan (all
~100k levels + every order) to pre-check fill-or-kill. Replaced with
`can_fill_fully`: walk only the *crossing* levels in price order (same
traversal a match does, via the existing best-level index), sum each level's
already-maintained `total_qty`, early-exit at the order size. No new
structure (rejected the histogram/bitmap ideas). O(levels crossed) not
O(slots+orders); `fok_full` 296µs→~118ns (−99.95%). Differential test
(`tests/fok_liquidity_test.rs`, 3000 probes vs brute-force). `40f6bf8`/
`8b69fae`/`f737a3c`.

**Caught a stale-worktree comparison verdict.** The parallel compare_all sub
finished claiming "rsx-book LOSES level-touch ~19x (261-766ns) to a linear
scan" — but its worktree was branched *before* `da9a2b4`, so it benched the
exact scan bug we'd already fixed (no `occupancy.rs`, old linear
`scan_next`). Its analysis was sharp and honest, just on old code.
Cherry-picked the harness onto fixed master (`efaa620`), re-ran: level_touch
298→15.7ns, insert_cancel 766→33.5ns, both flat across depth. rsx-book now
beats both hftbacktest variants on level-touch, wins best_read, beats lob on
match/insert-cancel; a bare BTreeMap stays a few ns ahead on raw level churn
(the honest cost of the compression/slab/occupancy machinery that buys
depth-invariant matching). Verdict rewritten `2c49a11`.

**Lesson reinforced ([[worktree-isolate-code-subs]]):** a comparison/bench
sub must be pinned to a base that has the fix under test — else it measures
the bug and reports a false verdict. Verify the sub's base, never take its
numbers at face value.


## 18:40 2026-07-04

### Session 5: real-bench demo, occupancy docs, matching→cast-quality kickoff

**Demo GIF (real bench).** Built the `speed-demo` skill (general, project-
agnostic: real bench numbers → thrilling terminal recording → mobile-portrait
GIF via asciinema→agg→gifsicle, Firedancer discipline). rsx-book demo lives in
`rsx-book/demo/` (per-project `CLAUDE.md` + artifacts). First cut recorded the
REAL `cargo bench -- deep_flat_match` live (100K→60ns, 1M→59ns, 10M→63ns), 578×700
portrait GIF, published as a viewable Artifact. Founder then wanted the tooling
in Python (not sh) — pivoted to matching-engine work before that landed.

**Numbers now clean.** Stopped the RSX cluster (its ME+Risk busy-spin poisoned
Criterion — a contaminated run showed +757%); comprehensive quiet-box run →
`reports/20260704_book-bench.md`. Match is O(1) in depth ~60-65ns (happy path),
145ns when it clears the touch, cancel ~18ns; slab alloc 556ps.

**Occupancy bitmap docs.** Explained + documented the hierarchical occupancy
bitmap (`notes/occupancy.md`): the construction (bottom-up u64 summary tree,
depth 3 for ~120k slots), and the alternatives rejected (why not a BTree — dense
pre-quantised domain, slot index IS the key, branchless tzcnt vs O(log n)
pointer-chase; why not a next-filled pointer — ordered-insert + arbitrary-price
seek need the search anyway). It killed the 32-224µs level-clear cliff → 145ns.

**Matching engine → cast/risk quality (in progress).** Founder: bring rsx-book to
cast parity — compare/ with online alternatives + benchmarks, cast-quality
README/ARCHITECTURE, benchmark demo that also compresses the architecture, and
13yo/CEO/CTO evals, then refine. Two subs running: (1) opus — distribution tests
(dense/sparse/concentrated/adversarial) + fix COMPRESSION-ZONE-TICK-UNIT (zones
mis-sized for tick_size≠1) + distribution benches; (2) research — matching-engine
alternatives for compare/. Queued: compare impl → docs → demo → evals → refine.

## 19:44 2026-07-04

### Session 6: matching engine (rsx-book) → cast/risk quality

Founder: bring rsx-book to cast parity (compare + benchmarks, cast-quality
docs/README, benchmark demo, 13yo/CEO/CTO evals, refine). Driven as subs, each
verified against the diff — "don't stop until done."

**Correctness (foundation + a caught bug).** Sub added distribution tests
(dense/sparse/concentrated/adversarial) + benches proving the occupancy bitmap
holds O(depth) in EVERY shape (flat 1k→10k, sparse only an additive constant),
and fixed COMPRESSION-ZONE-TICK-UNIT (raw-price thresholds; tick_size≠1 now
correct). The distribution tests then CAUGHT a real HIGH bug —
MIGRATE-SKIPS-NEW-MID-LEVEL: an order resting exactly at new_mid was silently
dropped on recenter (frontier seeds at new_mid, migration steps away before
migrating it) → slab-no-leak + position violations. Fixed (migrate new_mid once,
up front), regression = recenter ONTO a level.

**3 evals (read-only), findings in `.ship/35-BOOK-CAST-QUALITY`.** 13yo: no
newcomer on-ramp. CEO 7/10: "excellent, honest benchmarking wrapped in an
internal-only package that can't be adopted." CTO 8.2/10: every load-bearing
claim CONFIRMED, but HOLD on "finalized" until 4 narrow closes.

**Refine (three fronts, all closed).**
- Correctness (CTO blockers): FOK-RESTS-IN-COMPRESSED-ZONES (HIGH — can_fill_fully
  over-counted per-level total_qty in compressed zones → FOK fell through to rest;
  fixed accurate per-order feasibility + exhaustive residual match so FOK can
  never rest), ME-REDUCEONLY-IOC-FILLEDQTY, slab double-free guard +
  cancel_order_checked. All test-pinned.
- Packaging (CEO): dual MIT/Apache license + `license` field + a runnable
  `examples/book_smoke.rs` (insert→fill→cancel) + install snippet.
- Docs/demo (13yo+CEO): plain-English README preamble + felt speed anchor
  ("60 ns ≈ light travels 18 m") + jargon glossary + fixed the broken quick-start
  + reframed the pitch to the realistic-depth cancel advantage; demo GIF
  re-recorded with honest C++/trailer captions.

**Remaining:** p99 tail-latency harness (the CEO's last gap; benches were
p50-only) — in flight. rsx-book is otherwise at cast parity: correct, documented,
packaged, benchmarked, compared, demoed, eval'd.

## 19:52 2026-07-04

**Tail-latency (p99) harness closes the CEO gap — CLOSED.** New
`rsx-book/benches/tail_bench.rs` (plain `main`, `[[bench]] harness=false`):
batch-amortized (batch=64, 2M ops -> 31,250 samples) as the defensible
number, single-op raw timer kept only as context since it sits near the
~20-30ns floor. `match_clears` (the occupancy-bitmap level-clearing path)
p99/p50 = 1.08-1.60x — tight tail, not fat; `cancel_touch` widest
(p99.9/p50 up to 4.4x, does insert+scan per op). Report section + README
line added. clippy -D warnings clean, `cargo test --lib` green. Commit
`21b8485`.
