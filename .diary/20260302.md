---
summary: |
  RSX perp exchange. Solo work. Trade UI WS audit sprint.
  - positions U-message handler was broken (order vs position not discriminated)
  - WS backoff was resetting on first message, not on open — silent auth failures stalled retries
  - upstream WS close codes not forwarded to browser by playground proxy
---

## 22:06

Cleared stale `.ship/TRADE-UI/LOG.md` left over from earlier audit. Started fresh reconnect audit sprint.

## 22:09

Rebuilt dist bundle after WS reconnect audit pass. Ship log updated — nginx `/ws/` headers already correct, reconnect backoff indicator confirmed present before today's fixes.

## 22:10

Added positions store slice (`rsx-webui/src/store/trading.ts`, +23 LOC). Discriminated `{U:[{type:"position",...}]}` from order updates in `usePrivateWs.ts`; added `updatePosition` upsert/remove by `symbolId` — was silently dropping position messages before (d7347dd).

## 22:16

Bug: WS backoff `retryRef` was reset on first message, not `onopen`. Silent auth failure → connection opens, no message → retry delay never reset → exponential stall. Fixed: reset in `onopen` (6a8add8).

## 22:20

Audit findings logged: reconnect re-subscription and position re-fetch chain both confirmed clean. No code changes needed beyond the two fixes above.

## 22:25

Bug: playground proxy wasn't forwarding upstream WS close codes. `WSMsgType.CLOSE` frames in `fwd_up` now caught and relayed — gateway-initiated closes (e.g. `4001` auth failure) now reach browser `onclose` handler intact (7cca533).
