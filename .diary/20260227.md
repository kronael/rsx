---
summary: |
  RSX perp exchange. Solo work. Heaviest day of W09: sim crate deleted,
  multi-symbol routing wired, wal-dump feature-complete, perf-verification
  shipped, trade UI WS reconnect hardened. 62 commits.
  - decision: rsx-sim dead crate deleted; stress = managed subprocess
  - multi-symbol: RSX_ME_CMP_ADDRS, risk + marketdata route by symbol_id
  - wal-dump: --type/--symbol/--user/--from-ts/--to-ts/--follow/--stats
  - open: bench-gate.sh ÷0 guard needed (fixed same day)
---

## 05:50

/v1/account human-readable formatting (a5e939a): raw i64 prices/qty
converted at API boundary. Investor demo tests added (api_e2e_test.py
+74 LOC). AUDIT.md updated.

## 09:10

/v1/orders human-readable prices (5b9fc95). Blog #27 written (1167081):
"finalize round" — 106-line post. WAL timeline filter + colored record
rendering wired (5552b3a, 932aaad): parse all 14 record types,
filter-buttons connected to backend. Bug: wrong key `type_name → type`
(d0a990). Book ladder decimals fixed (11715b9, 15a0fa8, d73ce32, 17194eb).

## 14:08

Spec housekeeping (f3bf5d6): 29 completed specs/plans moved to
`specs/done/`. AGENTS.md deleted, index.md deleted, rsx-webui/LOG.md
removed. TESTING.md created (79 lines). -172/+934 LOC.

## 14:44

Four new specs written (d60a3ba): specs/v1/CLI.md (155 lines),
SCENARIOS.md (200 lines), SIM.md (184 lines), TRADE-UI.md (168 lines).

## 15:13

**Decision: delete rsx-sim.** SIM.md rewritten to remove sim mode;
stress_client.py promoted to managed subprocess (e2f49e7). rsx-sim crate
deleted — Cargo.lock -11 lines (1f52f60). sim mode stripped from
server.py -147 LOC (21d7dec). stress.py created as standalone entry
point (1c5f4f8). ~5 chore commits log progress.

## 18:29

**Multi-symbol routing** (02afd20, 5fc1853, d2137e9): `RSX_ME_CMP_ADDRS`
env var routes orders to correct ME by symbol_id. Risk builds `me_senders`
map keyed by symbol_id. Marketdata subscribes all ME CmpReceivers.
Backward-compat path `RSX_ME_CMP_ADDR` singular preserved + tested
(62ec63f, ecea9d1). Regression test verifies serial execution
(6648bb1).

## 19:30

**wal-dump CLI feature sprint** (44d00b1 → a36216e): `Filters` struct
with per-type field extraction (`--type`, `--symbol`, `--user`,
`--from-ts`, `--to-ts`). `--stats` for per-type counts. `--follow`
poll loop with ctrlc handler (100ms poll, reopen from last seq+1).
`--tick-size`/`--lot-size` display scaling (raw i64 always in JSON).
ORDER_REQUEST/ORDER_RESPONSE decode arms added. Bug: unknown `--type`
was silently skipped → now exits 1. Bug: `next_seq` not advancing on
filtered records in `--follow` → regression test added. +247 LOC CLI
tests for LIQUIDATION/ORDER_REQUEST/ORDER_RESPONSE decode.

## 21:25

perf-verification spec rewritten (bdd775f): removed rsx-sim references,
resolved Criterion JSON path ambiguity, scoped to 3 concrete deliverables.
Sprint wired (eb9a0f4): `scripts/bench-gate.sh` (102 lines), Makefile
target, `/x/gateway-mode` HTMX endpoint, latency + gateway-mode pytest
tests. Bug: bench-gate.sh ÷0 on zero baseline (9a7b7c3). Bug:
`declare -A` unbound under `set -u` (5c9306e). Both fixed same session.

## 22:28

Trade UI WS reconnect (1eac7fd): `RECONNECTING`/`OFFLINE` states added
to `WsStatus`. code 1013 detection. TopBar shows live/connecting/
reconnecting/offline label. nginx config: proxy_http_version 1.1,
Upgrade/Connection headers on /ws/, docs proxied to :49171 (501d7ec).

## 23:39

Private WS subscribe fix (e02cba2): sends `{"N":["positions","orders",
"fills"]}` on connect so gateway delivers position snapshots. Backoff
reset moved from `onopen` to first message for robustness. 150ms delay
after fill so WAL flushes before position re-fetch (2ae6647). 4001 auth
failure handled in private WS hook (79faca2).
