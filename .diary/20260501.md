---
summary: |
  RSX perp exchange. Solo work. Big day: 06-PUBLISH sprint closed + rsx-messages
  crate split + CMP WAL-backed NAK retransmit + E2E latency probe shipped.
  - 06-PUBLISH: all 10 tasks complete
  - rsx-dxs split into transport (rsx-dxs) + domain wire records (rsx-messages)
  - CMP two-tier loss recovery: hot in-memory ring → cold WAL random-access
  - E2E latency probe: dashboard tab + persistent bench-baseline.json
  - security batch: per-IP limiter bounded (10k cap), JWT min-secret + nbf + jti
  - CMP send_ring preallocated: no per-send heap alloc
  - [open] security/cmp source-IP filter added — may need revisit vs spec trust model
---

## 08:27

Stale frozen_orders cleared in seed_accounts (cccda62). release-gate canonical Playwright total bumped 421→419. play_overview scenario-selector test renamed to match impl. 06-PUBLISH all 10 tasks marked complete (cd76421).

## 09:15

Dev-secret defaults removed; insecure flag now logs a warning; runbook depersonalised (9aac710). -75 LOC across market_maker, server, stress_client.

## 09:18

PROGRESS.md and TESTING.md reconciled: test counts corrected, stale 78M slot claim fixed (67cf481).

## 10:33

Spec honesty pass: specs/2/4-cmp.md and specs/2/45-tiles.md rewritten for honesty (f4fc79c). ~1600 lines changed. Removed overclaims.

## 10:36

CMP receive path hardened + fuzz target added (b76f152). fuzz/fuzz_targets/wal_header.rs created; header_test.rs gets 38 lines of coverage. 6 files, 343 ins / 157 del.

## 10:37

Playground synthetic-data fallbacks tagged with 'source' field (f6e8cac) to distinguish from real engine data.

## 10:40

Dashboard latency tab + persistent bench-baseline.json (554616a). bench-gate.sh updated; specs/2/22-perf-verification.md aligned.

## 12:22

Launcher contradictions closed; marketdata config fix (6abd1f9). playground script hardened. 10 files, 146 ins / 77 del.

## 12:26

Launcher injects dev JWT secret; remaining contradictions reconciled (186ff60). docs/DEMO.md updated.

## 12:29

Walkthrough subtitles fixed; CLAUDE.md: final budget vs measured pass (66c888).

## 12:51

E2E latency probe shipped (bded133): POST /api/latency-probe opens WS to gateway, submits marketable order, measures fill round-trip with perf_counter_ns(). GET /api/latency returns e2e block alongside gateway-roundtrip numbers. 1000-deep ring buffer. 166 LOC in server.py.

## 12:54

CMP WAL-backed NAK retransmit: two-tier loss recovery (366d1b2). Hot tier: BTreeMap send_ring 4096 entries. Cold tier: wal::read_record_at_seq scans WAL file by name ({stream_id}_{first}_{last}.wal) to serve any seq within retention window. Closes biggest CMP-spec gap.

## 13:27

E2E probe Playwright tests added; canonical count 419→421 (b48b0aa).

## 13:48

bench-baseline.json committed from existing criterion data (f348740). 38 lines, regression detection at >10%.

## 16:13

Decision: extract domain wire records from rsx-dxs into new rsx-messages crate (8495ace). rsx-dxs is now domain-agnostic — zero rsx-types dep in production builds (provable via cargo tree). 11 domain records moved (Fill, BBO, Order*/Cancel*/*, MarkPrice, Liquidation, ConfigApplied). 12th crate added. 54 files changed, 594 ins / 538 del.

TESTING.md + BLOG.md updated: 11→12 crates (ae74651).

## 20:08

WAL append errors on fill path now propagated instead of silently dropped (82a9206). rsx-matching main.rs refactored. PLAN.md for sprint 13-A16Z-FIXES added.

## 20:11

security: CMP source-IP filter added (acd245f). CmpReceiver drops datagrams from IPs not matching sender_addr. recv_allow_any_source escape hatch in CmpConfig. 3 unit tests.

## 20:13

security: gateway per-IP rate-limiter map bounded at 10,000 entries with FIFO eviction (b579160). Prevents slow-burn memory DoS from IP rotation. New test: ip_limiter_map_is_bounded.

## 20:15

security: JWT min-secret length enforced, nbf validated, jti tracker added (a6a92c3). 5 files, 166 ins / 1 del.

## 20:17

Gateway JWT test secrets bumped to 32 bytes to satisfy new min-length check (83c2534).

## 21:45

CMP send_ring preallocated: three fixed slabs (ring_seqs, ring_lens, ring_frames) replace BTreeMap (7befe76). Slot = seq & SEND_RING_MASK. No per-send heap alloc. Large frames bypass ring → WAL cold-tier.

## 21:48

Honesty pass: "100%" claims dropped, test counts reconciled, monoio truth corrected across CLAUDE.md, FEATURES.md, PROGRESS.md (19a3d6e).

## 21:49

WEDGE.md: single strategy chosen for founder review (9bd5fc6). 151 lines in .ship/13-A16Z-FIXES/.
