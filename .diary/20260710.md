
## 06:32 2026-07-10 — terminal: flow tests + FLOWS.md

Founder asked "did you clickthrough all flows? are flows documented?" — both
were real gaps: ~90 per-fold unit tests but nothing driving a whole user
journey, and no walkthrough doc. Closed both (`3446061`, `11fb431`):
- **ui/flows_test.go** — 16 end-to-end scenario tests, each driving the model
  start-to-finish through one journey: connect, place limit (decimal in → raw
  out → ack → panel), cancel-selected/all, market, fill→position, flatten,
  reverse, ARMED (guard still holds), click-to-price, +/-/j/k, fat-finger
  block, reject, md-down+recovery, gw-down (links independent), F3/? overlays.
  Compose the existing press/typeDigits/apply/clickAt helpers; ui now 83 tests,
  -race/vet/fmt green.
- **FLOWS.md** — user walkthrough, one section per test (same name) so doc and
  checks stay in lockstep; cross-linked from SCREENS.md.

Honest gap that remains: flows run over MockGateway (same Update path as live;
conn/live_test.go covers the socket) — a manual TTY session against a running
cluster is still the one thing not machine-checked.

## 06:55 2026-07-10 — terminal: symbol-config auto-fetch (last queued item)

Resumed the one open thread (queued when subs were rate-limited/529'd): the
terminal now fetches decimals+tick from the gateway's `GET /v1/symbols` at
startup (`80d0a21`) instead of trusting env defaults. Same listener as the WS
(non-upgrade requests fall through to REST — no protobuf Metadata frame
needed, spec 49's SymbolMeta stays unimplemented server-side). Env vars are
explicit overrides; failed fetch → PENGU defaults + note, never blocks launch.
Verified two ways: httptest (shape copied verbatim from rest.rs) and a LIVE
smoke — launched the real rsx-gateway on :18080, FetchSymbolConfig parsed its
actual response (bare gateway serves 8/8 until playground pushes per-symbol
config). Spec 55 updated. All session threads now closed; the only unchecked
box is a human TTY session against a full cluster.
