
## 06:32 2026-07-10 — terminal: flow tests + FLOWS.md

Founder asked "did you clickthrough all flows? are flows documented?" — both
were real gaps: ~90 per-fold unit tests but nothing driving a whole user
journey, and no walkthrough doc. Closed both (`3446061`, `11fb431`):
- **ui/flows_test.go** — 16 end-to-end scenario tests, each driving the model
  start-to-finish through one journey: connect, place limit (decimal in → raw
  out → ack → panel), cancel-selected/all, market, fill→position, flatten,
  reverse, ARMED (guard still holds), click-to-price, +/-/j/k, fat-finger
  block, reject, md-down+recovery, gw-down (links independent), F3/? overlays.
  Compose the existing press/typeDigits/apply/clickAt helpers; ui now 83 tests,
  -race/vet/fmt green.
- **FLOWS.md** — user walkthrough, one section per test (same name) so doc and
  checks stay in lockstep; cross-linked from SCREENS.md.

Honest gap that remains: flows run over MockGateway (same Update path as live;
conn/live_test.go covers the socket) — a manual TTY session against a running
cluster is still the one thing not machine-checked.

## 06:55 2026-07-10 — terminal: symbol-config auto-fetch (last queued item)

Resumed the one open thread (queued when subs were rate-limited/529'd): the
terminal now fetches decimals+tick from the gateway's `GET /v1/symbols` at
startup (`80d0a21`) instead of trusting env defaults. Same listener as the WS
(non-upgrade requests fall through to REST — no protobuf Metadata frame
needed, spec 49's SymbolMeta stays unimplemented server-side). Env vars are
explicit overrides; failed fetch → PENGU defaults + note, never blocks launch.
Verified two ways: httptest (shape copied verbatim from rest.rs) and a LIVE
smoke — launched the real rsx-gateway on :18080, FetchSymbolConfig parsed its
actual response (bare gateway serves 8/8 until playground pushes per-symbol
config). Spec 55 updated. All session threads now closed; the only unchecked
box is a human TTY session against a full cluster.

## 18:45 2026-07-10 — Makefile de-crufting, Go on PATH, terminal direction

**Removed the PROGRESS.md governance machinery.** Deleted 12 stdlib-only Python
scripts (~3843 lines: check-progress, status_doctor, acceptance-bundle,
gen-release-truth, exit-criteria, task-report, lint-snapshot, ci-guard,
local-runner, meta-guard, publish-progress, regen-progress) + scripts/tests/,
plus ~20 Makefile targets. It was agent-babysitting — machinery policing a
60-line markdown status file (meta-guard "block meta tasks", local-runner for
"quota-blocked agent sessions"). Docs are LLM-managed; no value to a human-run
repo. CI (ci/ci-full) rewired to gates+integration; release-gate dropped. (7a2f06a)

**Makefile self-documenting help.** Replaced the 48-line hand-maintained help
echo block (drifted — claimed "154 tests" etc.) with a grep|sort|awk one-liner
reading `## ` descriptions off targets. 607→429 lines. (f07c221)

**tui → term rename.** Renamed make targets tui/tui-local/tui-demo/tui-ssh-setup
→ term-* to match the Go rsx-term binary. Left the rsx-tui unix user + dispatch
scripts (real install names). (acbd17e)

**Lifecycle stays in make.** Rejected a pyproject/uvx layer for playground
lifecycle — it would just replace make with another task-runner in a Rust repo.
Makefile owns demo/local/stop/reset (wrapping the playground CLI); added
stop/reset. (05488fd)

**Go on PATH.** go was the official tarball at /usr/local/go but not on PATH (a
tarball can't set it; /usr/local/go/bin isn't a default PATH dir). Added
/etc/profile.d/go.sh — the proper system fix, not a symlink hack. `make term-*`
now work.

**Terminal direction (design, not built).** Resolved "is a trading terminal
viable": the static-DOM framing is theater (µs latency is irrelevant to a 200ms
human; volume comes from the API). Viable direction = a streaming text-mode
Bookmap — LOB heatmap over time with three nonlinear compressions (price
fisheye, log-size→color, time compression); trades render as aggressor flashes;
spoof/absorption become emergent. Plus a **dual mode**: depth view = one asset,
for quoting/market-making; assets view = many pairs, no depth, single-keypress
aggressive trades / quote-at-best-and-chase. Next: SCREENS.md mockup + a flagged
Bubble Tea prototype (RSX_TERM_STREAM=1).
