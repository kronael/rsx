---
summary: |
  RSX perp exchange. Solo work. CMP gap recovery + auto-sync wired;
  playground invariant SQL fixed; perf-verification spec written.
  - CMP gap-skip: prevents permanent stall after ME restart
  - CMP auto-sync: receiver syncs seq on first packet, detects sender reset
  - open: mark TLS failing for Binance WS sources (next day)
---

## 22:04

Three-commit fix batch late evening. Invariant SQL in server.py
corrected for positions, funding, stale-orders queries (c78dfa8).
TODO.md written: first time open work captured in one file.
`specs/perf-verification.md` scaffolded (244 lines).

## 22:11

CMP gap-skip recovery (64ab744): receiver now skips to sender's seq
on restart instead of stalling permanently. `/api/latency` endpoint
added to playground. Real production-correctness fix.

## 22:28

CMP auto-sync on first packet (0bf9afc): receiver syncs expected_seq
to first-seen seq rather than requiring a reset handshake. Sender-reset
detection added. Seed collateral bumped to unblock margin checks in
tests. cmp.rs -64/+95 LOC; cmp_test.rs adjusted.
