---
summary: |
  RSX perp exchange. Solo work. WAL schema versioning + O(1) cancel + 14-REFINE-PASSES sprint.
  - WAL version byte added to WalHeader (byte 8, V0=legacy, V1=current)
  - matching cancel: O(1) via FxHashMap<(user_id, oid) -> handle> replacing linear slab scan
  - source-IP filter reverted: spec §10.4 already documents trusted-network model
  - CLAUDE.md "Trust boundaries" section added to prevent future misclassification
  - 14-REFINE-PASSES: 46-item checklist shipped (A1-A8, B1-B6 complete today)
  - refine pass: let_=, unwrap, imports, dead code, size asserts, comment hygiene
---

## 05:44

WAL schema version byte added to WalHeader (730a441). Byte 8 = version (V0=legacy, V1=current), bytes 9-15 = reserved. Wire size unchanged (still 16 bytes). Adding new record types does NOT bump version (additive). Closes spec gap flagged by audit.

## 05:46

O(1) cancel in matching engine (cdc9360). Replaced linear slab scan (up to 65,536 ops per cancel) with FxHashMap<(user_id, oid_hi, oid_lo) -> slab_handle>. Index maintained via book.events(): OrderInserted → insert, OrderDone → remove. No leaks.

## 05:47

latency-publish: founder-runnable measurement script for E2E p50/p99 (d0144b4). scripts/latency-publish.sh, Makefile target, README updated.

## 05:49

13-A16Z-FIXES REPORT.md: every finding mapped to its resolution (ff432e2). 89 lines, PLAN/REPORT pair auditable.

## 07:13

Decision: source-IP filter reverted (bde3211). acd245f's "CVE-grade" finding was a misclassification — specs/2/4-cmp.md §10.4 already explicitly documents "trusted internal network, no authentication, no encryption." Filter contradicts spec, breaks multi-IP deployments, gives false confidence (co-located attacker can spoof source IP). Workspace 880→878 tests.

CLAUDE.md gains "Trust boundaries" section: when spec delegates a concern to a different layer, cite the spec — don't add code in the wrong layer.

## 08:22

14-REFINE-PASSES sprint started (8172f1a). 46-item checklist across A (code quality) and B (domain correctness) categories.

## 08:31

Refine A1: all `let _ =` on Result returns swept and replaced (caa9a5b). Refine A6: grouped imports split to one-per-line (6f8a581). 12 files in A1.

## 08:34

Refine A2: bare .unwrap() replaced with .expect("INVARIANT: ...") throughout (b393b95). gateway ws.rs + marketdata protocol/ws.rs.

## 08:39

Refine A3: .expect() calls annotated with SAFETY/INVARIANT messages (11bad1d). Refine A5: client.rs TODO linked to TODO.md 10-DEPLOY entry (89e57ec).

## 08:59

Refine A7+B1: dead code sweep + rsx-types pruning (5539ef5). rsx-types/time.rs unused exports removed; rsx-risk/lib.rs pruned.

Refine A8: history/narration comments dropped (9e683e1). 8 files, -17 LOC.

Refine B3: 22 compile-time size+align asserts on rsx-messages structs (f9a6c61). Ensures repr(C) layout is exactly what wire expects.

Refine B11: rsx-cli error/exit-code routing cleaned up (762308e).

## 09:06

Refine B2: rsx-dxs transport hygiene (1dafd35). server.rs + gateway rest/ws hardened.

## 09:07

Refine B5+B6: matching + risk hygiene with named-invariant messages (86f736b). rsx-matching + rsx-risk INVARIANT comments added.
