## 05:30 2026-07-11 — terminal + maker passes: session boundary, resumed to finish

Two background passes from the 07-10 session hit limits/boundaries; reconciled
the tree and resumed both.

**rsx-term (fable pass)** got most of the spec committed before hitting the
session limit: prototype → DOM-lock golden → depth view (log-time axis, trade
layer, game entry, persistence) → four screens (pair grammar, symbol switcher,
mode-line, lots) → venue seam (per-venue markets, F9 picker, read-only guard) →
**Hyperliquid read-only connector** (l2Book/trades/meta, sectors). It died
mid-chunk on the Tree of Alpha news reader — that WIP verified build/vet/test
GREEN, so I committed it (a5b132a) to preserve it, then resumed the pass to
finish the remainder: keymap + rebindable/prefix bindings + keymap-generated
help, pair depth-glyph/flow-bar, news sector market-map, LLM view, cursor
selection, standalone-HL end-to-end, calibrated glyphs, prior-art research.
Instruction: make it good, not fast.

**rsx-maker (opus)** had only written go.mod at the boundary. Resumed with its
recon: gateway JSON orders `{N:[...]}`/`{C:[...]}`, MD len-prefixed protobuf,
true mark on a separate `mark` WAL stream (not the MD WS). Building a clean
mark-anchored Go maker (PriceSource: mark-preferred, BBO fallback), isolated in
rsx-maker/, playground-launched.

**Glyph bank (my tooling, scratch)**: rasterized candidate glyphs + measured
coverage — shades ░▒▓█ are NOT evenly spaced (0.22/0.56/0.86); braille is tofu
in DejaVuSansMono; letters give the fine light-end ramp; markers (●◆■) for the
trade layer. Fed the measured vocabulary to the pass. Promote the tool to
rsx-term/tools/ once the pass stops touching that tree.

**SOL**: already in the RSX exchange config (runtime.py, id 3, large-cap);
routed "feature SOL in the demo watchlist" to the terminal pass.

Parked (exchange-side, Go): mark-anchored maker's real mark needs an external
feed; the liquidity-mirror (HL price → rsx-mark → maker → RSX book tracks
reality) + a trading competition are the launch/stress-test plan — deserve a
specs/ doc once terminal + connector land.

## 20:55 — terminal: streaming four-screen build complete

Shipped the full streaming terminal (13 [term] commits, a3c7842..9cc1704),
DOM byte-locked by golden throughout. BOOK: log-time multi-res heatmap
(now/100ms/10s→hours cascade, px-space rows, single size ramp, ○◆●■ trade
layer, ▚ persistence via book.AgeSource seam, game entry + fisheye cursor).
PAIR: letter-armed vim-count lots (notional×mult), depth/flow glyphs. NEWS:
sector tiles + ToA feed (RSX_TERM_NEWS=1, severity heuristic). LLM: real
news.AssistantContext handoff, placeholder reply. Venue seam: feed.VenueMsg;
hyperliquid read-only venue — live e2e smoke passed (1.4s to a real BTC
book). One keymap table → dispatch+hints+help, JSON rebinds. Open: HL
trading (EIP-712 TODO), engine order-age feed, LLM wiring.

## 08:00 — refine + codex hardening + a direction pivot

**Refine pass** (4-bucket sonnet review → triage → serial apply): fixed the
real fisheye inverse bug (odd-anchor bid dropped a half-tick — common tight
spread), ui unknown-id instrument stub + cross state-leak, HL Close CloseNow,
maker dial-timeout. Deferred the speculative/by-design ones to BUGS.md.

**Mouse removed entirely** — keyboard-only ("mouse is poison"). Dropped
WithMouseCellMotion, the MouseMsg handler, dead click helpers + tests, all
click prose. DOM golden held.

**Codex adversarial bughunt** (independent, high-effort) found 7 REAL bugs;
sonnet fixed all (verified real, no false positives): MD-startup no-retry,
Submit phantom-pending-on-write-fail, fisheye compress DoS via an untrusted
public MD level (upgraded my "speculative overflow" deferral to a real DoS),
cascade time-drop after a stall, blockable readers, HL malformed trade side,
position i64 overflow. All green + DOM-invariant. (Codex over-stepped and
edited files unprompted — reverted its diary/BUGS edits, kept the findings.)

**Direction pivot** (opus deep-research → 5 unique directions → founder chose):
reject the Hyperliquid-specific plays — the terminal is generic MULTI-EXCHANGE
(RSX + HL are just two backends). Adopt #4 "The Microscope" as the BOOK screen's
spine: scrub the log-time history (already kept), freeze on an event, hand the
frozen window to the LLM (the liked LLM connection, made concrete). Keep LLM +
NEWS. REMOVE the PAIR screen; NEWS absorbs cross-symbol as **correlations +
inter-symbol movement** (overview, not a trading grid). Keep the model/handoff
UI-agnostic so a GPU/bitmap frontend reuses it later. Stay terminal, minimal.
Next: codex pressure-test #4, then build the restructure (remove PAIR → NEWS
rework → scrub/freeze/handoff) — pending founder confirm of the pivot.
