---
summary: |
  RSX perp exchange. Solo work. Heavy sprint: refine-2 spec reconciliation
  + v0.2.0 release + playground audit (F1-F19 filed + first fixes landed).
  - F1 cascade (ME restart → 45GB OOM + WAL truncation) fixed
  - F2-F11 playground truthfulness fixes landed
  - F12 trade UI quiet first-paint fixed
  - F13-F19 oracle audit filed; fixes deferred to next session
  - T3.2 state-machine risk promotion refactor shipped
  - open: F13-F19 fixes, rsx-maker dead code removal
---

## 19:53

Spec reconciliation sprint (14-REFINE-PASSES bucket C):

- C3 53eb5f1: matching + orderbook specs reconciled. Discovered book.rs:88
  silently drops on event-buffer overflow with eprintln!; left for
  explicit decision (rsx-book has no tracing dep).
- C4 7dbaaa1: risk spec was understating — replication, tip-sync, lock-poll
  promotion all live but undocumented. Flagged env::set_var + recursive
  run_main (T3.2) in spec.
- D6 be9c162: `update_positions_on_fill` downgraded to pub(crate); 30
  other candidates kept pub (test/bench consumers).

## 19:56

- C5+C6 ce4f74c: marketdata + gateway specs reconciled.
- C7+C8 c7f9765: mark + messages specs reconciled.
- C9+C10 f93a395: TILES.md line-number drift corrected; WAL version byte
  (byte 8, V0/V1) documented. Adding a record type does NOT bump version.
- C11 1d7edd0: validation-edge-cases spec ↔ code confirmed current.
- C12 75dd74b: 10 correctness invariants cross-referenced spec ↔ code.

## 20:04

D4 f771680: annotated per-subscriber hot-path heap allocs in marketdata.
D3 1fea075: clippy auto-fix sweep, 13 → 6 warnings.
Refine-passes final report 8ca201b: 27 rounds shipped across buckets A-D.

## 20:42

Pre-v0.2.0 docs sweep 34c535c: 19 files refreshed against current code.
v0.2.0 release 8c48b94 tagged.

## 20:49

WEDGE.md decision locked 6db4382: **B+A** — exchange-in-a-box SDK on top
of orthogonal open-source parts (especially rsx-dxs: WAL + CMP). The demo
is the proof; the SDK is the product.

## 20:53

T3.2 risk promotion refactor: e2e test 2ce09fe + two commits refactoring
state-machine loop (8c76ec1, 2c58c9e); replaces env::set_var + recursive
run_main pattern. Spec + report cd79623.

## 21:06

Five integration test gaps filled 1753b50 across risk/gateway/matching.

## 21:28

Regression discovered: dev JWT secret regressed to 16 bytes in a6a92c3.
Fixed back to 32 f1f8e45.

## 21:52

Gate wiring fixes ab25683: `make test` now runs all tests; ci wires
integration; bench gates removed (benches ≠ correctness checks).
Trivially-passing + silent-skip tests pruned 27a08e5.
Codex findings b19ac52: honest bench labels, persist worker, doc claims.
Docs sweep 6ff6afa + a648735: bench-label honesty across 3 more docs.

## 21:56

e2e_us baseline a065337: latency-publish health probe fixed; first
bench-baseline.json capture.
Risk O(1) lookup a648735 + 3d151f1: killed 11.7ms hot-path scans; per-user
position lookup and fill dispatch now O(1) via FxHashMap.

## 22:07

Playground UI audit 21e7317: **12 findings** filed in FINDINGS.md.
trade UI link opens in new window 9f2e1a0.
Tests fixed 03d80bd: stress_integration JWT + play_safety audit-log per spec.

## 22:17

Shipped .ship/ prune 8d47b4b; TODO.md refreshed with audit punchlist.
Play test cascade-restore + removed-API mock fix c7017d5.

## 22:37

F12 fixed 37f6edf: trade UI no longer shows red "Authentication failed"
toast on first paint before sign-in. Distinguishes signed-in vs logged-out
state; private WS close(4001) only surfaces error when token was present.
Two play_trade.spec.ts tests added.

## 22:42

Oracle adversarial pass ffdfcfb: **7 more dashboard lies** (F13-F19).
Critical: F13 /x/pulse shows green if ANY process up; F14 circuit-breaker
'closed' hardcoded; F16 funding index_px synthetic (mark × 1.0001).
Filed with line citations + playwright test names. Carries to next session.

## 22:43

F1 fixed 0120806: CMP SO_REUSEPORT + SIGTERM drain + 1MB wire ceiling.
Stops the ME restart cascade (AddrInUse panic → 45GB marketdata OOM + WAL
truncation). Socket2 bind + matching graceful shutdown. Regression test in
play_readiness.spec.ts polls /api/processes for 5min.

## 22:44

F2-F11 fixed c190aea: truthful health, verify, WAL, topology across
server.py + pages.py. 9 findings in one batch.
