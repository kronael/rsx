---
summary: |
  RSX perp exchange. Solo work. 2026-W07 day 1.
  - spec compliance audit across all 6 crates; 222 tests pass.
  - CRITIQUE fixes landed: order IDs (UUIDv7), risk binary,
    DXS sidecar wiring, panic handlers, macros.
  - open: frozen margin still not released on cancel/done.
  - open: marketdata topology still violates spec (CMP not SPSC).
---

## 14:22

Spec compliance audit against specs/v1/ — first systematic pass across all 6
crates (3dfb971). Fixes: `FinalStatus`/`OrderStatus`/`FailureReason` enums in
rsx-types; OrderSlot uses Price/Qty newtypes; WAL record layouts corrected
field-by-field per DXS.md; BBO event variant added. 222 tests pass, 0 clippy.

## 20:07

CRITIQUE fixes checkpoint (74036b5): UUIDv7 order IDs wired, rsx-risk binary
compiled (was missing `main.rs`), DXS sidecar wiring started, panic handler
`install_panic_handler()` added to all binaries, `assert_invariant!` /
`debug_assert_eq!` macros extracted to rsx-types. Cargo.lock grew 1667 lines
(new deps for testcontainers + prost). PROGRESS.md refreshed. CRITIQUE.md
updated — 2 critical items remain open.
