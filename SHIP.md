ship the app.described by spec
use .md files to keep track inspect to see which is which
use Progrress.md and citritique.md uppercase to go back and forth
stop once sll trsts work and describe all edge cases
adhere tow isdom and dumb code
make use of cargo check
spec is in specs/*.md
