[project]
name = "rsx-playground"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "sse-starlette",
    "psutil",
    "httpx",
    "jinja2",
    "asyncpg",
    "python-multipart",
    "pytest",
]
