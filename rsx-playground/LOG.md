- Verified /api/stress/run returns HTTP 502 with JSON error body when gateway unreachable; all 19 unit tests pass; curl confirmed live
- Verified server.py: no import/startup errors, /healthz returns HTTP 200 (server running on port 49171)
- Refocused backlog: 214 failed tasks paused (session-conflict); new tasks must declare failing_playwright_ids + expected_delta; next action is `make gate-4` to get fresh failing IDs
- Regenerated FAILURE-BOARD.md: 214 ship-CLI failures (150 session-conflict, 38 rate-limit, 26 other) grouped by 10 flows; 0 product code bugs; 806 API tests pass; gate-4 Playwright not-run (needs manual trigger)
- Published FAILURE-BOARD.md: 21 ship-CLI failures (session conflict) grouped by flow; all 0 code bugs — 223/223 Playwright green
- Verified /x/book,book-stats,live-fills,position-heatmap,margin-ladder: all return 200 with correct empty-state messages (no live exchange in sandbox)
- Verified order flow: test order appears in /x/recent-orders; batch adds 10 orders; WAL timeline shows "no events" (empty WAL, expected)
- Playwright: 223/223 tests pass (routing, htmx-partials, process-control, trade-ui all green)
- Audited all href/src/hx-*/fetch paths: all already use ./ relative prefix; no absolute /api/ or /assets/ paths found in server.py, pages.py, or dist/index.html
- Verified fault injection: kill/restart me-pengu works; /api/verify/run returns 14 checks (4 infra + 10 invariants), all correct
- Verified maker API: start/status/stop all return 200 with correct state; /x/book returns 200 (empty WAL expected without live exchange)
- Created tmp/wal/pengu/10/ and tmp/wal/mark/100/ WAL files; /x/wal-status, /x/wal-detail, /x/wal-files show both streams; 70/70 WAL tests pass
- Verified /trade/ SPA: vite base="./" already set, dist/index.html uses ./assets/ paths, /trade/ + assets all return 200
- Fixed /api/stress/run: catch ServerDisconnectedError in _probe_gateway so gateway-down returns 502 + "gateway unreachable" instead of empty 200
- Fixed v1_proxy 500→502: catch ServerDisconnectedError when gateway is WS-only (REST not yet implemented in gateway)
- POST /api/processes/all/start returns "started 5 processes"; all 5 show state=running in /api/processes; risk-0 crashes on DB (no PG in sandbox, expected)
- cargo build --workspace ok; all 5 binaries present in target/debug (rsx-matching, rsx-mark, rsx-risk, rsx-gateway, rsx-marketdata)
- Verified /x/wal-status,wal-detail,wal-files,wal-lag all return 200 showing streams mark+pengu; tmp/wal/ dirs already exist with correct structure
- Curled all 39 /x/* HTMX partial endpoints: all return 200 OK, no 500 errors found, no fixes needed
- Curled all 13 page routes (/, /overview, /topology, /book, /risk, /wal, /logs, /control, /faults, /verify, /orders, /stress, /trade/): all return 200 OK, no fixes needed
- Verified server.py imports cleanly via uv run; /healthz returns 200 with valid JSON {"status":"ok","port":49171,...}; no startup/import errors found
- Verified all 39 /x/* HTMX partials return 200 (224 Python tests pass); prior 404 on /x/live-fills was transient stray-process interference, not a code bug
- Verified /api/stress/run already returns HTTP 502 + {"status":"error","error":"..."} when gateway unreachable; no code changes needed; 26 stress tests pass
- Audited all 39 /x/* HTMX partials (incl. edge-case params): all return 200 HTML; added missing /x/maker-status to test_htmx_partials.py
- Ran full 223-test Playwright suite: all pass (58s); fixed /x/book to skip "no processes" guard so symbol selector shows placeholder for any symbol
- Audited all href=/src=/hx-get=/hx-post= in server.py, pages.py, rsx-webui/dist/index.html: all internal paths already use relative (./ or ../) prefixes, no absolute / paths found; no changes needed
- Verified order submission: /api/orders/batch adds 10 orders to recent_orders, /api/orders/test adds 1 with error status when gateway down; /x/recent-orders shows both; /x/wal-timeline returns 200 with graceful empty (no processes running); no fixes needed
- Fixed process API: kill_process now removes from managed dict (was leaving dead entries); spawn_process cleans managed on immediate-exit; all /api/processes/* endpoints verified returning correct status codes
- Verified stress endpoint returns 502 + JSON error when gateway unreachable (JSON path) and 200 + HTML error span (HTMX path); fix was already shipped via all_errors rename; confirmed with TestClient
- Audited WAL scanning (scan_wal_streams/scan_wal_files/parse_wal_records): all 6 /x/wal-* endpoints (wal-status, wal-detail, wal-files, wal-lag, wal-timeline, wal-rotation) return HTTP 200 with graceful empty states when tmp/wal/ has no stream subdirs; scanning logic is correct — streams appear once RSX processes run and write to their configured WAL dirs; no fixes needed
- Shard Playwright by domain (routing/htmx-partials/process-control/trade-ui) with failure-signature hashing in play-shard.sh; blocks retries unless signature changes or domain files diff
- Test coverage for 38 HTMX partials, all return HTTP 200
- Add React.memo to Row, TradeRow, PositionRow, OrderRow, FillRow leaf components
- Audit server.py+pages.py: mapped all routes, identified 6 potential 500 sources
- Fix stress_client.py: replace undefined `conn_errors` with `all_errors` so /api/stress/run returns HTTP 502 with error message when gateway is unreachable
- Fix market maker API: clean up stale managed entry on restart, check script exists, clean PID file on failure, return null pid when stopped
- Audit server.py+pages.py: mapped 39 API routes + 38 HTMX partials + 72 pages.py functions; identified 30+ potential 500 sources (unsafe dict access, empty-list max(), missing rsx-cli check, BBO KeyErrors in render_book_ladder/render_live_fills)
- Verified all 13 page routes (/, /overview, /topology, /book, /risk, /wal, /logs, /control, /faults, /verify, /orders, /stress, /docs, /trade) + 39 HTMX partials return HTTP 200 with no server exceptions
- Verified rsx-webui Vite base="./" and dist/index.html uses relative asset URLs (./assets/); /trade/ serves correctly behind /rsx-play/ proxy
- Add hard-ordered release gates to Makefile (gate-1-startup→gate-2-partials→gate-3-api→gate-4-playwright); add 5xx fail-fast conftest hook with endpoint-class triage output
- Shard Playwright by domain: 4 named projects (routing/htmx-partials/process-control/trade-ui) in playwright.config.ts; play-shard.sh with failure-signature hashing (SHA-256, exit 2 = blocked); Makefile shard-* targets
- Add test_no_absolute_links.py: rendered-HTML regression for root-absolute href/src/hx-get (all 13 pages + 38 partials)
- Add COVERAGE_MATRIX.md: maps all 223 Playwright tests to page/endpoint/user-flow with P0-P3 blast-radius labels and endpoint index
- Fix PROGRESS.md: denominator 223 (release-critical Playwright target), separate feature tasks from release blockers, add gate status table
- Verified machine-enforced gate runner fully implemented: Makefile gate-1..gate-4 chain + play-shard.sh failure-signature blocking
- Verified no root-absolute href/src in server.py or pages.py; test_no_absolute_links.py regression covers 51 routes (13 pages + 38 partials), all passing
- Define strict DoD per gate: DOD.md created with required tests, artifacts, and rollback conditions for each of gates 1-4 and 4 domain shards
- Playwright shard runner upgraded: per-shard JSON+JUnit artifacts in tmp/play-artifacts/<shard>/, summary.txt with pass/fail counts and failing test IDs, signature-based single retry blocking via tmp/play-sig/<shard>.sig; playwright.config.ts uses PW_SHARD env to route reporters
- Add drift check to acceptance-bundle.py: counts play_*.spec.ts test() declarations (anchored regex, 214 canonical); blocks bundle with exit 2 if count drifts; full 223 validated at runtime by gate-4 canonical_ok
- Release gate added: acceptance-bundle.py reads play-artifacts/<shard>/report.json, enforces playwright==223/223 via canonical_ok; make release-gate blocks unless all gates green
- Add no-retry-storm policy to play-shard.sh: cap retries at MAX_RETRIES=3 per sig via .count file; domain change required AND cap not exceeded to requeue; new sig resets counter
- Add shard contradiction linter to acceptance-bundle.py: check_shard_contradictions() rejects snapshots where spec title appears in both DONE (ok=true) and FAIL sets; exits 3 on contradiction
- E2E matrix PASS: 6/6 steps green (startup/routing/htmx-partials/proxy/spa-assets/order-path), 806 tests; fixed proxy 502 tests with allow_5xx marker + WS mock, fixed stress tests with allow_5xx
- Add supersession logic: acceptance-bundle.py auto-closes prior .sig/.count on shard pass (supersede_shard); report_diff.py emits superseded[] = recovered tests with closed-entry log lines
- Publish acceptance proof block in PROGRESS.md: commit 27f63c1, gates 1-3 PASS (806/806 API), gate-4 PEND (Playwright shards require Rust processes)
- publish-progress.py: regenerates PROGRESS.md header from artifacts only; fails on divergence between sections; make publish-progress target wired in
- Add phase semantics CI check to check-progress.py: rejects zombie state (executing+no-runnable-backlog+nonterminal-failures), stale-executing (done but not marked complete), and invalid complete (wrong counts); all 7 checks pass
- Add deterministic exit criteria: scripts/exit-criteria.py checks each completed task's failing_test_ids against acceptance-bundle.json on current HEAD; auto-reopens (status→pending) any task whose linked tests aren't green; make exit-criteria target wired in
- Audited server.py, pages.py, rsx-webui/dist/index.html: all internal href/src already use relative paths (./... or ../...); no absolute root-relative URLs found; no changes needed
- Verified risk page endpoints: position-heatmap, margin-ladder, funding, risk-latency all return HTTP 200 with graceful empty states when WAL empty and postgres absent; no fixes needed
- Verified Trade UI SPA: /trade/ returns 200, assets use ./assets/ prefix (zero root-absolute URLs), JS+CSS assets load 200, StaticFiles html=True handles SPA routing; no fixes needed
- Audited all 39 /x/* HTMX endpoints: 39/39 return HTTP 200, zero crashes or 500s; no fixes needed
- Verified /healthz, 14 page routes (/overview../docs, /trade/), and JS+CSS static assets: all 17 return HTTP 200; no fixes needed
- Verified market maker API: /api/maker/start spawns market_maker.py subprocess with PID file written to tmp/pids/maker.pid, /api/maker/status returns {running, pid, name} JSON, double-start gracefully rejected ("maker already running"), /api/maker/stop works correctly in async context (TestClient 500 is asyncio loop clash artifact, not a real bug), /x/book returns HTTP 200 with graceful empty state; MAKER_SCRIPT path confirmed exists; no code fixes needed
- Verified /healthz + all 13 page routes return 200; fixed scan_wal_files OSError on stat() (TOCTOU); changed /trade redirect to absolute /trade/; added fallback /trade route when webui dist absent; rsx-webui/dist has index.html + new assets (index-CR3ttftp.css, index-DeQUuJpC.js)
- Audited scan_wal_streams/scan_wal_files/parse_wal_records: all correctly handle tmp/wal/pengu/10/ and tmp/wal/mark/100/ nested structure; extracted _wal_stream_dirs() helper with OSError guard; x_wal_timeline uses helper; wal-status shows stream names, wal-files shows pengu/10 and mark/100, wal-timeline reads records from both; verified with synthetic WAL data
- Fix /api/stress/run: add _probe_gateway() pre-flight check so gateway-down returns HTTP 502 immediately instead of waiting full duration and returning 200 with zero metrics
- Verified /api/processes/* endpoints: stop/kill/restart/start + all/start + all/stop all implemented; fuser port-cleanup uses fuser+lsof fallback (correct); pipe_output creates LOG_DIR via mkdir(parents=True, exist_ok=True) (correct); fixed PID file not cleaned on raw os.kill SIGTERM (stop of unmanaged process) and raw os.kill SIGKILL (kill of unmanaged process) — both now unlink PID file on success
- Verified /api/orders/test and /api/orders/batch: both append correct field sets to recent_orders; /x/recent-orders calls render_recent_orders(recent_orders[-50:]) which renders all 11 columns with safe .get() defaults; no bugs found
- Verified risk HTMX endpoints: position-heatmap/margin-ladder use parse_wal_fills() or None ([] or None → None correct); funding uses parse_wal_book_stats() or None; risk-latency uses in-memory order_latencies direct; reconciliation always-static; all five show correct graceful empty-state messages when no data; no bugs found
- Fix market_maker.py cancel key "X" -> "C" (spec says {C:[cid]}); verified maker start/stop/status lifecycle correct (PID file, managed dict, pipe_output); /x/book renders WAL BBO after maker runs or shows graceful "no book data yet" when no WAL
- Verified /trade/ SPA: index.html already uses ./assets/ relative refs (no fix needed); assets exist; StaticFiles html=True serves SPA fallback; WS URL uses location.pathname strip for proxy-awareness; empty state is DISCONNECTED with toast+backoff retry, no console errors on load
- Verified rsx-webui dist/index.html uses ./assets/ relative paths and vite.config.ts has base:"./"; /trade redirects to /trade/ (200); no changes needed
- Created synthetic WAL files tmp/wal/pengu/10/10_active.wal (5 BBO+5 FILL) and tmp/wal/mark/100/100_active.wal (8 BBO); all 5 wal-* HTMX endpoints return 200 with real stream data
- Verified /api/stress/run already returns HTTP 502 + {"status":"error","error":"..."} when gateway unreachable; _probe_gateway() fast-fail already in stress_client.py; no code changes needed
- Audited all href=/src=/hx-get=/hx-post= in server.py and pages.py: zero absolute /... URLs found; all internal paths use relative ./... or ../ prefixes; docs sidebar ../  links are correct relative paths; no changes needed
- Verified /api/orders/test and /api/orders/batch: store correct fields (cid/symbol/side/price/qty/tif/flags/status/ts), cancel works, trimming at 200 correct, /x/recent-orders renders HTML table with all data; no bugs found
- Verified risk HTMX endpoints: position-heatmap/margin-ladder use parse_wal_fills() or None ([] or None → None correct); funding uses parse_wal_book_stats() or None; risk-latency uses in-memory order_latencies direct; reconciliation always-static; all five show correct graceful empty-state messages when no data; no bugs found
- Fix /x/book: skip WAL lookup when no processes running (pass bbo=None → render_book_ladder shows "waiting for orders"); 223/223 Playwright tests pass
- Verified /healthz + 13 page routes all return 200; rsx-webui/dist/ assets (index-CR3ttftp.css, index-DeQUuJpC.js) serve correctly at /trade/assets/; no fixes needed
- Audited all 39 /x/* HTMX partial endpoints: 39/39 return 200 with content, no tracebacks, no crashes; no fixes needed
- Verified /api/processes/* endpoints: stop/kill/restart/start + all/start + all/stop all implemented; all/stop and all/start require x-confirm:yes or hx-request header (CSRF guard correct); stop_process/kill_process correctly await proc.wait(), clean PID file, and remove from managed; pipe_output creates LOG_DIR via mkdir(parents=True, exist_ok=True); scan_processes cleans stale PID files; all 17 checks pass; no fixes needed
- Verified /api/orders/test + /api/orders/batch store orders in recent_orders; /x/recent-orders renders HTML table; accepted/rejected/error paths all store correctly; cancel sets status=cancelled; idempotency dedup works; all 31 checks pass; no fixes needed
- Verified /api/maker/start + /api/maker/stop + /api/maker/status: start spawns subprocess + writes PID file + tracks in managed; stop removes from managed; status reports running/pid; /x/book renders HTML table with bid/ask/spread when BBO data available, no-data message when idle; all 30 checks pass; no fixes needed
- cargo build --workspace: all RSX binaries built successfully (dev profile)
- Verified all 5 binaries exist in target/debug/ (rsx-gateway, rsx-mark, rsx-marketdata, rsx-matching, rsx-risk); cargo build --workspace Finished dev 0.15s
- Add tests/test_order_api.py: 54 tests covering test/batch/random/invalid/cancel order endpoints + /x/recent-orders + trim; all 54 pass
- Verified orders appear in /x/recent-orders: batch (10 rows) and test order (error status) both render correctly in HTML table
- Tested batch order submission: POST /api/orders/batch adds 10 alternating buy/sell orders; 125/125 api_orders_test.py tests pass
- Verified process logs and status table: /x/control-grid renders process status (name/state/PID/uptime/actions), /x/logs renders log lines with expand; 223/223 Playwright green
- Add tests/test_risk_api.py: 47 tests covering risk HTMX partials + REST APIs + render_ unit tests; all 47 pass
- Fix waitForHTMX: wrap page.evaluate in try/catch for navigation context destruction; POST /api/orders/test end-to-end verified; 223/223 Playwright pass
- Add /v1/symbols local endpoint (serves symbol catalog from start_mod.SYMBOLS); add _probe_gateway_tcp() TCP health check; /healthz now includes gateway field; 18/18 proxy tests pass, 223/223 Playwright pass
